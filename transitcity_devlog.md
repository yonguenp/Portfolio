# 트랜짓시티 — 개발 히스토리 (Devlog)

`transitcity.md`(기획서, §1~18)와 분리한 개발 로그. **처음부터** 나눠서
시작한다 — GoStop이 기획서와 개발 히스토리를 한 파일(CLAUDE.md)에 계속
append하다가 15,564줄까지 불어났던 걸 반복하지 않기 위해서다(§18.3 참고).

- `transitcity.md` — 무엇을·왜 만드는지(기획/시스템 설계). 결정이 바뀌면
  여기를 고친다.
- 이 파일 — 실제로 구현하면서 무엇을·어떻게 고쳤는지, 버그와 원인, 검증
  결과. 시간순으로 append.
- 사용자가 직접 수기로 기능 검증을 담당하기로 했으므로(§18.2), 이 로그의
  "검증" 항목은 컴파일 성공 여부 등 Claude가 확인 가능한 범위로 한정될
  것으로 예상 — 기능 동작 확인은 사용자 쪽 기록/피드백에 의존한다.
- 이 파일이 나중에 GoStop처럼 한 하위 시스템(예: 교통 시뮬레이션)에서만
  비대해지면, 그때 `transitcity_devlog_<주제>.md` 형태로 쪼갤 것 — 지금은
  아직 내용이 없어 미리 나눌 근거가 없다.

---

## 프로토타입 v0 — 첫 플레이어블 슬라이스 (2026-09-22)

### 실행 방법

`Assets/Scenes/TransitCityScene.unity`를 열고 Play. 좌하단 "소로 건설" 버튼을
누르면 건설 모드가 켜지고(초록 테두리), 보라색 박스(이웃 도시) 옆 빈 땅을
클릭하면 도로가 착공된다. 도로가 완공되면(바닥에서 솟아오르다 "짠" 하고
튕김) 그 도로에 닿은 빈 땅에 주거지가 자연 발생하고(초록 박스, 인구가
늘수록 높아짐), 인구가 쌓이면 도로가 초록→빨강으로 혼잡해지고, 가끔 노란
박스(장식용 차량)가 집에서 이웃 도시까지 달려간다.

### 구현 범위 (실제로 동작하는 것)

- §4.1(도로, 단 격자 **타일**로 단순화 — 아래 참고) + §4.7(건설 시간, 짓는
  동안 "공사 중" 상태로 표시)
- §5.1~5.3(개발 포텐셜 → 주거지 자연 발생 → 인구 성장)을 대폭 단순화한
  버전 — 이웃 도시 1개, 소득 계층 없음, 화물/상업/공업 없음
- §6(교통 흐름)도 단순화 — BFS 최단경로 기반 통행량 누적, BPR 공식 없이
  선형 임계치로 혼잡 판정
- §11(단순 3D 박스 아트, 도로/차량 포함)
- §18의 아키텍처 컨벤션 전부 적용: asmdef 분리, ScriptableObject 데이터
  (BalanceConfig/RoadTypeData), Command 패턴(BuildRoadCommand), 상태 패턴
  (BuildingState/CompletedState), 상속(GridVisualBase→Road/HouseVisual),
  오브젝트 풀링(차량), 이벤트 기반(Update()엔 입력 폴링만, 시뮬레이션 로직은
  전부 이벤트로 전파), SerializeField 참조, 로깅 유틸(Logger, 기본 무음)

### 의도적으로 줄인 스펙 — 왜 줄였는지

이건 "언젠가 다 만들 것"이라는 뜻이지 "이게 최종 설계"라는 뜻이 아니다.
transitcity.md 본문의 설계를 바꾼 게 아니라, **하룻밤에 핵심 루프 하나가
재밌는지 검증**하는 게 목적이라 스펙을 잘라냈다.

1. **도로가 "타일 변 그래프"가 아니라 "타일 자체"** — §4.1은 도로가 타일
   변 위에 있는 그래프라고 설계했는데, 프로토타입은 고전 SimCity처럼
   도로가 타일 자체인 모델로 축소했다. 구현 시간이 훨씬 짧고, BFS
   경로탐색도 훨씬 단순해서 하룻밤에 "된다"는 걸 보여주는 데 유리했다.
   실제 프로덕션에서는 교차로 처리(§4.6)나 도로 폭 표현이 타일-그래프
   모델과 잘 안 맞을 수 있어 재검토가 필요하다.
2. **소득 계층(§5.5)·화물 산업 체인(§5.4)·공공 인프라(§5.8)·만족도(§5.9)
   전부 없음** — R zone 하나만 있다. §13.2/13.3에서 이미 Phase 2~3으로
   미뤄뒀던 것들이라 예정대로 뺐다.
3. **BPR 혼잡 공식(§6.3) 대신 단순 임계치** — `트립수/용량` 비율로 색만
   바꾼다. 실제 "실통행시간 증가로 인한 포텐셜 하락" 피드백 루프(§6.4)는
   아직 없다 — 도로가 막혀도 지금은 시각적으로만 빨개질 뿐, 그게 다시
   인구 성장을 늦추지는 않는다.
4. **이웃 도시 1개, 정적** — §9.1~9.3(여러 이웃 비교, 포화도 기반 판단)은
   전혀 없다. 그냥 지도 한쪽에 고정된 인구원 하나.
5. **건설 진행 애니메이션이 완벽히 부드럽지 않을 수 있음** — 시뮬레이션
   틱(2초 간격)과 별개로 건설 진행은 매 프레임 갱신하도록 분리해뒀지만,
   `buildTimeSeconds`(2.5초)가 짧아서 체감상 "쑥" 하고 빠르게 솟아오르는
   정도다 — 나중에 더 느린 인프라(철도·지하철)가 생기면 이 분리 구조가
   진가를 발휘할 것.
6. **카메라 조작 없음** — §11에서 "확대/축소, 좌우 드래그"를 언급했는데
   지금은 고정 시점(정투영, 살짝 위에서 내려다보는 각도)이다.
7. **저장/불러오기, 배속, 일시정지 없음** — §17.1에서 이미 공백으로
   짚어뒀던 것, 프로토타입엔 아직 없다.
8. **밸런스 수치는 전부 감으로 잡은 값**(BalanceConfig.asset) — 도로
   50원, 인구 성장률 등. 플레이하면서 바로 에디터에서 조정 가능하니
   느낌 보고 직접 만져봐도 됨.

### 방향을 고치면 더 좋을 것 같은 것 — 제안

- **혼잡의 피드백 루프(§6.4)를 빨리 연결하는 게 최우선 순위라고 본다.**
  지금은 도로가 빨개지는 게 순수 장식이라 "그래서 뭐?"가 된다 — 혼잡한
  구간을 지나야 하는 집의 인구 성장이 느려지게만 연결해도, "여기 막히니까
  우회로를 뚫어야겠다"는 판단이 처음으로 의미를 가진다. 이게 이 게임의
  핵심 재미(§2)가 실제로 느껴지기 시작하는 지점이라고 본다.
- **"자잘한 디테일"을 더 넣을 자리가 많다** — 말씀하신 대로 참고해서
  이번에 건설 펀치스케일/집 성장/차량 정도만 넣었는데, 다음으로 넣으면
  좋을 후보: 도로 클릭 시 살짝 튕기는 커서 피드백, 인구 카운터 숫자가
  스냅이 아니라 굴러가듯 세는 연출, 집이 인구로 꽉 차면(포화) 아주 살짝
  반짝이는 표시, 막 완공된 도로에 반짝이는 파티클 한 번.
- **소도시 언락(§9) 없이도 대로/철도를 조금 일찍 맛보게 할지 고민.**
  지금은 소로 하나뿐이라 "교통망 설계"라는 느낌이 아직 약하다 — 혼잡
  피드백이 연결된 다음 단계로, 대로(더 비싸지만 용량 큰) 하나만 추가해도
  "소로로 다 채우기 vs 대로 하나로 몰기"라는 최소한의 설계 판단이 생긴다.

### 같이 만들어보고 싶은 것 (우선순위 순 제안)

1. **혼잡 → 성장 피드백 연결**(위 참고) — 가장 적은 코드로 가장 큰 재미
   변화를 만들 수 있는 지점이라고 본다.
2. **대로(2번째 도로 계층) 추가** — RoadTypeData가 이미 재사용 가능한
   구조라 .asset 하나 더 만들고 빌드 바에 버튼 하나 추가하는 정도로 끝남.
3. **카메라 확대/축소·드래그** — 지금 고정 시점이라 큰 지도를 만들면
   답답해질 것.
4. **일시정지·배속(1x/2x/3x)** — §12에서 이미 설계했던 것, 손 놓고 지켜보는
   재미(§2 "지켜보는 재미")에는 배속이 특히 중요할 것 같다.
5. **철도(§4.2) 하나** — 단선/복선·폐색 구간 같은 "변태 디테일"까지는 아직
   이르고, 우선 "여객철도 = 대량 인구 유입"만 맛보기.
6. **소득 계층(§5.5) 첫 단계** — 백분위 정규화 하나만 넣어서, 같은 도시
   안에서도 어디가 "더 좋은 자리"인지 색으로 구분되기 시작하는 지점.

### 겪은 함정 (다음에 또 안 겪게)

- **`UIDocument.panelSettings`를 C# 프로퍼티로 직접 대입하면 Edit 모드
  스크립트에서는 저장이 안 될 수 있다.** `uiDoc.panelSettings = x;`로
  설정했는데 씬을 저장하고 다시 열어보니 `m_PanelSettings: {fileID: 0}`
  (null)이었다 — 이 프로젝트가 GoStop 등에서 이미 여러 번 확인한
  "SerializedObject로 필드를 직접 써야 확실하다"는 교훈이 UIDocument
  컴포넌트에도 그대로 적용된다. `new SerializedObject(uiDoc).
  FindProperty("m_PanelSettings").objectReferenceValue = ...` +
  `ApplyModifiedPropertiesWithoutUndo()`로 고쳤다.
- **`unity command eval_file`이 "Main thread operation timed out after
  5000ms"를 반환해도 스크립트가 끝까지 실행됐을 수 있다.** 씬 생성·저장
  같은 무거운 작업 2건에서 이 에러가 났는데, 둘 다 실제로는 파일이 정상
  저장돼 있었다(GoStop 히스토리에 이미 기록된 것과 같은 패턴). **에러가
  나면 바로 재시도하지 말고, 먼저 디스크에 실제로 뭐가 만들어졌는지 확인할
  것** — 재시도하면 중복 생성될 위험이 있다.
- **큐브 프리미티브의 피벗이 중심이라, `localScale.y`만 키우면 바닥이 아니라
  중심에서 위아래로 동시에 자란다.** "도로가 바닥에서 솟아오른다"는 연출을
  의도했는데 처음엔 반쯤 땅에 파묻힌 채로 자랐다 — `RoadVisual`/`HouseVisual`
  에 `SetScaleGrounded()`(스케일 바뀔 때마다 `localPosition.y = scale.y/2`로
  같이 보정)를 넣어서 고쳤다.

## 프로토타입 v0.2 — 플레이 피드백 반영 (2026-09-22)

v0.1을 실제로 플레이해본 뒤 받은 피드백 7건을 모두 반영했다.

### 고친 것

1. **도로 구간 드래그 건설** — `TransitCityManager.Update()`가 이제
   `leftButton.isPressed`를 매 프레임 확인해, 마우스를 누른 채 지나가는
   칸마다 한 번씩 `BuildRoadCommand`를 실행한다(`PaintCoord`, 같은 칸
   중복 실행 방지). 한 칸씩 클릭하던 v0.1의 답답함 해소.
2. **건물이 있으면 도로 불가 + 철거 도구** — 이미 `CanBuildRoadAt`이
   빈 땅만 허용해서 건물 위엔 도로가 안 지어졌는데(의도된 동작), 되돌릴
   방법이 없었다. `DemolishCommand`/`CityGridModel.DemolishAt` 추가 —
   도로 또는 주거지 한 칸을 철거해 다시 빈 땅으로 되돌린다. HUD에
   "철거" 버튼과 Inspect/BuildRoad/Demolish 3모드(`EditMode`)를 추가.
3. **차량이 도로 아래를 달리던 버그** — 원인 확정: `CarVisual`이 쓰는
   경로 좌표가 항상 로컬 Y=0(`CoordToLocal`)이었는데, 도로 비주얼은
   `SetScaleGrounded`로 바닥(Y=0)에서 위로 쌓아 올려져 있어(완공 높이
   1.2) 차가 도로 슬래브의 "바닥면"을 달리고 있었다. `TransitCityManager`에
   `carRideHeight`(도로 완공 높이 + 차량 절반 높이) 상수를 추가해 차량
   경로 좌표에 전부 이 오프셋을 더했다.
4. **보라색(이웃도시) 근처 7칸에만 건물, 나머지는 영구 빈 땅** —
   `maxUsefulDistance`(10→26)와 `residentialPotentialThreshold`
   (0.3→0.15)가 너무 빡빡해서 도로망 거리 7칸을 넘으면 포텐셜이
   바로 0에 가까워졌다. 값을 완화해 거리 22칸 정도까지는 개발 가능하게
   늘렸다(격자가 14×10이라 맵 대부분을 커버).
5. **건물 클릭 시 정보 패널** — Inspect 모드(기본 모드)에서 주거지를
   클릭하면 HUD 우상단에 좌표/인구/재산 수준(상·중·하)/만족도가 뜬다.
   `TransitCityManager.OnBuildingSelected`/`OnBuildingDeselected` 이벤트로
   UI와 분리했다.
6. **건물 크기 다양화** — 주거지 발생 시점에 `CityTile.SizeTier`(포텐셜
   기반 + 약간의 무작위)를 한 번 정해서, 같은 인구 비율이어도 건물마다
   가로/세로/높이가 다르게 보인다.
7. **소득계층/만족도/공공인프라 — v0.1에서 가장 크게 지적받은 부분**.
   세 가지를 한 번에 엮어서 넣었다(자세한 설계는 아래 참고). 화물
   체인/상업·공업 구역은 여전히 없다 — 이건 이웃도시 하나뿐인 현재
   구조로는 만들 수 없어서(교역 상대가 필요) 의도적으로 v0.3으로 미룸.

### 소득계층·만족도·공공인프라 (§5.5.1/§6.4/§5.8 첫 단계)

- **재산 계층(Wealth)**: 절대 포텐셜이 아니라 "다른 주거지 대비 상대
  백분위"로 계산한다(§5.5.1 정확히 그대로) — 매 틱, 주거지 전체를
  포텐셜로 정렬해 0(최빈)~1(최부유)을 매긴다. 집 색이 이걸로 바뀐다
  (빈곤=베이지, 부유=파랑).
- **만족도(Satisfaction) + 혼잡 피드백(§6.4)** — v0.1 devlog에서 "최우선
  순위"로 꼽았던 바로 그 기능. 각 주거지는 자기 진입 도로의
  혼잡도(트립수/용량)를 확인하고, **부유할수록 혼잡에 더 민감하게**
  (§5.6 부유층 이탈의 전조) 만족도가 깎인다. 만족도는 그 틱의 인구
  성장률에 직접 곱해져서(최저 15%까지 감쇠), 혼잡한 곳은 눈에 보이게
  성장이 느려진다 — "여기 막히니 우회로가 필요하다"는 판단이 처음으로
  의미를 가짐.
- **공공시설(Utility) 자동 배치(§5.8 단순화)** — 누적 인구가
  `utilityPopulationStep`(기본 40)의 배수를 넘을 때마다 "밀린 공공시설"
  카운트가 쌓이고, 그 다음 새로 개발 가능해지는 빈 땅이 나타나는 즉시
  그중 하나가 공공시설로 배정된다(회색 박스, 인구 성장 없음). 반경
  내 주거지에 만족도 보너스를 준다. 도로도 못 짓고 인구도 못 사는
  칸이라 은근히 "여기는 건드리지 마라"는 제약이 하나 더 생긴 셈.

### 테스트 중 발견한 재미있는(그리고 알아둘 만한) 동작

- **공공시설이 "밀린 채로" 쌓였다가 한꺼번에 터지는 문제를 헤드리스
  테스트로 잡았다.** 처음엔 "인구가 마일스톤 넘는 바로 그 틱에 마침
  새 빈 땅이 생겨야만" 배치되게 짰는데, 도로망이 안 늘어나는 동안은
  이 우연이 거의 안 맞아서 인구 340이 될 때까지 공공시설이 0개였다.
  `nextUtilityMilestone`을 넘을 때마다 `utilityBacklog`를 쌓아두고,
  나중에 어디서든 새 빈 땅이 열리는 순간 밀린 만큼 소진하도록 고쳐서
  해결 — `unity command eval_file`로 시뮬레이션만 떼어내 헤드리스로
  여러 시나리오를 돌려보며 확인했다(§18.2 검증 방식대로 Play 모드 없이).
- **도로 확장이 "경쟁 상태"에 걸릴 수 있다.** 도로 한 칸을 짓고
  2초(`tickIntervalSeconds`) 넘게 그대로 두면, 그 도로 바로 앞칸이
  자동으로 주거지가 돼버려서 같은 방향으로 더 못 뻗는다(철거 후 다시
  지어야 함). 드래그로 쭉 이어 지으면(항목 1) 이 틈보다 훨씬 빨리
  끝나서 실전에서는 거의 안 부딪히겠지만, "한 칸 짓고 한참 고민하다
  이어 짓기" 패턴이면 마주칠 수 있다 — 의도치 않은 부작용이지만
  "빨리 결정하고 이어 지어야 한다"는 나름의 텐션이라 일단 그대로 뒀다.
  거슬리면 `tickIntervalSeconds`를 늘리거나, 주거지 발생 시 "도로
  건설 중인 방향"은 잠깐 봐주는 규칙을 추가하는 걸 다음 후보로.

### 여전히 없는 것

화물체인/상업·공업 구역(§5.4), 다중 이웃도시 비교(§9), 카메라 조작,
저장/배속/일시정지 — v0.1 devlog에 적은 것과 동일하게 아직 손 안 댐.
화물체인은 이웃도시가 하나뿐이라 구조적으로 아직 불가능 — 두 번째
이웃도시(교역 상대)가 생겨야 의미가 있어서 다음 우선순위 후보.

## 프로토타입 v0.3 — 대로·니즈·상업/공업 (2026-09-22)

v0.2 피드백("재미없다", 도로 조작이 답답하다, 인프라가 안 생긴다) 반영.
"시뮬레이션의 재미를 느낄 수 있는 수준까지"라는 요청에 맞춰, 이번 판은
**도로 등급 선택이 실제 전략적 판단이 되게** 만드는 데 초점을 맞췄다 —
소로만 있던 v0.2는 사실상 선택지가 없어서 "재미없다"는 말이 나올 만했음.

### 도로 건설 방식 변경 — 드래그 → 2클릭 시작/끝점

v0.2에서 넣은 드래그 방식을 이번에 다시 걷어냈다. 첫 클릭이 시작점,
두 번째 클릭이 끝점이 되고 그 사이를 ㄱ자 경로로 이어 짓는다(가로 먼저,
세로 나중 — 가로/세로 직선도 이 경로의 특수한 경우라 별도 처리 불필요).
시작/끝 어느 쪽이 기존 도로망에 붙어있는지 몰라도 되도록, "지을 수 있는
칸이 없어질 때까지 반복 시도"하는 방식이라 양쪽 다 새 구간이어도, 한쪽만
붙어있어도 자연스럽게 이어진다. 우클릭으로 시작점 선택 취소, HUD 상단
중앙에 "시작점을 클릭하세요"/"끝점을 클릭하세요" 힌트가 뜬다.

### 일시정지

HUD에 "일시정지" 버튼 추가. 누르면 시뮬레이션 틱과 건설 진행 애니메이션이
전부 멈춘다(플레이어의 건설/철거 입력은 계속 받는다) — "철거하고 다시
지으려는데 그 사이에 건물이 들어와 버린다"는 문제를 근본적으로 없앴다.

### 대로(§4.1 두 번째 계층) 추가

`RoadTypeData`에 `tier`(Local/Avenue) 필드 추가, `MainRoad.asset` 신설
(140원, 완공 4초, 통행량 용량 45 — 소로의 3배). HUD에 "대로 건설" 버튼이
따로 있어 소로/대로를 골라 지을 수 있다. **핵심은 다음 항목** — 그냥
"더 비싼 도로"가 아니라 이게 뭘 가능하게 하느냐가 중요함.

### 대로변 전용: 상업·공업·인프라 (§5.4/§5.8 첫걸음)

이번 판의 핵심 설계 결정. **소로변엔 주거지만 들어서고, 대로변에만
주거/상업/공업/인프라가 섞여 들어선다**(대로 인접 빈 땅이 새로 개발
자격을 얻으면 50% 주거·25% 상업·25% 공업 중 하나로 정해짐). 이걸로
"도로를 어디에 어떤 등급으로 놓을지"가 실제 도시 형태를 결정하는
선택이 된다 — 소로만 뻗으면 조용한 주택가, 대로를 뚫으면 상공업이
섞인 번화가. transitcity.md §2의 "입력은 교통망뿐" 원칙을 살리면서
처음으로 "무엇을 지을지"가 아니라 "어디에 어떤 도로를 놓을지"만으로
도시 성격이 갈리는 걸 체감할 수 있게 했다.

상업(청록)·공업(주황)은 주거지와 다른 색으로 렌더링되고, 포텐셜에 따라
"활성도"(인구 대신 쓰는 이름, 클릭 정보 패널에 표시)가 자라난다. 다만
니즈(수도/전기 등)는 사람에게만 있다는 설계라 상업/공업엔 없음 — 성장도
만족도 게이팅 없이 포텐셜에만 비례(의도적 단순화).

### 니즈 시스템 — 수도/전기/치안/소방 (§5.8)

인프라 시설 4종(상수도/발전소/치안시설/소방시설, 각각 파랑/노랑/남색/
빨강)이 누적 인구 마일스톤(40명)마다 **대로변에** 하나씩 수도→전기→
치안→소방 순환으로 자동 배치된다. 각 주거지는 4가지 니즈 각각에 대해
가장 가까운 해당 시설로부터의 커버리지(0~1, 거리 감쇠)를 매 틱 계산하고,
**재산이 많을수록 요구 수치(InfraDemand)가 높다** — 판자촌은 전기가
좀 약해도 버티지만 부촌은 조금만 부족해도 민감하게 반응하는 설정.
건물 클릭 정보 패널에 4개 니즈 전부 "수도: 62% (충족)" 식으로 뜬다.
충족 못 한 니즈는 만족도를 깎아 인구 성장을 늦춘다 — 혼잡 피드백에
이어 두 번째로 연결된 "관리 안 하면 성장이 느려지는" 루프.

### "인프라가 안 지어지는 것 같다" — 진짜 원인과 근본적 해결

v0.2에서도 있었던 문제인데 이번엔 도로 등급까지 겹쳐서 더 심해질 수
있었다. 헤드리스 시뮬레이션으로 원인 재확인: 새 빈 땅이 "이번 틱 마침
대로변으로 새로 열리는 그 순간"이 아니면 절대 인프라를 못 가져간다는
게 문제였음(v0.2에서 "밀린 개수를 쌓아뒀다 나중에 소진"으로 1차 완화는
했지만, "나중에 대로변 빈 땅이 열려야 한다"는 전제 자체가 여전히
안 깨졌었다). 이번엔 안전장치를 하나 더 추가: 그 틱에 새로 열리는
대로변 빈 땅으로도 밀린 걸 다 못 채우면, **대로변에서 가장 덜 자란
(인구가 가장 적은) 건물 하나를 강제로 인프라로 전환**한다. 방금 막
생긴 빈집 하나를 뺏기는 셈이라 손실이 작고, 대로만 있으면 인프라가
반드시 몇 틱 안에 나타나는 걸 헤드리스 테스트로 확인(누적인구 120에서
수도·발전소·치안시설 3개 정상 배치, 순환 순서까지 정확히 일치).

### 남은 것

화물체인(원자재→상품 이동, §5.4 본편)은 여전히 없음 — 공업지가
"뭔가를 만들어 상업지로 보낸다"는 흐름은 아직 없고, 지금은 그냥
포텐셜 따라 자라기만 하는 활성도 숫자다. 다중 이웃도시, 카메라 조작,
저장/배속도 그대로 없음. 다음 우선순위는 화물체인 첫 단계(공업→상업
흐름 하나만이라도)이거나, 혹은 이미 있는 시스템들(혼잡/니즈/계층)의
되먹임을 시각적으로 더 잘 보이게 다듬는 쪽(예: 만족도 낮은 건물이
눈에 띄게 칙칙해진다든지) — 둘 중 어느 쪽이 "재미"에 더 크게
기여할지는 플레이해보고 판단하는 게 맞을 것 같다.

### 추가 수정 — 2클릭 도로 건설이 안 먹던 버그 + 고스트 프리뷰 (2026-09-22)

**원인**: `TransitCityManager.Update()`가 "마우스가 UI 위에 있는지"를 전혀
체크하지 않았다. "소로 건설" 버튼을 누르는 바로 그 클릭이 UI 이벤트로도
처리되는 동시에, 같은 프레임에 월드 클릭으로도 해석돼 버튼 아래 화면
좌표에 해당하는 격자 칸을 엉뚱하게 시작점으로 잡아버렸다 — 그러면 유저가
실제로 의도한 시작점 클릭이 사실은 "끝점"으로 처리되면서 한 클릭씩
밀렸고, 밀린 좌표쌍은 대개 도로망과 안 이어져 있어 `CanBuildRoadAt`이
계속 실패해 "눌러도 안 지어짐"으로 나타났다. `EventSystem.current.
IsPointerOverGameObject()`로 UI 위 클릭을 걸러내도록 고쳤다.

**고스트 프리뷰 추가**: 시작점을 찍은 뒤 마우스를 움직이면, 실제로
지어질 ㄱ자 경로를 반투명 박스로 매 프레임 미리 보여준다(소로=연두,
대로=옅은 보라, 이미 건물이 있어 막힌 칸=빨강). `Shader.Find("Unlit/
Transparent")` + 런타임 생성한 1x1 흰 텍스처로 새 머티리얼 에셋 없이
코드만으로 구현 — 고스트 개수가 항상 적어(한 번에 최대 맵 대각선 길이
정도) 매 클릭마다 인스턴스 머티리얼을 만들어도 부담 없다.

### 인프라 배치를 "인구 마일스톤 순환"에서 "실측 결핍 우선"으로 (2026-09-22)

요청: "부지를 철거하면 그 이후 들어올 건물이 우선적으로 인프라에 부족한
점이 있다면 부족한 시설 우선으로 들어왔으면 좋겠어."

기존엔 누적 인구가 40명 넘을 때마다 수도→전기→치안→소방 순으로 무조건
순환 배치했다(어떤 게 실제로 부족한지와 무관). 이번엔 완전히 갈아엎었다
— `CityGridModel.FindWorstInfraDeficit()`가 매 틱, 모든 주거지의 4개
니즈 각각에 대해 "요구치 − 커버리지" 평균을 재서 가장 심한 걸 고른다.
평균 결핍이 기준치(`infraPriorityThreshold`, 0.15) 이상이면 그 시설이
"지금 가장 급한 인프라"가 되고, **새로 개발 가능해지는 대로변 땅
(자연 성장이든, 철거로 다시 비워진 자리든 구분 없이 전부)** 이
우선 그 시설로 채워진다. 인구 마일스톤/밀린 개수(backlog) 개념 자체를
없앴다 — 이제 트리거가 "인구가 몇 명이냐"가 아니라 "지금 실제로 뭐가
부족하냐"라서 훨씬 직관적이고, 철거 후 재개발에도 자연스럽게 똑같이
적용된다(철거된 자리든 새로 뻗은 도로변이든 "새로 열린 빈 땅"이라는
점에서 모델 입장에선 차이가 없다).

헤드리스 테스트로 확인: 수도3·전기2·치안5·소방4 상태에서 주거지 하나를
철거하니, 몇 틱 뒤 그 자리가 정확히 가장 적었던 "발전소(전기)"로
채워짐.

## 프로토타입 v0.4 — 순차적 성장, 인프라 티어, 맵 확장 (2026-09-22)

큰 요청 묶음 6개 처리. 노동시장(철거→실직→인력난으로 기능정지)과
다중 타일 건물(인프라 증축용 공간 확보) 두 가지는 **의도적으로 이번엔
안 함** — 둘 다 이 프로토타입에 아직 없는 새 하위 시스템(고용 매칭,
멀티타일 배치)을 통째로 새로 설계해야 해서, 다른 6개와 한꺼번에
욱여넣기보다 다음에 제대로 된 설계로 따로 붙이는 게 맞다고 판단.

### 청결 니즈 + 상수도/하수도 분리

수도를 상수도(UtilityWater)와 하수도(UtilitySewage, 신설)로 나눴다.
하수도가 새로운 "청결" 니즈를 담당 — 기존 4개 니즈 패턴 그대로 재사용해
5번째로 얹었다(주거지 정보 패널에도 5줄 전부 표시).

### 겹친 건물 버그 — 원인: 강제전환 경로가 철거 이벤트를 안 쏨

재현 없이 코드 추적만으로 원인 특정. `SpawnAndGrowZones`의 "밀린
인프라를 위해 기존 건물 하나를 강제로 인프라로 바꾸는" 경로가
`tile.Kind`만 직접 바꾸고 `OnZoneRemoved`를 안 쏘고 있었다 —
`TransitCityManager.HandleUtilitySpawned`는 `houseVisuals[coord]`에
새 비주얼을 그냥 덮어쓰기만 해서, 옛 건물 비주얼이 파괴되지 않은 채
씬에 고아로 남았다(모델은 논리적으로 한 칸에 하나만 갖고 있으니
"버그가 없어" 보이지만, 화면엔 두 개가 겹쳐 있었던 것 — 그래서
철거해도 모델의 그 한 개만 지워지고, 도로를 지어도 고아 비주얼은
여전히 남아있었다). 두 겹으로 고쳤다: ① 모델 쪽 강제전환 경로가
`Kind`를 바꾸기 전에 `OnZoneRemoved`부터 쏘도록, ② 매니저 쪽에
`DestroyExistingVisualAt` 방어 로직을 넣어 어떤 경로로 오든 그 칸에
비주얼이 이미 있으면 먼저 지우도록.

### 순차적 성장 — "우후죽순 자라는 게 재미없다"

세 가지로 나눠 손댔다:
- **재산 계층이 즉시 정해지지 않고 서서히 수렴** — 예전엔 매 틱마다
  포텐셜 기준 상대 순위를 그대로 재산으로 꽂았다(방금 생긴 빈 땅도
  포텐셜만 좋으면 바로 "부촌"이 됨). 이제 그 순위는 "목표치"일 뿐이고,
  실제 `Wealth` 값은 `Mathf.MoveTowards`로 틱마다 조금씩만 다가간다
  (`wealthDriftPerTick`, 기본 0.04). 막 생긴 주거지는 항상 0(빈곤)에서
  시작해 자리 잡을수록 계층이 오른다 — "인접도시에서 못 구한 사람들이
  먼저 이주해 낮은 밀도로 살다가"라는 요청 그대로.
- **상업·공업은 주거지 니즈가 어느 정도 찰 때까지 잠금** — 도시 인구가
  `commercialUnlockPopulation`(25)을 넘고 주거지 평균 만족도가
  `commercialUnlockSatisfaction`(0.35)을 넘어야 대로변에 상업·공업이
  섞이기 시작한다. 그 전엔 대로변도 전부 주거지로만 채워진다 — "주거는
  부족한 게 많은데 상가부터 생긴다"는 순서 역전을 막음.
- **인프라 우선배치는 이미 v0.3에서 실측 결핍 기반으로 바꿔둔 상태라
  그대로 유지.**

### 인프라 티어(소형→중형→대형)

각 인프라 시설이 자기 반경 안의 실제 커버 인구를 매 틱 재고,
`utilityTier2Population`(40)/`utilityTier3Population`(100)을 넘으면
2티어/3티어로 승급한다. 티어가 오르면 커버리지 반경도
`utilityRadiusPerTierBonus`(2)만큼 늘고, 비주얼도 그 자리에서 그대로
커진다(파괴 후 재생성 아님 — `HouseVisual.BindUtility`를 새 티어
값으로 다시 호출). "동네 변전소 → 중급 → 발전소로 진화" 요청의 공간
확장(증축/철거로 자리 만들기) 부분은 이번엔 뺐다 — 지금 모델이 건물
1칸=1타일 고정이라, 여러 칸을 차지하는 건물을 넣으려면 철거·도로
인접성·인스펙터 코드 전반에 영향이 커서 별도 설계가 필요함.

### 맵 3배 확장 + 카메라 재조정

`gridWidth` 14→24, `gridHeight` 10→18 (칸 수 기준 약 3.1배). 고정
카메라(줌·팬 없음)라 맵만 키우면 2/3가 화면 밖으로 밀려날 상황이라,
새 맵 중심(23,17)에 맞춰 카메라 위치와 Ground 평면도 같이 옮기고
orthographic size를 15→28로 키웠다. `capture_game_view`로 실제
프레이밍을 스크린샷으로 확인 — 맵 전체가 여백을 두고 화면 안에
들어오는 것까지 확인함(§18.2 검증 방식대로 Play 모드 스크린샷으로,
Screen Space Overlay가 아니라 Main Camera가 그대로 찍히는 3D 씬이라
가능했다).

### 헤드리스로 검증한 것

`eval_file`로 CityGridModel만 떼어 확인: 막 생긴 주거지 재산이 2틱
뒤 0.08(거의 빈곤)로 시작해 90틱 뒤 최대 1.0까지 오르는 것, 하수도
포함 5개 니즈가 전부 값을 갖는 것, 인프라가 3티어까지 승급하는 것,
철거 없이도 강제전환 경로에서 `OnZoneRemoved`가 정상적으로 발생하는
것(겹침 버그 수정 확인)까지 전부 확인됨.

## 프로토타입 v0.5 — 노동시장, 인프라 이주/상한, 커버리지 표시 (2026-09-22)

### 노동시장(집계 기반, 개별 고용 매칭 없음)

`CityGridModel.LaborAvailability`(0~1) — 도시 전체 일자리 수요(상업·공업
정원 + 인프라 시설 필요 인원, 티어 높을수록 더 필요) 대비 지금 사는 인구
비율. 매 틱 재계산되고, 두 곳에 곱해진다: ① 상업·공업 활성도의 성장
상한이자, 이미 그 이상이면 실제로 줄어드는(레이오프) 기준, ② 인프라
커버리지 자체(인력이 없으면 그 시설은 사실상 기능을 못 한다). 건물
클릭 정보 패널에 "인력 수급: XX% (정상/인력난/사실상 정지)"로 표시.

헤드리스로 확인한 실제 캐스케이드: 주거지 대부분(90%) 철거 →
바로 다음 틱 labor 1.00→0.26로 급락 → 상업지 인구 20→16→13→12→11로
연쇄 축소(레이오프) → 이후 몇 틱에 걸쳐 labor 0.35→0.45→...→0.73로
서서히 회복되며 상업지 인구도 11→12→14→15로 다시 늘어남 — "철거하면
일하던 사람도 같이 없어지고, 인력이 없으면 기능이 축소·정지된다"는
요청 그대로 동작.

### 인프라 승급 — 타일을 벗어나지 않고, 공간 있는 곳으로 이주, 없으면 상한

두 가지 문제를 같이 고쳤다:
1. **비주얼 오버플로 버그**: `HouseVisual.BindUtility`가 티어가 오를 때
   바닥 면적(footprint)까지 최대 1.7배로 키우고 있었다 — 3티어면 칸
   너비(2)보다 큰 값(1.7×1.7≈2.9)이 나와 옆 칸을 침범했다. 이제
   footprint는 최대 1.15배로 상한을 두고(칸 안에 항상 들어감), 대신
   높이를 크게 키워(최대 2.5배) "커졌다"는 느낌을 준다.
2. **공간 판단 후 이주/상한**: 승급하려면(예: 2티어는 빈 이웃 1칸,
   3티어는 3칸) 지금 자리 주변에 여유가 있어야 한다. 없으면 대로변에서
   여유 공간이 가장 넉넉한 다른 빈 땅을 찾아 그리로 통째로 이주한다
   (기존 자리는 빈 땅으로, 새 자리에 승급된 티어로). 그마저도 없으면
   —"타일 한계까지 꽉 차면 더 이상 성장하지 못하게"라는 요청대로—
   승급을 보류하고 지금 티어에 머문다.

헤드리스로 상한(capping) 쪽은 확실히 검증함: 주변 4칸이 전부 막힌
채로 커버 인구 109(3티어 문턱 100 초과)를 만들어도 30틱 내내 1티어에
그대로 머무는 것 확인. 이주(relocation) 성공 경로는 코드 리뷰로는
확실하지만(이미 검증된 이벤트 패턴 재사용), 자연 발생 로직이 같은
틱 안에서 대로변 빈 땅을 거의 다 먼저 채워버려서 "여유 공간이 남아있는
상황"을 합성 테스트로 안정적으로 재현하기 어려웠다 — 실제 플레이(느린
성장, 철거로 생기는 빈 땅)에서는 더 자연스럽게 관찰될 것으로 예상.

### 커버리지 반경 시각화

인프라 시설을 클릭하면(Inspect 모드) 실제 커버 반경(티어에 따라 늘어남)
경계를 맨해튼 거리 다이아몬드 모양의 반투명 흰 마커로 지면에 표시한다.
도로 미리보기 고스트와 같은 머티리얼/기법 재사용(새 에셋 없이 코드로만).
건물을 바꿔 선택하거나 철거되면 자동으로 정리되고, 선택 중인 시설이
티어 승급하면 반경 표시도 같이 갱신된다.

### 덤으로 잡은 버그 — 인프라 강제전환 폭주

이번 작업 도중 헤드리스 테스트로 발견: 18칸짜리 긴 대로 하나에서 인프라
5종(상수도/하수도/전기/치안/소방)이 각자 자기 커버리지 부족을 채우려고
매 틱 경쟁적으로 기존 주거지를 잡아먹어, 50틱 만에 주거지 37개 중
27개가 인프라로 전환되는 폭주가 있었다(요청받은 기능은 아니었지만
품질에 심각한 문제라 같이 고침). 원인은 "강제전환 최후 수단"에 빈도
제한이 전혀 없었던 것 — 도시 규모 대비 인프라 총량에 상한
(`utilityCapPerPopulation`, 인구 40명당 1개 꼴)을 둬서 해결. 수정 후
같은 시나리오에서 주거지 37→25에서 안정되고(더 안 줄어듦), 인구도
용량 상한(500)에서 안정적으로 정착하는 것 확인.

## 프로토타입 v0.6 — 근무 인원 표시, 커버리지 가시성, 밀도 비례 크기, 합병, 플랫 디자인 (2026-09-22)

### 비주거 건물 근무 인원 표시

상업·공업은 "활성도"를 "근무 인원 X/Y명"으로 고쳐 표시. 인프라 시설도
이제 `Population`/`Capacity`에 실제 근무 인원/정원을 채운다 —
`UtilityJobsForTier(tier)`(3 + 3×(티어-1))가 정원, 거기에
`LaborAvailability`를 곱한 값이 실제 근무 인원. 인프라 정보 패널에도
"근무 인원 X/Y명"이 뜬다.

### 커버리지 시각화가 건물에 파묻히던 문제

바닥에 납작하게 깔던 마커를 **세로로 긴 얇은 기둥**(높이 3.5, 어떤
건물보다 확실히 높음)으로 바꿨다. 색도 흰색 반투명(알파 0.6)에서 밝은
노란색(알파 0.85)으로 바꿔 눈에 더 띄게 했다.

### 건물 크기 — 밀도 비례

`HouseVisual.Refresh()`가 이제 높이뿐 아니라 **바닥 면적도** 밀도(인구/
정원 비율)에 비례해서 키운다(0.55배~1배). 저밀도 주거지는 작고 낮은
박스, 꽉 찬 고밀도는 크고 높은 박스로 보인다.

### 인프라 합병(merge) — 소형 시설 여러 개가 안 합쳐지던 문제

원인: 승급(이주 포함) 로직 자체는 있었지만, **같은 종류의 인접한
다른 시설을 흡수하는 로직이 아예 없었다** — 그래서 결핍이 심할 때
같은 종류가 여러 칸에 나눠 생긴 뒤, 나중에 하나가 커져도 나머지는
그대로 남아있었다. `AbsorbNearbyRedundantUtilities`를 추가해, 승급
(제자리든 이주 후든) 직후 자기 새 반경 안에 들어온 같은 종류의 다른
시설을 철거해 흡수 합병하도록 했다. 승급 자체가 막혀 있던 것도 같이
손봤다 — 방 여유 판단을 4방향(상하좌우)에서 **8방향(대각선 포함)**
으로 넓혀서 "옆 건물을 부숴 공간을 만들어줬는데도 안 됨" 케이스를
줄였다. 스냅샷 순회 중 먼저 합병된 유령 타일을 다시 처리하는 버그도
방지 처리함.

헤드리스로 확인: 같은 종류(UtilityPower) 두 시설을 거리 2칸에 심고
한쪽만 승급 조건을 채웠더니, 승급하며 반경이 넓어져(반경 6) 다른 쪽을
흡수 — 최종적으로 한 시설만 남고 티어 2로 확인됨.

### 플랫 디자인

입체감이 과하다는 지적에 도로·건물 기본 높이를 대폭 낮췄다 —
도로 완공 높이 1.2→0.5, 주거지 높이 범위 0.6~3.5→0.3~1.8, 인프라
기본 높이 2.2→1.1(티어 배율은 유지). 차량 탑승 높이는 도로 높이에서
자동 계산되는 구조라 따로 손대지 않아도 같이 낮아졌다.

## 프로토타입 v0.7 — 성장/쇠퇴 사이클 (2026-09-22)

"성장이 상한(고정 Capacity)에 닿으면 그냥 멈춰서 평형에 고정된다 —
사이클이 안 보인다"는 지적에 대한 답. 앞서 제안했던 4개 항목을 한 번에 구현.

### 1. 주거지 밀도 재개발(§5.5.3 부활) — 핵심

인프라 티어와 같은 패턴을 주거지에 적용. `Capacity`가 더 이상 고정이
아니라 저밀도(20)→중밀도(35)→고밀도(55) 3단계로 재개발된다. 만족도가
좋은/나쁜 상태로 **연속 8틱**(디바운스, 순간 혼잡 한 번으로 안 깜빡이게)
쌓이면 승급/강등. 강등되면 정원 초과분 인구는 그 자리에서 정리됨.

헤드리스로 완전한 사이클 확인: 인프라 다 채운 채로 18틱 방치 →
만족도 0.87~0.95 유지 → goodStreak 8 도달 시점(t6~9 사이)에 1→2티어
승급(정원 20→35, 인구 즉시 35까지 성장) → 다시 스트릭 쌓여 2→3티어
승급(정원 35→55, 인구 55까지) — "저밀도가 인프라 넉넉해지면 고밀도로"
요청 정확히 재현됨. 강등(쇠퇴) 쪽은 코드는 동일 패턴으로 넣었지만
헤드리스 테스트에서 만족도를 0.3 밑으로 떨어뜨리는 데 실패해(혼잡+
인프라 결핍만으로는 이 테스트 조건에서 충분히 나빠지지 않음) 실제
발동은 못 봤다 — 실제 플레이(대규모 인프라 철거, 혼잡한 소로 등)에서
확인 필요.

### 2. 상업지 규모 — 주변 인구 비례

인프라와 같은 패턴: 반경(`commercialServiceRadius`=6) 안 인구가
`commercialTier2Population`(60)/`Tier3Population`(150)을 넘으면 승급,
밑돌면 강등(양방향). 헤드리스로 확인: 인구를 33→55로 강제로 늘리자
곧바로 2→3티어로 정확히 반응(정원 35→55).

### 3. 공업지 이주 — 땅값 기반

주변 재산 수준(Wealth) 평균이 `industrialLandValueRelocateThreshold`
(0.6)를 넘으면 "땅값이 올랐다"고 보고, 그보다 싼 대로변 빈 땅을 찾아
이주(§5.6 부유층 이탈의 산업판). `AverageNearbyWealth` 계산 자체는
직접 수기 계산과 대조해 정확함을 확인했지만(0.36 정확히 일치), 실제
이주 이벤트가 발동하는 걸 헤드리스로 깔끔하게 재현하진 못했다 — 작은
테스트 맵에서 인프라 강제전환/자연 조닝 등 다른 시스템이 같은 칸을
먼저 채가는 간섭이 반복됨(유틸리티 이주 검증 때 겪었던 것과 같은
종류의 문제). 로직 자체는 이미 검증된 유틸리티 이주 패턴을 그대로
재사용한 것이라 구조적으로는 확신하지만, 실제 플레이에서 직접
확인이 필요하다.

### 4. 디바운스

`GoodStreak`/`BadStreak` 카운터로 재개발 판정을 연속 N틱(기본 8)
기준으로 못박아, 순간적인 혼잡 한 번에 승급/강등이 깜빡거리지 않게
했다.

### 설정값 (BalanceConfig 신규)

`redevelopGoodSatisfaction`(0.6) / `redevelopBadSatisfaction`(0.3) /
`redevelopStreakTicks`(8) / `densityTier2Capacity`(35) /
`densityTier3Capacity`(55) / `commercialServiceRadius`(6) /
`commercialTier2Population`(60) / `commercialTier3Population`(150) /
`industrialLandValueRelocateThreshold`(0.6) / `industrialLandValueRadius`(4)

## 프로토타입 v0.8 — 도로 단절 시 폐허화 (2026-09-22)

지적: 도로가 사라진 건물이 그냥 그 자리에 얼어붙은 채로 남아있었다(성장만
멈출 뿐 아무 일도 안 일어남). 원인은 두 가지가 겹쳐 있었다 — ① Potential이
0이 돼도 그걸 실제로 "처벌"하는 로직이 없었고, ② 오히려 도로가 없으니
혼잡도 0이라 만족도가 역설적으로 좋게 나왔다(고립이 혼잡 없음으로
잘못 해석됨).

`CityGridModel.UpdateAbandonment()` 추가 — 도로망과 완전히 끊긴
(Potential 0) 건물은: 만족도를 0으로 강제(고립이 혼잡보다 나쁜 상태) →
`abandonGracePeriodTicks`(3틱) 유예 → 그 뒤로는 틱마다 정원의
`abandonDecayRatePerTick`(25%)만큼 인구를 잃음 → 0이 되면 완전히
사라져 빈 땅으로. 인프라 시설은 "정원이 서서히 준다"는 개념이 안 맞아
(사람이 아니라 시설이므로) 유예 기간 지나면 바로 폐쇄.

헤드리스로 전체 사이클 확인: 도로 하나에 의존하던 주거지 3칸(각 20명
만석)에서 그 도로를 철거 → 만족도 즉시 0.00 → 3틱 유예 → 인구
20→15→10→5로 감소 → 6틱째에 3칸 전부 완전히 사라짐(빈 땅으로 복귀).
요청한 "쇠퇴하다가 없어지는" 흐름 정확히 재현됨.

## 프로토타입 v0.9 — 순찰차, 입주/이탈 사이클, 공실 방치 철거 (2026-09-22)

### 경찰·소방 순찰차 — 장식이 아니라 실제로 수치를 올림

`TransitCityManager.TrySpawnPatrolCar()` — 경찰서·소방서에서 도로를 타고
순찰 반경만큼 나갔다 되돌아오는 왕복 경로로 차량을 스폰한다(기존 주민
출퇴근 차량과 같은 풀/시스템 재사용, `CarVisual.SetColor`를 새로 추가해
경찰=남색/소방=빨강으로 구분). 그리고 이 순찰은 실제로
`CityGridModel.ComputeCoverage`에서 치안·소방 커버리지 반경에
`patrolRadiusBonus`(2칸)를 더해준다 — "순찰 돌면서 수치를 올려줬으면"
요청대로 시각 연출과 수치가 실제로 연결됨. 헤드리스로 확인: 기본
반경(4) 밖·순찰 반경(6) 안인 거리 5 지점에서 커버리지 0.17로 정상 계산됨
(반경 밖이었다면 0이어야 함).

### 만족도 — 입주 50%에서 시작, 서서히 변화, 10% 밑이면 이탈

기존엔 만족도가 매 틱 혼잡·인프라로 다시 계산되는 "순간값"이었다.
이제 실제 거주자가 있는 칸은 만족도가 **목표치(그 계산값)를 향해
서서히 수렴**(Wealth 드리프트와 같은 패턴, `satisfactionDriftPerTick`
=0.08)하고, **처음 입주하는 순간엔 무조건 50%에서 시작**한다.
`satisfactionLeaveThreshold`(0.1) 밑으로 떨어지면 성장 대신 이탈 —
정원의 `satisfactionLeaveRatePerTick`(30%)만큼 거주자가 빠져나간다.

### 공실 방치 철거 + 알아낸 진짜 버그

"모든 니즈가 0%인 건물이 왜 안 없어지지" 질문의 답 — 두 가지가 겹쳐
있었다:
1. **애초에 인프라 결핍만으로는 만족도가 10% 밑까지 안 떨어짐**(혼잡
   없이 인프라 결핍 혼자로는 최대 감점이 제한적) — 이건 정상 동작(§5.8
   설계 의도대로, 결핍은 성장을 늦출 뿐 즉시 이탈을 부르진 않음).
   혼잡이 같이 있어야(도로가 붐비거나 완전히 끊기거나) 진짜로 10% 밑까지
   떨어진다.
2. **진짜 버그**: 거주자가 다 떠나 인구 0이 된 바로 다음 틱에, "새로
   입주하는 상황"으로 취급돼(만족도 50%로 리셋) 조건이 그대로 나쁜데도
   즉시 다시 채워지는 문제가 있었다 — 성장 로직에 "포텐셜만 있으면
   최소 1명은 보장"이라는 무조건적 하한이 걸려 있었기 때문. 그래서
   공실이 단 1틱도 못 버티고 매번 즉시 리필돼, `VacancyStreak`가
   쌓일 기회 자체가 없었다. **목표 만족도가 이탈 문턱 밑이면 애초에
   아무도 새로 이사 오지 않는다**는 조건을 추가해 고쳤다.

수정 후 헤드리스로 전체 사이클 확인: 좁은 소로(용량15)에 만원 부촌
(재산 1.0) 옆에 빈 칸을 두니 — t0 입주(만족도 정확히 0.50) → 혼잡으로
만족도 서서히 하락(0.42→0.34→0.26) → t6에 만족도 0.02로 전원 이탈
(인구 0) → 공실 스트릭이 즉시 리필되지 않고 정상적으로 쌓임(1→4) →
조건이 나아지자 t12에 다시 새 입주(또 만족도 0.50에서 시작) — "입주
50%→니즈 안 채워지면 하락→10% 밑이면 이탈→빈 건물은 기다리다 방치되면
철거"라는 요청 전체가 정확히 재현됨. 상업·공업의 공실 철거는 이미
노동력 상한(`laborCap`)이 0이 될 수 있어 같은 버그가 없었음(그대로 둠).

## 프로토타입 v1.0 — 건물 발자국(1x1/1x2/2x2), 교육 니즈, 상업·공업지구 폐지 + 의료·소비·일자리 니즈 (2026-09-22)

### 건물이 진화하면서 실제로 더 넓은 칸을 차지 (모델 레이어)

"건물이 진화하려면 크기도 1x1→1x2→2x2로 커지고, 커진 만큼 영향력
반경도 넓어져야" 요청. `CityTile`에 `FootprintExtensions`(원점 말고
추가로 차지한 칸들)와 `OwnerCoord`(확장 칸이 자신을 소유한 원점을
가리키는 역참조)를 추가하고, `TileKind.Extension`을 새 종류로 도입—
"다른 건물이 차지한 칸이라 독립적인 개발 대상이 아님"을 표시한다.

발자국 모양은 티어로 결정: 1티어=1x1(추가 칸 없음), 2티어=1x2(상하좌우
중 빈 칸 하나 추가), 3티어=2x2(네 방향 중 하나로 정사각형을 이루는
조합을 시도, `Square2x2Offsets`). `FindFootprintFor`가 원점 기준으로
가능한 모양을 찾고, 없으면 `FindRelocationSiteForFootprint`로 발자국을
통째로 놓을 수 있는 다른 대로변 빈 땅을 찾아 이주한다(기존에 살던
사람은 순간이동 못 하므로 **주거지는 이주하지 않고** 자리가 생길 때까지
그 자리에서 재시도만 함 — 반면 인프라 시설은 이주 가능). 그마저도
없으면 승급을 보류하고 다음 틱에 재시도(스트릭 유지) — 맵이 꽉 차도
무한정 커지지 않음.

철거·폐허화·공실철거·강제전환(인프라 결핍 이주)·합병(Absorb) 등 건물이
없어지거나 옮겨가는 모든 경로에서 `ReleaseFootprint`로 확장 칸을 먼저
반납하도록 일괄 반영 — 한 곳이라도 빠뜨리면 확장 칸이 영영 Extension
상태로 남아 그 땅을 잠가버리는 버그가 나므로 전수 점검함.

**비주얼 레이어(HouseVisual)는 아직 미착수** — 지금은 기존의 "가짜 티어
범프"(칸 안에서 살짝 커 보이게 하는 스케일 트릭)를 그대로 쓰고 있다.
실제 발자국 크기·위치(앵커+중심 오프셋)를 반영한 진짜 렌더링은 다음
과제로 남겨둠.

### 교육 니즈 추가 — 학교

`TileKind.UtilityEducation` 추가. 기존 상수도/하수도/전기/치안/소방과
완전히 동일한 패턴(거리 기반 커버리지, 결핍-우선 대로변 자동배치,
인구 기반 티어 성장)으로 편입 — 새 코드 경로 없이 배열에 하나 추가하는
것만으로 전체 시스템에 자연스럽게 녹아들도록 설계된 부분이 제값을 함.

### 상업지구·공업지구 폐지 — "니즈"로 재해석

"상업지구/공업지구 개념을 없애자"는 요청에 따라 `TileKind.Commercial`/
`Industrial`과 그 전용 메서드(`UpdateCommercialTiers`,
`UpdateIndustrialMigration`, `RollAvenueZoneKind`, 상업/공업 잠금
해제 문턱 등)를 전부 삭제. 대신:

- **의료**(`UtilityMedical`, 병원) — 교육과 동일한 패턴으로 편입.
- **소비**(`UtilityStore`, 상점~백화점) — 역시 결핍-우선 시스템에
  동일하게 편입되지만, 정원만 훨씬 크게(`UtilityJobsForTier`에서
  상점·공장은 15+25×(티어-1), 나머지 인프라는 3+3×(티어-1)) — 실제
  상업시설다운 고용 규모를 흉내냄.
- **일자리**(`UtilityFactory`, 공장) — 유일하게 결핍-우선 시스템 밖에서
  **전용 배치 규칙**으로 동작. 도로변 빈 땅이 채워지지 않는 한 새
  대로변 땅은 거의 언제나 그 틱 안에 전부 주거지로 소진되므로("Pass1"
  루프가 인프라 결핍 하나만 우선권을 주고 나머지 빈 땅은 즉시
  주거지화), 공장이 그 경쟁에서 항상 밀렸던 실제 버그를 겪음 — 해결책은
  (1) Pass1 루프가 시작되기 **전에** 미리 "제일 싼 빈 땅"을 찜해 Pass1이
  그 칸에 도달하면 최우선으로 공장을 배정하고, (2) 그마저 못 찾으면(거의
  항상 이 경우 — 도로를 새로 낸 바로 그 순간이 아닌 한 이미 열린 땅이
  없음) 인프라와 같은 "강제 전환" 최후 수단을 공장에도 추가해, 대로변
  주거지 중 **재산 수준(Wealth)이 가장 낮은 곳**을 공장으로 바꾼다.
  일자리 부족 판정은 `Population > (공장 제외 모든 인프라 정원 합)`,
  공장이 이미 있으면 재생성 안 함(그 뒤 성장은 기존 티어 시스템이 처리).

새 니즈 `JobCoverage`(도시 전체 고용률 = 공장 포함 전체 일자리 정원 /
인구, 거리 무관 — 모든 주거지가 같은 값)를 추가. 기존
`LaborAvailability`(인구/일자리, "고용주가 사람을 구할 수 있는가")와는
정반대 방향의 별개 지표라 재사용 불가, 별도로 계산.

`FindWorstInfraDeficit`의 자동배치 우선순위 후보는 이제 8개
(상수도/하수도/전기/치안/소방/교육/의료/소비) — **일자리(공장)는
의도적으로 제외**, 별도 규칙이므로.

### 헤드리스 검증

- 도로 하나에 인구 꽉 찬 주거지를 여럿 붙이고 조건을 관찰: 8종 결핍-우선
  인프라(물/하수/전기/치안/소방/교육/의료/소비)가 전부 자동배치되고
  총량 상한 안에서 안정됨(주거지 30개 안팎 유지, 붕괴 없음).
- 인구를 수백까지 강제로 채운 상태에서 공장 미배치 상태 재현 →
  Pass1-사전예약 로직 추가 전엔 영구히 안 생기는 버그 확인 → 수정 후
  같은 시나리오에서 다음 틱에 "재산 수준이 가장 낮은" 기존 부지를 공장
  으로 전환하는 것까지 확인(부촌 Wealth 0.9/빈민가 0.1로 대비시켰더니,
  실제로는 그보다 더 낮은 — 방금 자연 발생해 Wealth가 아직 0에 가까운
  — 다른 칸이 선택됨. 로직이 "설정한 라벨"이 아니라 "진짜 최저값"을
  정확히 찾는다는 뜻이라 오히려 올바른 동작).
- 순수 유기적 성장(수동 개입 없이 도로만 깔고 방치) 60틱 시뮬레이션:
  t9에 공장이 자연 발생, t29에 상점도 자연 발생, 인구 280~370대에서
  안정적으로 진동, 자금은 꾸준히 우상향 — 9종 건물이 전부 자연스럽게
  갖춰지는 것을 확인.
- `JobCoverage`: 인프라가 0개면(첫 틱, 인구도 0이라 보너스 처리) 1.00,
  인프라가 자동배치되고 인구가 급증한 뒤엔 0.19까지 떨어짐 — "일자리가
  부족하다"는 신호가 정확히 반영됨(공장 스폰 트리거와 일관됨).

### 남은 작업

1. **발자국 비주얼 렌더링** — 실제 1x2/2x2 크기·위치를 반영한 진짜
   멀티타일 렌더링(지금은 가짜 범프). 앵커+중심 오프셋 설계는 끝났지만
   미착수.
2. UI 정보 패널에 교육/의료/소비/일자리 니즈 행 추가 완료
   (`TransitCityHud.uxml`/`TransitCityUIController.cs`) — 검증은
   Play 모드 수기 확인 필요(UI Toolkit 헤드리스 검증 불가).
3. `BalanceConfig`에서 상업·공업 전용 필드(`commercialServiceRadius`
   등 7개) 삭제 — `.asset` 파일은 안 건드림(Unity가 알아서 안 쓰는
   YAML 키를 무시하므로 재직렬화 전까지 그대로 남아있어도 무해함).

## 프로토타입 v1.1 — 니즈 커버리지: 직선거리 → 도로망 홉 거리 (2026-09-22)

### 문제

기획서(§5.8.2)엔 "커버리지는 실제 이동시간(교통 시뮬레이션)으로 계산한다"고
못박혀 있었는데, 이번 세션 초반에 9-니즈 시스템을 만들면서 `ComputeCoverage`가
**맨해튼 직선거리**로 단순화돼 있었다 — 병원과 주거지 사이에 도로가 한 칸도
없어도 반경 안이면 그냥 커버됐다. 사용자가 "도로가 하는 일이 별로 없는 것
같다"고 지적한 근본 원인이 바로 이것이었다 — 인프라 8종(상수도~소비) 전부
도로망을 사실상 무시하고 있었으니, 도로는 "땅이 개발 가능한지"와 "혼잡도
색깔" 딱 두 가지만 담당하고 있었던 셈.

### 수정

`FloodRoadDistance(entryRoad, maxHops)` — 인프라 시설의 진입 도로에서 시작해
도로망만 타고 가는 유계 BFS(반경을 넘으면 더 안 감). 시설 하나당 틱에 한 번만
계산해서 캐싱(`utilityFloods`) — 주거지 수와 무관하게 인프라 개수만큼만 도니
비용은 여전히 가볍다. `ComputeCoverage`는 이제 주거지의 진입 도로가 그 플러드
결과 안에 있는지 조회만 한다 — **직선거리로 가깝지만 도로가 안 이어지면 이제
정확히 0**이 나온다.

`UpdateUtilityTiers`의 티어 성장 판정(`servedPopulation`)도 같은 플러드 결과를
재사용하도록 맞췄다 — 안 그러면 "커버리지 표시"와 "승급 조건"이 서로 다른
거리 기준을 쓰게 돼 화면에 보이는 숫자와 실제 승급 타이밍이 어긋나는
불일치가 생긴다.

### 헤드리스 검증

병원 바로 옆칸(맨해튼거리 1)인데 그 칸이 도로에 전혀 안 붙어있는(병원을
거치지 않으면 도로 접근이 없는) 위치에 주거지를 두고 확인 — 이전 방식이면
반경(4) 안이라 거의 만점으로 커버됐을 상황인데, 수정 후 **정확히 0.00**.
반대로 도로로 실제 연결된(도로 홉 거리 기준 반경 안) 주거지는 0.75로 정상
커버됨. 기존 유기적 성장 60틱 시뮬레이션(단일 직선 도로라 홉거리≈직선거리에
가까운 시나리오)도 재확인 — 인구/건물 수/자금 전부 이전과 같은 범위에서
안정적, 회귀 없음.

### 다음 (설계 확정 대기)

사용자가 이어서 요청한 것: (1) 사람 이동을 작은 원, 차량 이동을 네모로
표현(미니메트로/미니모터웨이 스타일 비주얼), (2) 도로가 실제로 막히는(교통
정체) 시뮬레이션, (3) 교차로 처리 방식 선택(로터리/신호등/미설치 시 전원
정지 후 순서대로 통과) — 규모가 큰 별도 시스템이라 세부 설계를 사용자와
먼저 맞추고 착수할 예정.

## 프로토타입 v1.2 — 보행자(원)/차량(네모) 이원화, 교차로 처리(로터리·신호등·미설치), 진짜 정체 (2026-09-22)

### 배경

사용자가 미니메트로/미니모터웨이 스타일을 원하면서 두 가지를 요청: (1) 사람
이동은 작은 원, 차량 이동은 네모로 — 나중에 버스·지하철·공항철도가 추가될
걸 감안해 "도보"를 차량과 별개 단위로 분리해둔다는 의도. (2) 교차로에
로터리/신호등을 선택해 지을 수 있고, 아무것도 안 지으면 "차들이 전부 멈췄다
순서대로" 통과하는 실제 정체가 있어야 한다.

### 보행자(PersonVisual) — 라스트마일만 표현

`PersonVisual.cs`(CarVisual과 같은 패턴, 훨씬 단순 — 구간 속도/정차 없이
그냥 걷기만) 신설. 집에서 나와 **가장 가까운 도로 진입점까지만** 걸어가고
사라진다 — 그 뒤(도로 위)는 차량이 이어받는다는 설정. 지금은 다른 교통수단이
없어 이 라스트마일이 전부지만, 나중에 버스 정류장/지하철역이 생기면 그
지점까지 걸어가는 구간으로 자연 확장될 지점.

`Person.prefab`(Sphere, CarMat 재사용, `Assets/Resources/Prefabs/TransitCity/`)을
에디터 스크립팅으로 생성 완료. **씬의 TransitCityManager에 이 프리팹을
연결하는 마지막 배선은 아직 안 함** — 에디터가 Play 모드라 지금 연결하면
Stop 시 유실된다. Play 모드를 벗어난 뒤 연결 예정(§아래 "남은 작업" 참고).

### 교차로 처리 — 로터리/신호등/미설치

`IntersectionControl.cs`(열거형: Uncontrolled/Roundabout/TrafficLight) 신설,
`RoadCell.Control` 필드로 도로 칸마다 기억. `CityGridModel.IsIntersection`이
그 칸에 이어진 도로가 3방향 이상이면 교차로로 판정(`intersectionMinConnections`,
기본 3). `EffectiveCapacity`가 도로 등급의 기본 용량에 처리 방식별 배율을
곱한다:

| 처리 방식 | 배율 | 근거 |
|---|---|---|
| 로터리 | 0.9 | 거의 안 막힘 |
| 신호등 | 0.65 | 어느 정도 처리하지만 대기 있음 |
| 미설치 | 0.3 | "사거리 전원이 멈췄다 순서대로" — 실효 용량이 가장 낮음 |

`SetIntersectionControlCommand`(Command 패턴)로 건설 — 직선 도로엔 못 짓고
(교차로가 아니므로 `CanSetIntersectionControl` 실패), 이미 같은 방식이
설치돼 있으면 중복 건설 거부(돈 안 나감), 다른 방식으로 바꾸는 건 다시
비용을 내고 성공한다(헤드리스로 전부 확인). UI에 "로터리"/"신호등" 버튼 추가
(`TransitCityHud.uxml`/`TransitCityUIController.cs`), 지어지면 도로 위에
표식(로터리=납작한 원반, 신호등=가는 기둥)을 세워 처리 방식이 눈에 바로
보이게 함(`TransitCityManager.RefreshIntersectionMarkerAt`, 기존 ghost/coverage
마커와 같은 `CreatePrimitive` 패턴 재사용).

### 혼잡 계산 — "집 앞 도로 한 칸"에서 "경로 전체 최악 구간"으로

기존엔 주거지 바로 앞 도로 한 칸의 혼잡도만 만족도에 반영했다. 이제
통근 경로 전체(도로망 부모 체인을 이웃 도시까지 쭉 따라가며)에서
`EffectiveCapacity` 기준 가장 막힌 구간(교차로 포함) 하나가 그 집의 혼잡이
된다 — 경로 어디든 병목이 있으면 그게 그대로 체감되므로, "우회로를 뚫었더니
실제로 나아졌다"는 인과관계가 성립한다. `RoadVisual`의 혼잡 색상(초록→빨강)도
같은 `EffectiveCapacity` 기준으로 바뀌어 도로 등급뿐 아니라 교차로 처리
방식까지 색으로 드러난다.

### 장식 차량 — 진짜로 느려지고, 진짜로 멈춘다

`CarVisual.Drive`가 구간별 속도 배율(`segmentSpeedScale`)과 칸별 정차 시간
(`waypointPauseSeconds`)을 받도록 확장. `TransitCityManager.BuildTrafficLists`가
경로 위 각 도로 칸의 혼잡비로 속도를 낮추고(최저 25%), 미설치 교차로가
실제로 과부하 상태면 0.5~1.5초 무작위로 완전히 멈췄다 가게 한다("전원 정지
후 순서대로" 연출) — 신호등은 40% 확률로 0.2~0.6초 짧게 멈춰 "가끔 빨간불에
걸리는" 리듬을 준다. 출퇴근 차량·순찰차 양쪽 다 적용.

### 헤드리스 검증

- 십자 교차로에서 `IsIntersection=True`, 기본용량 15에 대해 미설치=4,
  로터리=14, 신호등=10 — 의도한 순서(로터리>신호등>미설치)와 배율대로 정확히
  계산됨. 용량과 정확히 같은 통행량에서 과부하 판정 True, 그보다 하나 적으면
  (별도 확인) False로 정상 작동.
- `SetIntersectionControlCommand`: 직선 도로엔 설치 거부(무료), 첫 설치는
  200원(로터리 비용) 차감 후 성공, 같은 방식 중복 설치는 거부(무료),
  다른 방식으로 교체는 120원(신호등 비용) 차감 후 성공 — 전부 기대대로.

### 남은 작업 (Play 모드 해제 후)

1. `Person.prefab`을 `TransitCityManager.personVisualPrefab` 필드에 연결 —
   에디터 스크립팅으로 처리 예정, 지금은 Play 모드라 보류.
2. 실제 화면에서 보행자(원)·정체 연출·로터리/신호등 마커·버튼 동작을 수기로
   확인 — 밸런스 수치(배율·정차 확률·구간 속도 하한 등)는 전부 `BalanceConfig`
   필드라 플레이해보고 조정 가능.

## 프로토타입 v1.3 — 차량 회전/우측통행, 교차로 UX 재설계, 도로 단일화 (2026-09-22)

### 차량이 진행 방향으로 회전 + 우측통행

`CarVisual.Drive`가 이제 `laneOffset`을 받는다. 각 구간마다 `Quaternion.
LookRotation`으로 진행 방향을 바라보게 회전시키고, 그 방향 기준 "오른쪽"
(`LookRotation(forward) * Vector3.right`)으로 `laneOffset`만큼 비켜서 지나가게
했다 — 반대 방향 차량과 같은 칸을 지나가도 겹치지 않고 도로 오른쪽에 붙어
달리는 것처럼 보인다. 오프셋 거리는 `BalanceConfig.carLaneOffsetRatio`(칸
크기 대비 비율)로 튜닝 가능.

### 교차로 정차 위치 버그 — "한복판이 아니라 진입 전에" 서야 함

사용자 신고: 차가 교차로 한복판에서 멈췄다 출발함. 원인은 `waypointPauseSeconds`가
그 칸에 **도착한 뒤** 적용되고 있었던 것 — 이미 교차로 중앙까지 들어간
상태에서 멈추니 어색했다. `CarVisual.DriveRoutine`을 고쳐, 다음 칸(path[i])이
대기가 필요한 칸이면 **그 구간을 타기 전**(아직 이전 칸에 서 있는 상태)에
미리 기다리도록 순서를 바꿨다 — 이제 교차로 진입 전에 정차, 그 다음 통과.

### 도로 색상 혼잡 표시 제거

사용자 판단: 이제 차량이 실제로 느려지고 멈추는 게 보이니 도로 색까지
초록→빨강으로 바뀔 필요가 없다(중복 정보). `RoadVisual.Refresh()`에서
`EffectiveCapacity` 기반 색상 보간 로직을 제거하고 완공 시 `data.baseColor`
한 번만 칠하도록 단순화했다. `EffectiveCapacity` 자체는 만족도 계산과 차량
속도/정차 연출에 계속 쓰인다 — 도로 "색" 용도로만 뺐다.

### 소로/대로 통합 — "도로 건설" 하나로

사용자 판단: 소로/대로 구분이 의미 없어 보임(소로를 안 짓게 됨). `RoadTier`
enum·`RoadTypeData` 구조 자체는 그대로 뒀다(나중에 고속도로 등 새 계층이
들어올 근거지로 남겨둠) — 다만 **플레이어에게 제시하는 선택지를 대로
(MainRoad, 기존 avenueRoadType) 하나로 고정**했다. `TransitCityManager`에서
`localRoadType` 필드·`SetRoadType`·`currentRoadType` 스위칭 로직을 전부
제거하고 항상 `avenueRoadType`(공개 프로퍼티는 `RoadType`으로 정리)만 쓴다.
UI도 "소로 건설"/"대로 건설" 버튼 두 개를 "도로 건설" 버튼 하나로 합쳤다.
씬 재배선 불필요(기존에 이미 연결돼 있던 `avenueRoadType` 참조를 그대로
재사용).

### 교차로 처리 — 건설 모드 버튼 대신 "선택 → 패널에서 고르기"

기존엔 "로터리"/"신호등" 건설 모드 버튼을 미리 고른 뒤 교차로를 클릭하는
방식이었는데, 사용자 요청대로 **교차로를 먼저 선택하면 로터리/신호등 중
뭘 지을지 고르는 패널이 뜨는** 방식으로 바꿨다:

- `EditMode.SetRoundabout`/`SetTrafficLight` 제거 — 이제 Inspect 모드에서
  교차로(도로 3방향 이상)를 클릭하면 `TransitCityManager.OnIntersectionSelected`
  이벤트가 뜬다(건물 클릭과 배타적 — 하나를 고르면 다른 쪽 패널은 자동으로 닫힘).
- 새 `intersection-panel` UI(현재 상태 표시 + "로터리 건설"/"신호등 건설"
  버튼 + 닫기) — `TransitCityManager.TrySetIntersectionControl(coord, control,
  cost)`를 호출해 실제 건설을 수행(Command 패턴 그대로 재사용).
- 기존 하단 툴바에서 로터리/신호등 버튼은 제거.

### 배선 — Person.prefab 연결 완료

지난 세션에서 Play 모드라 못 했던 `TransitCityManager.personVisualPrefab`
필드 연결을 에디터 스크립팅(SerializedObject)으로 완료하고 씬 저장까지
확인했다. 이제 보행자(원)가 실제로 스폰된다.

### 남은 작업

실제 플레이로 확인 필요: 차량 회전·우측통행이 자연스러운지, 교차로 진입
전 정차가 부드러운지, 로터리/신호등 패널 UX가 직관적인지, 도로 단일화 후
빌드 흐름에 위화감이 없는지 — 밸런스 수치(오프셋 비율·정차 확률 등)는
`BalanceConfig`에서 계속 조정 가능.

## 프로토타입 v1.4 — 차량 칸 점유 예약(진짜 줄서기), 순찰차 유턴 완화 (2026-09-22)

### 문제 — "차가 겹쳐서 지나가면 정체가 안 느껴진다"

기존엔 각 장식용 차량이 서로를 전혀 모르는 채(개별 코루틴이 정해진 경로를
독립적으로 재생) 움직였다. 혼잡(`TripLoad`/`EffectiveCapacity`)은 시뮬레이션
틱에서 집계된 **추상적 수치**일 뿐이라 실제 화면에 차가 몇 대 있는지와
무관했고, 그래서 두 차가 같은 칸을 같은 순간에 지나가도 그냥 겹쳐 통과해
버렸다 — "정체를 해결한다"는 느낌이 안 나는 근본 원인.

### 해결 — 칸 단위 점유 예약

`TransitCityManager`에 `Dictionary<GridCoord, CarVisual> tileOccupants`를
두고 `TryReserveTile`/`ReleaseTile`을 공개했다. `CarVisual.DriveRoutine`이
이제 다음 칸으로 넘어가기 **전에** 그 칸을 예약해야 하고, 이미 다른 차가
있으면(최대 `carMaxQueueWaitSeconds`, 기본 4초까지) 제자리에서 진짜로
기다린다 — 이게 "뒤차가 줄을 서는" 정체의 실체다. 너무 오래 막히면(교착
가능성 — 두 차가 서로의 칸을 기다리는 상황 등) 그냥 밀고 들어가게 해
영구 정지를 막았다.

### 순찰차 "임의 지점 유턴" — 사실은 의도된 왕복인데 티가 안 났음

신고하신 "차량이 임의 지점에서 유턴한다"는 경찰·소방 순찰차의 왕복 로직
(순찰 반경만큼 나갔다가 그대로 되짚어 돌아옴)이었다 — 로직 자체는
의도한 동작이지만, 방향 전환 지점에서 아무 예고 없이 즉시 180도 꺾여
글리치처럼 보였다. 방향이 뒤집히는 그 칸에 0.4~0.9초 무작위 정차를
추가해 "둘러보고 돌아간다"는 느낌을 주도록 완화했다.

### 남은 작업

실제 플레이로 확인 필요: 칸 점유 예약이 만드는 줄서기가 자연스러운
길이·속도로 보이는지, `carMaxQueueWaitSeconds`(교착 완화 타임아웃)가
너무 짧아 정체가 금방 풀려버리거나 너무 길어 뻑뻑하지 않은지, 순찰차
정차가 충분히 자연스러운지 — 전부 `BalanceConfig`에서 조정 가능.

## 프로토타입 v1.5 — 하루 시간대(생활 패턴 리듬), 순찰 경로 무작위화 (2026-09-22)

### 순찰차가 항상 같은 길로만 도는 버그

원인: 순찰 "나가는 길"이 이웃 도시까지 가는 **고정 최단경로**(BFS 부모
체인)를 그대로 썼다 — 도로가 여러 갈래로 나 있어도 그 트리는 항상 유일한
경로 하나뿐이라 매번 똑같았다. 진입 방향도 `Neighbors4`가 도는 순서상 항상
같은 첫 번째 후보만 골랐다(무작위 아님). 고쳐서: 진입 방향을 무작위로
고르고, 순찰 경로 자체도 매 스텝 갈 수 있는 이웃 도로 중(방금 온 칸 제외)
하나를 무작위로 골라 걸어나가는 랜덤워크로 바꿨다 — 이제 도로망이 복잡할수록
매번 다른 길로 순찰을 돈다.

### 하루 시간대 — 게임 속 하루 = 실시간 1분, 러시아워·심야 리듬

`CityGridModel`에 성장 틱(Day, 빠르게 흘러가는 추상적 하루)과 완전히
별개인 **연속 시계**(`TimeOfDayHours`, 0~24 순환)를 추가했다 —
`config.dayLengthSeconds`(기본 60초)에 걸쳐 하루가 흐른다.

- `DayActivityFactor01(hour)` — 코사인 곡선(정오=1, 자정=0)으로 낮/밤 기본
  활동량을 매끄럽게 표현. 자정을 넘나드는 구간도 분기 없이 자연스럽게 이어짐.
- `CommuteRushFactor01()` — 아침(6:30~7:30~9시)·저녁(17:30~18:30~20시)
  각각 텐트 커브로 러시아워 강도(0~1)를 계산, 둘 중 큰 쪽을 취함.
- `TrafficSpawnMultiplier()` — 낮/밤 기본 배율 위에 러시아워가 얹힌다
  (심야 0.15배 ~ 평상시 0.6배 ~ 러시아워 피크 2.5배, 전부 `BalanceConfig`).
- `PatrolSpawnMultiplier()` — 주간 1배, 심야 0.4배("야간 순찰은 있지만
  낮보다 적게" 요청 그대로 — 0으로 안 떨어짐).

**장식용 스폰을 성장 틱에서 분리**했다 — 예전엔 `HandleTickCompleted`(성장
틱마다, 기본 2초 간격)에서 차/순찰차/보행자를 각각 한 번씩만 시도했는데,
이제 `ConstructionLoop`(매 프레임) 안에서 타입별 독립 타이머가 흐른다:
`타이머 -= deltaTime × 그때그때의 배율`, 0 이하가 되면 스폰하고 기준
간격(`carSpawnIntervalSeconds` 등)으로 리셋. 배율이 높을수록(러시아워)
타이머가 더 빨리 닳아 더 자주 스폰되고, 낮을수록(심야) 뜸해진다 — 이게
"7~8시 출근 러시, 18~19시 퇴근 러시, 심야엔 감소"를 실제로 만든다.

HUD의 Day 표시를 `Day N · HH:MM`으로 확장(`TransitCityUIController.Update()`,
매 프레임 갱신 — 틱 이벤트를 타면 시계가 뚝뚝 끊겨 보이므로 별도로 뺐다).

### 헤드리스 검증

`dayLengthSeconds=60` 기준: 30초 진행 → 정확히 12.00시, 총 60초(하루 한
바퀴) → 0.00시로 순환 확인. `DayActivityFactor01`: 정오 1.00/자정 0.00.
아침 러시 피크(7.5시): `CommuteRushFactor01`=1.00, 교통 배율=2.50(설정한
피크값과 정확히 일치). 평상 낮(13시): 러시 0, 교통 배율≈0.59(기준 0.6에
근접), 순찰 배율≈0.99(기준 1에 근접). 심야(3시): 교통 배율≈0.22, 순찰
배율≈0.49 — 둘 다 설정한 심야 하한(0.15/0.4)보다 위이지만(자정에서 아직
좀 떨어진 시각이라) 뚜렷이 낮음, 0으로 죽지 않음(요청대로).

### 남은 작업

실제 플레이로 확인 필요: 러시아워 체감(밀도 급증이 보이는지), 심야 한산함,
순찰 경로가 이제 매번 다르게 도는지, HUD 시계 표시. `dayLengthSeconds`(하루
길이)·러시아워 시각·배율 전부 `BalanceConfig`에서 조정 가능.

## 프로토타입 v1.6 — 통근 방향(출근/퇴근) 반영 (2026-09-22)

### 문제

저녁 러시아워를 넣었는데도 장식용 차량이 여전히 전부 "집 → 바깥(이웃 도시
방향)"으로만 나갔다 — 퇴근이라면 반대로 바깥에서 집으로 들어와야 하는데,
경로 생성 로직이 방향을 아예 고려하지 않고 항상 한쪽으로만 짜여 있었다.

### 해결 — `HomeboundBias01`

`CityGridModel`에 0(출근 방향)~1(퇴근 방향) 값을 돌려주는
`HomeboundBias01()`을 추가했다 — 아침 러시아워 피크에서 0, 저녁 러시아워
피크에서 1이고 그 사이(주간)는 선형으로 전환, 저녁 피크~다음 날 아침 피크
(심야 포함) 구간도 자정을 넘나들며 끊김 없이 1→0으로 돌아온다.

`TrySpawnDecorativeCar`/`TrySpawnPedestrian`이 차량/보행자를 만들 때마다
이 값을 확률로 써서(`Random.value < HomeboundBias01()`) 방향을 정한다 —
집→바깥으로 나가는 기존 경로를 그대로 쓰거나(출근), 그 경로를 뒤집어
바깥→집으로 들어오는 경로로 쓴다(퇴근). 예약 대상 칸 목록(roadCoords)도
방향에 맞춰 뒤집었고, 도착점(건물)에는 예약이 필요 없다는 규칙은 양쪽
방향 다 대칭적으로 유지된다.

### 헤드리스 검증

아침 러시아워 피크(7.5시)에서 `HomeboundBias01`=0.00(전원 출근 방향),
저녁 러시아워 피크(18.5시)에서 1.00(전원 퇴근 방향), 그 중간(한낮)에서
0.50(반반) — 설계한 선형 전환과 정확히 일치. 자정·새벽 3시도 저녁 피크에서
다음 아침 피크로 매끄럽게 낮아지는 중간값(0.58, 0.35)으로 확인, 끊김 없음.

## 프로토타입 v1.7 — 관문 4곳으로 축소, 통근차 상한 분리, 낮 업무 통행 (2026-09-22)

### 외부 연결점을 4개 방향 1칸씩으로

원인: 인접 도시가 **왼쪽 가장자리 전체 열**이었다 — 도로를 그 열의 어느
칸에 이어도 다 "외부 연결"로 쳐줘서, 출구가 사실상 무한히 많았다. 여러
경로로 트래픽이 흩어지니 어느 구간도 용량을 넘길 일이 없어 정체가 전혀
안 생겼다(신고받음). 이제 `CityGridModel` 생성자가 **4방향 가장자리
중앙에 딱 한 칸씩**(서 `(0, h/2)` · 동 `(w-1, h/2)` · 북 `(w/2, 0)` · 남
`(w/2, h-1)`) 총 4개 관문만 둔다 — 어느 방향에서든 연결은 가능하지만, 그
방향에서 오는 모든 통행이 그 한 칸으로 몰려야 한다. 헤드리스로 정확히
4칸, 24×18 기준 `(0,9)/(12,0)/(12,17)/(23,9)`에 생기는 것 확인.

### 통근차 상한을 순찰차와 분리

`maxDecorativeCars`(통근차+순찰차 공용 상한, 12) 하나를 쓰고 있었다 —
러시아워에 통근차가 늘어야 할 때 순찰차가 같은 자리를 놓고 경쟁해서
실제 통근차 수가 적게 느껴졌다(사용자 질문: "일부러 그런 거냐" — 아님,
튜닝 미비였음). `maxCommuterCars`(30)/`maxPatrolCars`(6)로 분리하고,
기준 스폰 간격(`carSpawnIntervalSeconds`)도 0.8→0.5초로 낮췄다.

### 낮 시간대 업무 통행 — 건물→건물 / 건물→외부

새 `TrySpawnBusinessTrip()` — 출퇴근(집↔바깥)과 별개로, 가동 중인
(근무 인원>0) 인프라 건물에서 출발하는 트립. **건물 특성에 따라 목적지가
갈린다**: 상점은 무작위 주거지로 배달(건물→건물), 그 외(공장·발전소 등)는
기존 출퇴근과 같은 "이웃 도시 방향" 경로를 재사용해 물류/업무상 외부로
나간다(건물→외부). 건물→건물 트립을 위해 `CityGridModel.FindRoadPath
(from, to)`를 새로 추가했다 — 기존 `LastRoadParent`는 이웃 도시에 뿌리를
둔 고정 트리라 임의의 두 지점을 잇는 데는 못 쓰므로, 그때그때 새로 BFS.
스폰 빈도는 러시아워가 아니라 낮/밤 기본 활동 곡선만 따른다(심야에도
완전히 0은 아님). 배달 차량은 업무용 색(주황빛)으로 통근차와 구분된다.

### 헤드리스 검증

관문 4칸 정확한 위치 확인. `FindRoadPath`: ㄱ자로 이어진 14칸 경로를
정확히 복원(가로 8칸+세로 6칸), 도로가 안 닿는 지점으로는 빈 리스트(도달
불가) 정상 반환. 새 관문 체계로 유기적 성장 재확인 — 인구·자금 정상
증가, 회귀 없음(기존 테스트 스크립트들은 옛 "왼쪽 열 전체가 관문"
가정으로 짜여 있어 이번에 y=관문행 기준으로 다시 맞춰 검증함).

### 남은 작업

실제 플레이로 확인 필요: 4관문 체계에서 실제로 정체가 발생하는지, 통근차
밀도가 이제 충분히 느껴지는지, 상점 배달차가 실제로 주거지까지 잘
찾아가는지(경로가 없으면 조용히 스킵되므로 도로망이 끊긴 상점은 배달을
아예 안 보낼 수 있음 — 의도된 동작). `maxCommuterCars`/`maxPatrolCars`/
`businessTripSpawnIntervalSeconds` 전부 BalanceConfig에서 조정 가능.

## 프로토타입 v1.8 — NIMBY/PIMFY/NORMAL 분류, 의식주 필수 게이트, 필수 인프라 광역화 (2026-09-23)

### NIMBY/PIMFY/NORMAL 분류

`LandValuePreference(TileKind)` — PIMFY(+1, 비싼 땅 선호)는 상점·병원·학교,
나머지는 전부 NORMAL(0, 예전처럼 결핍만 본다). 배치 로직은 공장의 기존
패턴(Pass1 전에 부지를 미리 찜해두는 방식)을 일반화 — `FindLandValueSite`가
새로 열리는 빈 땅 중 PIMFY면 가장 비싼(주변 평균 Wealth 높은) 곳을 찾고,
빈 땅이 없어 기존 주거지를 밀어내야 할 때도 PIMFY는 재산 수준이 가장 높은
집을 대신 철거한다(NORMAL은 예전처럼 인구 최소인 집).

**당초 하수도·발전소도 NIMBY(싼 땅 우선)로 두려 했으나 헤드리스로 돌려보니
실제로 도시가 마비되는 버그가 나서 뺐다** — 이 게임에서 "싼 땅"은 거의
"관문(이웃 도시)에서 먼 땅"과 동의어인데, 정작 커버해야 할 주거지 대부분은
관문 근처에 몰려있다. 하수도·발전소를 NIMBY로 두면 그 시설이 반경 밖으로
밀려나 결핍이 영원히 안 풀리고, 같은 종류가 매 틱 다시 최우선으로 뽑혀
반복적으로 강제 전환(철거→재건설)되는 무한 루프에 빠졌다(공장 개수가
계속 늘어나는 등 실측으로 확인). "어디 있든 상관없는"(도달거리 개념
자체가 없는, `JobCoverage`가 도시 전체 집계인) 공장만 이 함정에서 자유롭기
때문에 NIMBY는 공장 전용으로 남겨뒀다. PIMFY는 반대로 괜찮다 — 비싼 땅은
대개 관문에서 가까워(Wealth가 Potential 상대순위 기반이므로) 오히려
커버리지에 유리하게 작용한다(헤드리스로 확인: 병원·학교 주변 평균
Wealth가 도시 평균보다 뚜렷이 높게 나옴).

### 상수도·하수도·전기 — 의식주 필수

`HasEssentialUtilities(tile)` — 셋 중 하나라도 커버리지 0이면(전혀 안
닿으면) 그 칸은 도로 완전 단절과 동급으로 취급(목표 만족도 0으로 강제,
`Potential<=0` 처리와 같은 급) + **입주 자체가 원천 봉쇄**(인구 증가
분기 조건에 `HasEssentialUtilities` 추가). `FindWorstInfraDeficit`도
이 셋을 별도 상위 티어로 분리해, 나머지 5종(치안·소방·교육·의료·소비)은
이 셋이 전부 충분해진 뒤에만 경쟁 대상이 된다 — "맨 처음 도로를 깔면
상수도·하수도·전기부터 들어와야" 요청 그대로.

**구현 중 실제로 도시가 영구 마비되는 순환 교착 버그를 발견해서 고쳤다.**
`LaborAvailability`(도시 전체 일자리 수요 대비 인구, 인프라 커버리지에
곱해지는 배율)가 있는데, 상수도 하나가 막 생긴 시점엔 일자리 수요(3)는
있고 인구는 0이라 `LaborAvailability=0`이 됐다. 그러면 **그 상수도
자신의 커버리지까지 0으로 계산돼**, "커버리지 있어야 입주 가능 → 입주해야
인구 생김 → 인구 있어야 LaborAvailability 회복 → LaborAvailability
있어야 커버리지 인정"이라는 순환 교착에 빠져 인구가 영원히 0에 머물렀다
(헤드리스로 재현: 상수도·하수도·전기가 계속 있는데도 80틱 내내 인구
0, 돈만 계속 줄어듦). 고친 방법: **상수도·하수도·전기는 노동시장
형편과 무관하게 항상 정상 가동**하는 것으로 취급(`ComputeCoverage`에서
이 셋만 LaborAvailability 배율을 안 곱함) — 애초에 의식주 필수 시설이
도시 경기에 좌우된다는 것 자체가 이상하기도 하고, 이걸로 교착이 완전히
풀렸다.

### 필수 인프라 반경 2~3배 확대

상수도·하수도·전기 전용 반경 필드 신설(`essentialUtilityRadius`=10,
`essentialUtilityRadiusPerTierBonus`=10 — 티어1=10, 티어2=20,
티어3=30홉) — 기존 일반 인프라(4/+2)의 2.5~3.75배. 최고 티어(30홉)면
어지간한 맵 크기(24×18)를 도로망이 어떻게 생겼든 하나로 커버할 만큼
넓다는 목표에 맞춤. `UtilityRadiusForTier`가 이제 종류를 받아 상수도·
하수도·전기만 이 확대된 반경을 쓰고 나머지는 기존 반경을 그대로 쓴다.

### 헤드리스 검증

- **부트스트랩 순서**: 빈 도로에서 시작 → t1 상수도 → t2 하수도 → t3
  전기 → t4부터 비로소 치안·공장 등 비필수 등장. 필수 3종이 전부
  비필수보다 먼저 나오는 것 정확히 확인.
- **입주 게이트**: 필수 인프라 하나도 없는 상태로 10틱 방치 → 인구 0
  유지(입주 자체가 안 일어남) 확인. 반대로 8종+공장이 갖춰진 80틱
  시뮬레이션에서는 인구가 200~256으로 정상 증가, 자금도 꾸준히 우상향 —
  교착 버그 수정 전/후 대조로 고쳐졌음을 확인.
- **PIMFY 검증**: 60틱 뒤 병원 주변 평균 Wealth=0.70, 학교 주변=0.62 —
  도시 전체 평균(0.40)보다 뚜렷이 높음. 반면 NORMAL인 치안시설은 특별한
  경향 없이(0.10, 이번 판에서는 우연히 낮은 지역) 결핍 위주로만 배치됨.
- **순환 교착 버그**: 수정 전엔 상수도 커버리지가 상수도 생성 다음 틱부터
  0으로 붕괴하고 이후 영원히 회복 안 됨(같은 종류가 계속 재평가돼 반복
  강제 전환되며 개수만 계속 늘어남 — 실측: 하수도 4개·발전소 3개까지
  누적). 수정 후 단일 상수도로 안정적으로 0.50 커버리지 유지 확인.

### 남은 작업

실제 플레이로 확인 필요: 초반 상수도·하수도·전기 순차 등장이 체감되는지,
필수 인프라 없는 구역에서 정말 아무도 안 들어오는지, PIMFY(상점·병원·학교)가
번화가에 몰리는 느낌이 나는지. 반경·게이트 수치 전부 BalanceConfig에서
조정 가능.

## 프로토타입 v1.9 — 니즈 3종 추가: 여유(공원)·교통·위생(쓰레기장) (2026-09-23)

작업 전 `transitcity.md` §5.8을 전면 갱신 — R/C/I·화물 산업 체인·계층
분화 등 이미 구현과 어긋난 옛 절들을 대체하는 "니즈 목록" 표와 NIMBY/
PIMFY/NORMAL 분류 원칙을 문서화했다(사용자 요청: 작업 전 md 정리).

### 새 니즈 3종

- **여유**(공원/녹지, `UtilityPark`) — **소득 상위 니즈**: 요구치가
  일반 니즈(`InfraDemand`, Wealth에 선형 비례)와 다른 별도 곡선
  (`LeisureDemand = Lerp(0, 0.8, Wealth²)`)을 쓴다 — 저소득층은 거의
  신경 안 쓰고 고소득층만 뚜렷이 신경 쓴다는 뜻. PIMFY(비싼 땅 우선).
- **위생**(쓰레기장, `UtilityGarbage`) — 결핍-우선 시스템에 그대로 참여
  ("위생 수치가 일정 이하로 떨어지면 자동으로 생김"은 이미 있던 결핍
  임계치 메커니즘과 동일). NIMBY(싼 땅 우선) — 공장 외에 처음으로 NIMBY를
  단 소프트 니즈. 소프트라서(입주를 막지 않음) 안전 — §5.8.2에 문서화한
  대로 필수 니즈였다면 이번에도 악순환에 빠졌을 것.
- **교통** — 유일하게 전용 건물이 없는 니즈. 그 칸 진입로의 통근 경로
  혼잡도를 그대로 `1-혼잡도`로 환산(`TransitCoverage`), 결핍-우선
  자동배치 대상에서 제외(지을 건물이 없으므로) — 오직 도로 설계로만
  개선된다. 나중에 버스·지하철이 생기면 정류장 근접도가 여기 합산될
  지점으로 미리 마련해둠.

`InfraDeficit` 평균이 9개 항목에서 12개 항목으로 늘었다(여유·위생·교통
추가). `FindWorstInfraDeficit`의 일반 5종 경쟁 목록도 7종으로 확장
(공원·쓰레기장 추가) — 교통은 지을 게 없어 여기 안 들어간다.

### 헤드리스 검증

- 100틱 유기적 성장: 9종에서 12종 니즈로 늘었어도 인구·자금 정상 증가
  (196~245명, 자금 우상향), 쓰레기장이 1개로 안정(폭주 없음 — NIMBY+
  소프트 니즈 조합이 실제로 안전함을 재확인).
- `FindLandValueSite`를 리플렉션으로 직접 호출해 공원·쓰레기장 후보지를
  비교 — 이 특정 테스트 시점엔 도로변 빈 땅이 단 1칸만 남아있어(60틱 뒤
  거의 포화) 두 후보가 같은 칸으로 나왔다(선택지가 하나뿐이라 방향성이
  드러날 여지가 없었을 뿐, 로직 버그 아님 — 같은 비교 로직으로 이미
  검증된 병원·학교 PIMFY 결과(주변 평균 Wealth 0.70/0.62, 도시 평균
  0.40보다 뚜렷이 높음)가 이 로직이 올바르게 동작함을 이미 증명함).
- `LeisureDemand` 곡선 확인: 표본 타일 wealth=0.64에서 일반 `InfraDemand`
  =0.52인데 `LeisureDemand`=0.33 — 같은 Wealth에서도 눈에 띄게 다른(더
  낮게 시작해 고소득 구간에서 더 가파르게 오르는) 곡선임을 확인.

### 남은 작업

실제 플레이로 확인 필요: 부촌에 공원이 실제로 몰리는 느낌이 나는지(땅이
넉넉한 초반~중반 구간에서 관찰 필요 — 위 헤드리스 테스트처럼 땅이 거의
포화된 후반부는 선택지 자체가 부족해 판단하기 어려움), 쓰레기장이 외곽에
자리 잡는 느낌, 교통 니즈 정보 패널 표시. UI에 여유/위생/교통 정보 행
추가 완료(`TransitCityHud.uxml`/`TransitCityUIController.cs`).

## 프로토타입 v1.10 — 인프라 중복 건설 방지("담당구역 안엔 또 안 짓는다") (2026-09-23)

### 문제

초반에 상수도·하수도·전기가 필요 이상으로 많이 지어졌다. 원인: 결핍-우선
배치가 **도시 전체 평균 결핍**만 보고 "어디"는 안 따졌다 — 이미 어떤
동네가 특정 시설로 잘 커버되고 있어도, 다른 동네가 아직 결핍이면 평균이
임계치를 넘어 "그 종류가 더 필요하다"고 판단했고, 그 판단에 따라 자리를
고를 때(새 빈 땅이든 강제 전환이든) **이미 커버된 곳 바로 옆에 또 지어도
막을 방법이 없었다.**

### 해결 — 담당구역 커버리지 사전 확인

`ComputeSameKindCoveredRoads(kind)` — 지금 존재하는 같은 종류 시설들이
이미 커버하고 있는 도로 칸 전체(합집합)를 시설 개수만큼만 플러드필해서
계산한다(가볍다). 이번 틱 우선순위로 뽑힌 종류가 Pass1에서 자리를 고를
때(새 빈 땅이든, 대체 후보 주거지 강제 전환이든) **이미 이 범위 안에
있는 칸은 후보에서 제외**한다 — 커버되고 있는 동네엔 또 안 짓고, 아직 안
닿는 동네 쪽으로 자연히 밀려난다. 도시 전체 평균 대신 "이 칸은 이미
됐는가"를 칸 단위로 직접 확인하는 방식으로 바뀐 것.

### 헤드리스 검증

- 20칸 도로 100틱 성장: 이전엔 하수도가 잠깐 2개까지 늘었었는데, 이번엔
  **상수도·하수도·전기·교육·의료·소비·쓰레기장·공장·치안·소방 전부
  정확히 1개씩으로 100틱 내내 고정**(중복 없음). 인구·자금도 정상 증가.
- 23칸 긴 도로(상수도 1티어 반경의 2배 이상) 150틱: 상수도가 여전히
  1개뿐이길래 "필요한 확장까지 막은 건 아닌지" 의심했으나, 직접 확인해보니
  상수도가 **자체적으로 2티어로 승급**(반경 10→20홉)해서 평균 결핍이
  0.132로 임계치(0.15) 바로 밑까지 내려간 상태였다 — 즉 "이 정도면
  충분하다"는 기존 임계치 판단이 정상 작동한 것이지, 이번 수정이 필요한
  확장을 막은 게 아님(같은 조건에서 인구가 더 늘어 임계치를 다시 넘으면
  두 번째 상수도도 정상적으로 생길 것).

### 남은 작업

실제 플레이로 확인 필요: 초반 부트스트랩에서 상수도·하수도·전기가 각각
정확히 하나씩만 생기는 느낌이 나는지, 도시가 커지면서 정말 필요할 때
(기존 시설 티어가 다 올랐는데도 결핍이 안 풀릴 때) 두 번째 시설이
정상적으로 생기는지.

## 프로토타입 v1.11 — 필수 인프라 커버리지를 이진값(0 또는 1)으로 (2026-09-23)

사용자 판단: 상수도·하수도·전기는 "닿으면 100, 안 닿으면 0"으로 극단적이어야
한다 — 물이 반만 나온다는 개념 자체가 이상하고, 어차피 같은 구역에 중복
건설도 안 되니(§v1.10) 그라데이션으로 메울 상황 자체가 없다는 것.
`ComputeCoverage`에서 필수 3종만 거리 비례 그라데이션 대신 반경 안이면
무조건 1.0, 밖이면(이미 그렇듯) 0으로 고정했다. 소프트 니즈(치안·소방·
교육·의료·소비·여유·위생)는 기존 그라데이션 그대로 유지 — "가까울수록
좋다"는 감각이 남아있어야 자연스러운 니즈들이라 구분해서 적용.

헤드리스 검증: 반경 안 주거지는 정확히 1.00, 반경 밖은 정확히 0.00.
100틱 재성장 테스트도 회귀 없음 재확인(전 인프라 종류 1개씩 안정 유지).

## 프로토타입 v1.12 — 인프라 반경 확대, 주거지도 이주하며 진화 (2026-09-23)

### 인프라 영향 반경 전체적으로 확대

전반적으로 반경이 좁다는 지적 — 일반 인프라(치안·소방·교육·의료·소비·
여유·위생) 기본 반경을 4→8홉(2배), 티어당 증가폭을 2→6홉으로(3티어면
20홉까지) 올렸다. 상수도·하수도·전기(필수) 기본 반경도 10→14홉, 티어당
10→13홉(3티어면 40홉 — 웬만한 맵을 하나로 커버). 순찰 보너스도 2→4홉.
`.asset` 파일에 이미 저장된 값이 새 C# 기본값을 덮어써서 안 먹히는 걸
알아채고 asset YAML을 직접 갱신 + 재임포트해서 반영했다(이 프로젝트
전역에서 반복되는 함정 — 새 필드는 기본값이 먹히지만 **기존 필드의
값 변경은 asset에 이미 저장된 값이 우선**한다).

### 주거지도 이주하며 진화

인프라 시설은 이미 "제자리에 공간 없으면 다른 대로변 자리로 통째로
이주"하는 로직이 있었는데, 주거지는 없었다(예전엔 "사람 사는 집은
텔레포트 못 한다"는 이유로 의도적으로 뺐던 것) — 사용자가 이번에
명시적으로 요청해서 추가했다. 거주자 수·재산·만족도·필지 크기는 그대로
데리고 "이사"한다.

**구현하면서 두 가지 실제 버그를 발견하고 고쳤다:**

1. **Pass1이 빈 땅을 한 틱 안에 전부 먹어치우는 문제.** 처음엔 승급
   판정(주거지 루프, Pass1 **이후**)에서 이주 후보를 찾아 대기열에
   쌓았다가 루프가 끝난 뒤 처리했는데, 이 시점엔 이미 Pass1이 그 틱에
   열린 빈 땅을 전부 다른 용도로 채운 뒤라 **이주할 곳이 하나도 안
   남아있었다**(장기 테스트로 재현: 40틱 넘게 이주가 단 한 번도
   안 일어남). 해결: 공장·NIMBY/PIMFY와 똑같은 패턴으로, **Pass1이
   시작되기 전에** 이주가 필요한 주거지가 있는지 먼저 찾아서(
   `FindResidentialRelocationSiteIfNeeded`) 목적지를 미리 찜해두고,
   Pass1이 그 칸에 도달하면 최우선으로 배정한다.
2. **캐시된 Potential을 썼다가 또 못 찾는 문제.** 1번을 고치고도 여전히
   이주할 곳을 못 찾길래 파봤더니, 기존에 있던 `FindRelocationSiteForFootprint`
   (인프라 이주용으로 먼저 만들어둔 함수)가 `tile.Potential`이라는
   **캐시된 필드**를 봤다 — 이 필드는 Pass1이 돌 때만 갱신되는데, 내
   이주 예약은 Pass1 **이전**에 실행되므로 그 시점엔 아직 이번 틱
   값으로 안 갱신된 상태(방금 막 비워진 칸 등은 기본값 0)였다. 공장/
   NIMBY/PIMFY는 애초에 이 문제를 피하려고 캐시 대신 그때그때
   `ComputePotential(roadDistance)`로 새로 계산하고 있었는데, 이주
   함수만 옛날 방식을 그대로 쓰고 있었던 것 — 같은 방식(로드디스턴스
   기반 즉석 계산)으로 통일해서 고쳤다.
3. **"Collection was modified" 런타임 예외.** 두 버그를 고친 뒤 처음
   돌렸을 때 실제로 이 예외가 났다 — Pass1이 `tiles.Values`를 순회하는
   **도중에** 새로 이주해온 칸의 발자국(1x2/2x2)을 그 자리에서 바로
   점유(`ClaimFootprint`)하려 했는데, 이 함수가 확장 칸의 딕셔너리
   엔트리를 통째로 교체하는 구조라 순회 중 딕셔너리를 건드리면 즉시
   깨진다. 발자국 점유(및 원래 칸 비우기)를 전부 Pass1 루프가 끝난
   뒤로 미뤄서 해결 — 이 프로젝트에서 반복적으로 마주친 "순회 중
   딕셔너리 구조 변경 금지" 원칙을 여기서도 지켜야 했다.

### 헤드리스 검증

이웃 4칸을 전부 다른 주거지로 채워 제자리 승급이 불가능한 상태를 만들고
승급 스트릭을 강제로 채운 뒤 한 틱 실행 — 원래 칸은 Empty로 비워지고,
다른 자리에 인구·재산을 그대로 유지한 2티어 주거지가 새로 생김을 확인.
100틱 유기적 성장 재테스트도 예외 없이 통과(인프라 전 종류 안정, 인구·
자금 정상 증가) — 실제 게임 진행 중에도 이주가 여러 번 일어날 텐데
문제없이 돌아감을 확인.

### 남은 작업

실제 플레이로 확인 필요: 반경 확대 체감, 주거지 이주가 시각적으로
자연스러운지(비주얼은 OnZoneRemoved/OnZoneSpawned 이벤트를 그대로
재사용하므로 기존 건물이 사라지고 새 건물이 다른 자리에 나타나는
방식 — 부드러운 "이동" 애니메이션은 아님, 필요하면 후속 작업).

## 프로토타입 v1.13 — 주거지 정원을 밀도×소득 3×3 표로 세분화 (2026-09-23)

"성장시키는 재미가 있으니 티어를 좀 더 늘려달라, 저밀도/중밀도/고밀도를
각각 고소득/중소득/저소득으로 더 쪼개달라"는 요청. 기존엔 밀도 티어
(`Tier` 1~3)만 정원을 정했다(20/35/55) — 여기에 재산 수준(`Wealth`)을
3등분한 소득 계층(`IncomeTier`)을 교차시켜 9단계 정원 표를 만들었다.
설계는 §5.8.8에 먼저 정리(이 세션의 확립된 순서: 코딩 전에 md부터).

- `CityTile.cs`: `IncomeTier` enum(Low/Middle/High) + `CityTile.IncomeTier`
  필드 추가.
- `BalanceConfig.cs`: `houseCapacity`/`densityTier2Capacity`/
  `densityTier3Capacity`(3개) → `residentialTier{1,2,3}{Low,Middle,High}Capacity`
  (9개)로 교체. 값: 15/20/26 · 32/40/48 · 58/70/85 — 같은 밀도에서 소득이
  높을수록 정원도 더 크게(9단계 전체가 매끄러운 오름차순이 되도록), 밀도
  축이 여전히 주력 성장 레버가 되게 설계.
- `CityGridModel.cs`: `IncomeTierFor(wealth)`(경계 0.34/0.67 — §10 UI의
  "재산 수준: 상/중/하" 표시와 동일하게 맞춤, 안 맞추면 화면 표시와 실제
  정원 판정이 어긋난다) + `ResidentialCapacityFor(tier, income)` 추가,
  기존 `DensityCapacityForTier` 삭제. `tile.Capacity`를 정하는 4곳(신규
  발생/승급/강등/이주)에서 전부 `tile.IncomeTier`도 같이 갱신하도록
  변경 — **재개발 시점에만** 갱신하고 그 사이엔 고정(매 틱 흔들리는
  `Wealth`를 그대로 쓰면 재건축 없이도 정원이 들쭉날쭉해진다). `DemolishAt`
  등 필드 리셋 지점에도 `IncomeTier = Low` 추가해 누락 없이 맞춤.
- `TransitCityUIController.cs`: 타일 인스펙터의 "밀도: 저밀도" 표시를
  "밀도: 저밀도 · 고소득"처럼 소득 계층도 같이 보여주도록 `IncomeTierLabel`
  헬퍼 추가.

### 헤드리스 검증

새로 생긴 주거지가 `Wealth=0.04`(신축 기본 저소득) → `IncomeTier=Low`,
`Capacity=15`(표의 T1-Low)로 정확히 일치하는 것 확인. 이후 `Wealth=0.9`로
강제하고 승급 스트릭을 채워 2티어로 승급시키니 `IncomeTier=High`,
`Capacity=48`(표의 T2-High)로 정확히 갱신됨을 확인 — 재개발 시점에
소득 계층이 다시 계산된다는 설계가 의도대로 동작. 100틱 유기적 성장
회귀 테스트도 예외 없이 통과(인프라 전 종류 안정, 인구·자금 정상 증가).

### 남은 작업

`houseCapacity` 등 옛 3개 필드가 빠진 자리를 `BalanceConfig.asset`이
아직 옛 값으로 들고 있다(무시되는 고아 키, 무해 — 이 세션에서 반복
확인된 패턴). 실제 플레이로 9단계 진행이 체감상 "더 자주, 더 잘게"
성장하는 느낌을 주는지 확인 필요 — 밸런스 수치(15~85)는 첫 설계값이라
플레이 피드백에 따라 조정 여지 있음.

## 프로토타입 v1.14 — 부트스트랩 시 의식주 우선순위 역전 버그 수정 (2026-09-23)

"처음에 도로를 지으면 상수도·하수도·전기도 없는데 왜 주거지부터
생기나"는 질문에서 발견. §5.8.1에 "의식주 필수 3종은 결핍-우선 배치에서
항상 최우선"이라고 적어뒀지만, 실제 판정 함수 `FindWorstInfraDeficit`가
`residentials.Count == 0`이면 곧바로 `null`을 돌려주는 얼리리턴을 갖고
있었다 — 결핍을 "주거지 전체 평균"으로 재는 구조라, 도로를 막 깐 시점
(주거지 0채)엔 평균 자체가 없어 결핍도 0으로 잡혔던 것. 그 결과 첫
틱엔 인프라 우선순위 검사(`infraNeeded`)가 통째로 꺼진 채로 Pass1이
대로변 빈 땅을 전부 주거지로 채워버렸고, 상수도·하수도·전기는 몇 틱
뒤 주거지가 쌓여 결핍이 실측되고 나서야(그것도 이미 자리 잡은 주거지를
강제 철거·전환해서) 뒤늦게 들어왔다 — 설계 의도와 순서가 반대였다.

고침: `residentials.Count==0` 체크보다 먼저, 의식주 필수 3종 중 **도시
전체에 하나도 없는 종류**가 있으면 주거지 존재 여부와 무관하게 결핍
1.0(최댓값)으로 최우선 반환하도록 앞단에 추가. 헤드리스로 확인(대로에
`MainRoad`로 16칸 깔고 틱별 종류 집계): t1에 상수도 1개 생성(+주거지
32개는 같은 틱에 동시 채워짐 — 한 틱-하나 제한 때문에 구조적으로
불가피), t2에 하수도 추가, t3에 전기 추가 — 의식주 3종이 정확히
1·2·3틱째에 순서대로 확정됨을 확인. 이전엔 5틱을 돌려도 셋 다 전혀
안 생기는 경우도 있었다(테스트에 `LocalRoad`를 썼던 첫 시도 — 인프라는
대로변에만 서므로 당연한 결과, 이건 버그가 아니라 테스트 설계 실수였고
`MainRoad`로 바꿔서 재확인). 100틱 유기적 성장 회귀 테스트도 그대로
통과(예외 없음, 인구·자금 정상 증가) — t0 스냅샷에 이제 `UtilityWater=1`이
즉시 잡히는 것도 확인.

§5.8.1에 이 부트스트랩 규칙(한 틱-하나 제한이 있는 한 "완전히 인프라부터
깔리고 그다음 주거지"는 구조적으로 불가능하다는 점 포함)을 명시적으로
추가.

## 프로토타입 v1.15 — 치명적 버그: 필수 인프라가 인구 400 전엔 2호기를 못 지어 인구 붕괴 (2026-09-23)

"저번엔 계속 켜놓으면 결국 우상향한다고 했잖아, 근데 지금은 인구가
줄어버리네 — 뭐가 바뀐 거냐"는 질문에서 출발. 짧은 도로(20칸)에서는
100~600틱을 돌려도 인구가 150~250 사이에서 건강하게 오르내렸지만,
**실제 플레이처럼 도로를 점점 늘려가는 상황**(20칸→80칸으로 확장)을
헤드리스로 재현하니 t99 근처부터 평균 전기 커버리지가 0.97에서 0.03으로
추락하고 t185 즈음 인구가 10 밑으로 떨어진 뒤 **다시는 회복하지 못하고
계속 0**으로 남았다(t200~t400 전부 0). 발전소 개수를 같이 찍어보니
붕괴 내내 "전기1"로 고정 — 커버리지가 거의 0인데도 2호기가 전혀 안
지어지고 있었다.

원인은 `RunTick`의 인프라 총량 상한(`Mathf.Max(PriorityPlacementKinds.Length,
Population/config.utilityCapPerPopulation)`, `Assets/Scripts/Games/
TransitCity/Simulation/CityGridModel.cs:406`)이었다. 이 하한(`PriorityPlacementKinds.Length`
=10=인프라 종류 수)은 "소프트 니즈가 무한정 늘어나며 주거지를 잡아먹는
폭주"를 막으려던 것인데, 상수도·하수도·전기 등 10종이 각 1개씩만
있어도 총량이 이미 10에 도달해버려 인구가 400을 넘기 전까진(=인구/40>10)
**어떤 인프라도(의식주 포함) 2호기를 절대 못 짓는** 부작용이 있었다.
도로가 늘어나 발전소 하나의 반경(최고 티어도 40홉, §5.8.6) 밖으로
도시가 커지면 진짜 2호기가 필요한데, 이 상한에 막혀 영원히 못 지어지고,
커버 못 받는 구역은 입주 게이트에 걸려 인구가 죽고, 인구가 죽으니
상한(인구/40)은 더 안 오르고 — 빠져나올 수 없는 죽음의 소용돌이였다.

고침: `infraNeeded` 판정에서 **의식주 필수 3종(상수도·하수도·전기)은
이 총량 상한을 아예 적용하지 않는다** — `IsEssentialUtilityKind(...) ||
totalUtilityCount < reasonableUtilityCap`. 필수 3종은 애초에
`IsAlreadyCoveredBySameKind`(§5.8.6, 이미 커버된 구역엔 또 안 짓는다)
만으로 충분히 안전하므로(진짜 안 겹치는 자리에만 새로 서니까), 총량
상한까지 이중으로 걸 필요가 없었다. 소프트 니즈(치안·교육 등)는 기존
상한 그대로 유지 — 그쪽의 "폭주 방지"라는 원래 목적은 여전히 유효하다.

헤드리스 재검증: 버그를 재현했던 20→80칸 확장 시나리오를 다시 돌리니
t200까지 인구 185~229에서 건강하게 유지(붕괴 없음). 반경을 확실히
넘는 120칸짜리 대로를 한 번에 깔고 150틱을 돌려도 인구 296~397에서
안정. 기존 100틱 회귀 테스트도 그대로 통과(예외 없음).

**교훈:** 이번 세션에서 "의식주 필수는 소프트 니즈와 다른 규칙이 필요하다"는
패턴이 벌써 세 번째다(NIMBY 금지 → LaborAvailability 면제 → 이번 총량
상한 면제). 필수 3종에 새 제약을 추가할 때마다 "이것도 소프트 니즈
전용 규칙 아닌가?"를 먼저 물어야 한다 — §5.8.2에도 같은 취지로
기록해둠.
