# 고스톱 / 맞고 통합 게임 기획서 및 개발 명세

## 0. 프로젝트 목표

현재 개발 중인 화투 게임에 **2인 맞고 / 3인 고스톱 / 4인 고스톱(광팔이)**을 하나의 게임 시스템으로 통합한다.

방에 입장한 실제 참가 인원에 따라 게임 타입을 자동으로 결정한다.

```text
2명 참가
→ 맞고

3명 참가
→ 일반 3인 고스톱

4명 참가
→ 4인 참가 판정
→ 광팔이/죽기
→ 최종 3명
→ 일반 3인 고스톱
```

핵심은 **게임 타입에 따라 별도의 게임을 새로 만드는 것이 아니라, 공통적인 고스톱 게임 엔진을 기반으로 하고 게임 시작 전의 참가 인원 및 패 분배/점수 조건 등을 게임 모드에 따라 변경하는 구조**로 만드는 것이다.

---

# 1. 게임 모드 결정

방에 참가한 인원에 따라 게임 모드를 결정한다.

| 참가 인원 | 게임 모드  | 실제 플레이 인원 |
| ----- | ------ | --------: |
| 2명    | 맞고     |        2명 |
| 3명    | 일반 고스톱 |        3명 |
| 4명    | 4인 고스톱 |     최종 3명 |

4인 고스톱의 경우 4명이 동시에 플레이하는 것이 아니다.

4명이 방에 참가하면 **게임 시작 전에 참가 여부를 결정하고 최종적으로 3명의 Active Player를 만든다.**

---

# 2. 게임 모드의 기본 설정

## 2.1 맞고

* 실제 플레이어: 2명
* 손패: 각 10장
* 바닥: 8장
* 목표 점수: 일반적으로 7점부터 Go/Stop 가능
* 패 분배: 4-5-4-5 방식

일반적인 맞고는 각 10장, 바닥 8장으로 시작하며 7점을 기준으로 Go/Stop을 판단하는 방식이 널리 사용된다.

### 패 배분 애니메이션

```text
바닥 4장
↓
플레이어 A 5장
↓
플레이어 B 5장
↓
바닥 4장
↓
플레이어 A 5장
↓
플레이어 B 5장
```

최종:

```text
A = 10장
B = 10장
Field = 8장
Deck = 나머지
```

---

# 3. 3인 일반 고스톱

## 3.1 기본 설정

* 실제 플레이어: 3명
* 손패: 각 7장
* 바닥: 6장
* 목표 점수: 일반적으로 3점
* 패 분배: 3-4-3-3 방식

일반적인 3인 고스톱은 각 7장, 바닥 6장으로 시작한다.

### 패 배분 애니메이션

선 플레이어를 기준으로 정해진 턴 순서대로 패를 배분한다.

```text
바닥 3장
↓
Player 1 4장
↓
Player 2 4장
↓
Player 3 4장
↓
바닥 3장
↓
Player 1 3장
↓
Player 2 3장
↓
Player 3 3장
```

최종:

```text
Player 1 = 7장
Player 2 = 7장
Player 3 = 7장
Field = 6장
Deck = 21장
```

패 배분은 실제 카드가 순서대로 이동하는 애니메이션으로 구현한다.

---

# 4. 4인 고스톱

4인 고스톱은 **4명이 동시에 플레이하는 게임이 아니다.**

4명이 방에 들어오면 먼저 참가 여부를 결정하여 최종 3명의 플레이어를 확정한다.

---

# 5. 4인 고스톱 참가 결정 순서

## 5.1 선 플레이어

선 플레이어는 무조건 게임에 참가한다.

선 플레이어에게 참가 여부를 묻지 않는다.

---

## 5.2 2번째 플레이어

2번째 플레이어에게 게임 참가 여부를 묻는다.

### 참가

다음 3번째 플레이어에게 참가 여부를 묻는다.

### 불참

2번째 플레이어를 게임에서 제외한다.

그리고:

```text
선 + 3번째 + 4번째
```

가 게임에 참가한다.

이 경우 **광팔이는 발생하지 않는다.**

---

## 5.3 3번째 플레이어

2번째 플레이어가 참가한 경우 3번째 플레이어에게 참가 여부를 묻는다.

### 불참

3번째 플레이어를 게임에서 제외한다.

그리고:

```text
선 + 2번째 + 4번째
```

가 게임에 참가한다.

이 경우에도 **광팔이는 발생하지 않는다.**

### 참가

선 + 2번째 + 3번째가 모두 참가하게 된다.

이 경우 4번째 플레이어는 강제로 광팔이를 진행한다.

---

# 6. 4번째 플레이어 광팔이

다음 조건에서만 광팔이가 발생한다.

```text
선 = 참가
2번 = 참가
3번 = 참가
4번 = 남은 플레이어
```

4번째 플레이어는 선택할 수 없다.

**강제로 광팔이를 하고 게임에서 빠진다.**

---

# 7. 광팔이 대상 패

광팔이 금액을 계산할 때 단순히 광패의 개수만 계산하지 않는다.

다음 패를 모두 판매 대상 장수에 포함한다.

### 포함

* 일반 광
* 비광
* 쌍피
* 국열끗
* 보너스패

즉:

```text
광패 수
+
쌍피 수
+
국열끗 수
+
보너스패 수
=
광팔이 대상 장수
```

광패는 일반 광/비광을 구분하지 않는다.

광패라면 모두 1장으로 카운트한다.

---

# 8. 광팔이 금액

광팔이 금액은 **판매 대상 패의 총 장수 × 1점 가격**을 기준 단가로 계산하되, 참가가 확정된 2번·3번 플레이어가 **각자 이 기준 단가 전액을 독립적으로**(분담이 아니다) 광팔이 플레이어(4번)에게 지급한다. 정확한 지급 방식은 49.1을 따른다.

예:

```text
1점 = 100원

광 2장
쌍피 2장
국열끗 1장
보너스패 1장

총 6장

기준 단가 = 6 × 100원 = 600원

2번 플레이어 → 600원 지급
3번 플레이어 → 600원 지급
4번(광팔이) 플레이어 → 총 1,200원 수령
```

1점 가격의 설정 방식, 최소/최대 범위, 지급 주체(누가 얼마씩 부담하는지)는 49번(정산/머니 세부 규칙)을 따른다.

> **2026-08-23 확정**: 기존 프로젝트에 이미 구현·검증돼 있던 광팔이 정산 규칙(장당 100원씩 2·3번째 플레이어가 **각자** 지급 → 4번째 실수령은 장당 200원)을 확정 값으로 채택했다. 이 문서의 최초 초안은 "총액을 절반씩 균등분담(장당 100원 실수령)"이었으나, 기존 구현을 우선한다는 47번 원칙에 따라 위와 같이 정정했다.

---

# 9. 광팔이 금액 지급 시점

광팔이 금액은 **본 게임 시작 전에 지급한다.**

순서:

```text
4번 플레이어 광팔이
↓
판매 대상 패 장수 계산
↓
광팔이 금액 계산
↓
즉시 해당 플레이어에게 지급
↓
해당 플레이어 게임 OUT
↓
3인 고스톱 시작
```

본 게임 종료 후 정산에 포함시키지 않는다.

**지급 주체**: 선 플레이어는 광팔이 금액을 지급하지 않는다. 참가가 확정된 나머지 두 플레이어(2번, 3번)가 **각자 독립적으로 전액**을(분담이 아니다) 4번 플레이어에게 지급한다. 정확한 계산 방식은 49.1을 참조한다.

---

# 10. 광팔이 플레이어의 게임 내 취급

광팔이한 플레이어는 해당 판에서 **게임 플레이어로 취급하지 않는다.**

따라서:

* 승패 계산 제외
* 점수 계산 제외
* 피박 제외
* 광박 제외
* 멍박 제외
* 고박 제외
* 본 게임 머니 정산 제외
* 비상 시스템 대상 제외
* 게임 결과 승자/패자 목록에서 제외

단, 결과 화면에는:

```text
광팔이 수익 +600
```

처럼 별도의 수익 항목을 표시한다.

---

# 11. 일반 죽기

2번째 또는 3번째 플레이어가 불참을 선택한 경우 이를 "죽기"로 처리한다.

죽은 플레이어는:

* 본 게임 참가 X
* 광팔이 X
* 광팔이 수익 X
* 패를 뒷패 더미에 반환
* 게임 관전
* 본 게임 정산 제외

죽은 플레이어도 광팔이 플레이어와 마찬가지로 해당 게임의 Active Player가 아니다.

단, 시스템상 `Dead`와 `LightSeller` 상태는 구분해서 관리한다.

---

# 12. 죽은 플레이어의 패 처리

죽은 플레이어의 손패는 게임에서 제거하고 **남은 뒷패 더미에 섞는다.**

주의:

* 단순히 버리는 것이 아니다.
* 다시 뒷패에 포함시켜야 한다.
* 랜덤하게 섞은 뒤 게임 진행에 사용한다.

---

# 13. 4인 게임의 최종 구조

4인 게임에서 최종적으로는 반드시 3명의 Active Player가 존재한다.

```text
FourPlayerLobby
        ↓
Participation Phase
        ↓
Active Player 결정
        ↓
Dead / LightSeller 처리
        ↓
3인 GoStop Game
```

실제 고스톱 엔진은 가능한 한 **3인 고스톱과 동일한 엔진을 사용한다.**

---

# 14. 최초 선 결정

첫 게임은 랜덤 패를 이용하여 선을 결정한다.

각 플레이어에게 패를 공개하고 가장 높은 숫자의 패를 가진 플레이어가 선이 된다.

동률이면 동률 플레이어끼리 다시 선 결정 패를 뽑는다.

> 비고: 동률 재시도가 반복될 경우의 무한루프 방지 로직은 발생 확률이 낮다고 판단하여 이번 작업 범위에서는 별도로 다루지 않는다. (필요 시 추후 검토)

---

# 15. 이후 게임의 선

정상적으로 게임이 종료되고 승자가 결정되면:

**직전 게임의 승자가 다음 게임의 선이 된다.**

다음 게임은 해당 선을 기준으로 플레이 순서를 다시 계산한다.

---

# 16. 나가리

나가리는 특별하게 처리한다.

나가리가 발생하면:

* 승자 없음
* 기존 선 유지
* 새로운 선 결정 없음
* 다음 판 참가 여부 결정 생략
* 광팔이 생략
* 기존 Active Player 구성 유지
* 다음 게임의 기본 게임비는 2배

즉:

```text
나가리
↓
기존 선 유지
↓
참가 결정 과정 생략
↓
광팔이 생략
↓
기본 게임비 × 2
↓
즉시 다음 게임 시작
```

다음 판은 직전 판의 Active Player 구성을 그대로 사용한다.

---

# 17. 정상적인 다음 게임

나가리가 아닌 경우:

```text
게임 종료
↓
승자 결정
↓
승자가 다음 판 선
↓
4인 참가자 재정비
↓
선 무조건 참가
↓
2번 참가 여부
↓
3번 참가 여부
↓
필요한 경우 4번 광팔이
↓
3인 고스톱 시작
```

---

# 18. 카드 구성

기본적으로 일반적인 화투 48장을 사용한다.

게임의 특수패/보너스패가 별도로 존재하는 경우 현재 프로젝트의 카드 데이터 정의를 우선한다.

카드 데이터에는 최소한 다음 정보를 갖도록 한다.

```text
Card
 ├─ Month
 ├─ CardType
 ├─ IsGwang
 ├─ IsBigwang
 ├─ IsTti
 ├─ IsYeolKkeut
 ├─ IsPi
 ├─ IsDoublePi
 ├─ IsBonus
 └─ ScoreType
```

국열끗처럼 게임에서 특수하게 취급하는 카드는 별도의 속성을 갖도록 한다.

---

# 19. 공통 카드 플레이 규칙

일반적인 고스톱 규칙을 기반으로 한다.

플레이어의 턴:

```text
1. 손패에서 카드 1장 선택
2. 필드에 카드 배치
3. 같은 월의 카드가 있는지 확인
4. 매칭 처리
5. 뒷패 1장 공개
6. 뒷패 매칭 처리
7. 최종 카드 획득 결정
8. 카드들을 Cap / Field로 이동
9. 족보 및 점수 갱신
10. 승리 조건 확인
11. 다음 플레이어 턴
```

단, **현재 프로젝트에서 정의한 뻑 판정 타이밍을 우선한다.**

특히 손패를 필드에 냈을 때 매칭되는 카드가 있어도 뒷패를 확인하기 전에 최종 결과를 확정해서는 안 된다.

---

# 20. 카드 애니메이션

패 배분부터 실제 플레이까지 카드 이동을 명확하게 보여준다.

특히:

```text
Hand
 ↓
Field
 ↓
Match
 ↓
Flip Card
 ↓
Final Resolve
 ↓
Cap / Field
```

의 과정이 플레이어에게 명확하게 보여야 한다.

매칭되는 카드가 있는 경우에도 손패 카드가 필드에 도착하기 전에 작아지거나 Cap Scale로 변경되어서는 안 된다.

---

# 21. 뻑

일반적인 고스톱의 뻑 규칙을 적용한다.

단, 기존 프로젝트의 구현을 먼저 분석하고 현재 시스템에서 이미 확정한 판정 타이밍을 유지한다.

특히:

```text
손패 카드 제출
↓
필드 처리
↓
뒷패 공개
↓
최종 결과 판정
```

순서를 유지한다.

뻑 여부가 뒷패를 공개하기 전에 플레이어에게 노출되어서는 안 된다.

---

# 22. 특수 규칙

일반적인 고스톱 규칙을 기본으로 다음 특수 규칙을 지원한다.

* 뻑
* 첫뻑
* 연뻑
* 따닥
* 쪽
* 싹쓸이
* 폭탄
* 흔들기
* 총통
* 고도리
* 홍단
* 초단
* 청단
* 피
* 광
* 피박
* 광박
* 멍박
* 고박
* 나가리

단, **점수와 효과가 서비스마다 다른 항목은 기존 프로젝트의 확정 규칙을 우선한다.**

이때 기존 코드에서 발견한 뻑/첫뻑/연뻑/따닥/쪽/폭탄/흔들기/총통 등 각 규칙의 실제 수치(배율, 피 이동 개수 등)를 **표로 정리하여 먼저 보고한다.** 코드에서 값을 확인할 수 없거나 여러 곳에서 다르게 정의되어 있어 애매한 경우, 임의로 값을 선택해 구현하지 않고 **구현을 중단한 뒤 확인을 요청한다.** (52번 추가 테스트 시나리오 참고)

---

# 23. 싹쓸이

싹쓸이는 점수를 추가하는 족보로 처리하지 않는다.

현재 게임의 확정 규칙:

**싹쓸이가 발생하면 다른 플레이어들의 피를 한 장씩 가져온다.**

```text
싹쓸이 발동
↓
상대 플레이어 A 피 1장 획득
↓
상대 플레이어 B 피 1장 획득
↓
끝
```

싹쓸이 자체로 점수를 추가하지 않는다.

따라서 ScoreDetail에서 싹쓸이를 점수 +1 등으로 처리하고 있다면 수정한다.

---

# 24. 국열끗

국열끗은 게임의 기존 규칙에 따라 끗 또는 쌍피로 선택할 수 있도록 한다.

중요:

**모든 카드가 최종적으로 Cap에 들어간 이후 선택 Popup을 표시한다.**

권장 흐름:

```text
손패 제출
↓
뒷패 공개
↓
최종 매칭
↓
뻑 여부 결정
↓
모든 카드 최종 위치 결정
↓
Cap 반영 완료
↓
국열끗 선택 Popup
↓
끗 / 쌍피 선택
↓
점수 갱신
```

---

# 25. 족보

기본 족보는 일반적인 고스톱을 따른다.

## 광

* 3광
* 4광
* 5광

비광 포함 여부에 따른 점수 차이는 일반적인 고스톱 규칙을 따른다.

## 띠

* 홍단
* 청단
* 초단

각 3장 완성 시 족보 적용.

## 열끗

열끗 개수에 따른 멍박 조건 등을 적용한다.

## 고도리

해당 고도리 구성 패 3장 완성 시 고도리 적용.

## 피

일반 피 및 쌍피를 합산하여 피 점수를 계산한다.

---

# 26. 비상 시스템

비상은 특정 족보가 완성되기 직전임을 알려주는 시스템이다.

대상:

* 고도리
* 홍단
* 초단
* 청단
* 3광

예:

```text
홍단 필요 패 = 3장

현재 Player A Cap
홍단 패 2장 보유

→ 홍단 비상 발동
```

다른 실제 플레이어들에게 비상 Effect를 보여준다.

홍단 3장이 완성되면 비상 상태를 종료한다.

---

# 27. 비상 Effect

각각 별도의 Prefab으로 관리한다.

```text
GodoriEmergencyEffect
HongdanEmergencyEffect
ChodanEmergencyEffect
CheongdanEmergencyEffect
LightEmergencyEffect
```

가능하면 공통 Base를 사용한다.

```text
EmergencyEffectBase
 ├─ Godori
 ├─ Hongdan
 ├─ Chodan
 ├─ Cheongdan
 └─ Light
```

단, 현재 프로젝트 구조상 과도한 추상화가 필요하다면 단순한 Prefab 구조를 우선한다.

---

# 28. Go / Stop

## 3인 고스톱

일반적으로 3점 이상부터 Go/Stop 선택이 가능하도록 한다.

## 맞고

일반적으로 7점 이상부터 Go/Stop 선택이 가능하도록 한다.

단, 실제 서비스 적용 시 설정값으로 분리한다.

예:

```text
GameModeConfig

GoStopThreshold
```

```text
GoStopThreshold.Normal = 3
GoStopThreshold.Matgo = 7
```

---

# 29. 점수 시스템

점수 계산과 게임 배율을 분리한다.

```text
BaseScore
↓
GoCount
↓
SpecialMultiplier
↓
PenaltyMultiplier
↓
FinalMultiplier
↓
SettlementAmount
```

예:

```text
족보 점수 = 7
고 횟수 = 2
기본 배율 = ×2
피박 = ×2
광박 = ×2
```

최종 배율을 계산한 후 각 상대 플레이어의 정산 금액을 계산한다.

모든 정산 금액은 정수 단위로만 계산한다. 소수점은 발생할 수 없다. 상세 규칙은 49.3을 참조한다.

---

# 30. 4인 게임의 정산 대상

4인 게임에서 광팔이/죽기로 빠진 플레이어는 본 게임 정산 대상이 아니다.

예:

```text
A = 선
B = 참가
C = 참가
D = 광팔이
```

실제 게임:

```text
A vs B vs C
```

D는 정산 대상에서 제외한다.

---

# 31. 광박

광팔이 플레이어는 광박 계산에 포함하지 않는다.

즉:

```text
A 승리
B = 광 없음
C = 광 있음
D = 광팔이 OUT
```

이라면 광박 여부는 **B/C만을 대상으로 계산**한다.

D는 게임에 존재하지 않는 것으로 처리한다.

---

# 32. 피박 / 멍박 / 고박

일반적인 3인 고스톱 규칙을 기본으로 한다.

단, 실제 계산 시 반드시:

**Active Player만 대상으로 한다.**

Dead / LightSeller는 계산에서 제외한다.

---

# 33. 결과 화면 요구사항

결과 화면에서는 단순히 승자만 표시하지 않는다.

다음 정보를 표시한다.

### 게임 결과

* 승자
* 각 플레이어 점수
* Go 횟수
* 최종 배율
* 적용된 족보
* 피박
* 광박
* 멍박
* 고박
* 기타 배율

### 머니

각 플레이어별:

```text
게임 시작 머니
게임 정산
게임 종료 머니
```

### 광팔이

광팔이 플레이어가 있다면:

```text
광팔이 플레이어
광팔이 대상 패 수
광팔이 금액
```

을 별도로 표시한다.

예:

```text
D
광팔이

광 2
쌍피 2
국열끗 1
보너스패 1

총 6장 (기준 단가 600원 × 2명)
광팔이 수익 +1,200
```

---

# 34. 각 플레이어 Cap 확인

결과 화면에서 게임 종료 시점의 각 Active Player Cap을 확인할 수 있어야 한다.

각 Cap에 어떤 패가 있는지 실제 카드 이미지로 표시한다.

광팔이/죽기 플레이어는 별도의 상태로 표시할 수 있다.

---

# 35. 그래픽 오브젝트 생성 규칙

게임오브젝트를 가능한 한 코드로 직접 생성하지 않는다.
유니티 씬내에 오브젝트들로 생성하고 온오프하여 사용하고 코드에서는 최대한 serialize Field를 활용하여 참조하여 구성한다.
공통적으로 사용되는 부분 예를들어 카드, 플레이어 상태창, 이펙트 등은 프리펩으로 분리하고 재사용가능하게한다.
팝업등도 프리펩으로 관리한다.


---

# 36. Network Room과 GameMode

네트워크 방에서 참가자가 늘어나면 게임 모드를 결정한다.

```text
Room Player Count

2
→ MATGO

3
→ GOSTOP

4
→ GOSTOP_4P
```

중요:

4명이라고 해서 `4인 게임 엔진`이 별도로 존재하는 것이 아니다.

```text
GOSTOP_4P
↓
Participation Phase
↓
Active Players = 3
↓
GOSTOP
```

으로 연결한다.

---

# 41. GameMode Enum 예시

현재 프로젝트의 네이밍 규칙을 우선하되 다음과 같은 개념을 사용한다.

```text
GameMode
{
    Matgo,
    GoStop,
    GoStopFourPlayer
}
```

그리고:

```text
GoStopFourPlayer
```

에서는 PreGame Phase를 추가한다.

```text
PreGamePhase
{
    DetermineParticipation,
    DeadPlayer,
    LightSelling,
    Complete
}
```

---

# 42. Game State

게임 전체 상태는 가능한 한 명확하게 분리한다.

```text
Room
 ↓
PlayerReady
 ↓
DetermineGameMode
 ↓
DetermineFirstPlayer
 ↓
DealCards
 ↓
ParticipationPhase   // 4인만
 ↓
GameStart
 ↓
Turn
 ↓
CardPlay
 ↓
FlipCard
 ↓
Resolve
 ↓
ScoreCheck
 ↓
GoOrStop
 ↓
GameEnd
 ↓
Settlement
 ↓
NextRound
```

---

# 43. Network에서 주의할 점

게임 결과와 참가자 결정은 반드시 서버 권한 또는 방장/권한 있는 네트워크 주체에서 최종 결정한다.

특히 다음 값은 클라이언트가 임의로 결정하면 안 된다.

* 선
* 참가/불참 결과
* 광팔이 금액
* 카드 배분
* 카드 랜덤 순서
* 카드 매칭 결과
* 뻑
* 족보
* 점수
* 배율
* 최종 정산
* 나가리

클라이언트는 서버가 결정한 결과를 받아 UI/애니메이션으로 표현하는 구조를 우선한다.

---

# 44. 랜덤 카드 처리

카드 셔플은 네트워크 게임에서 동일한 결과를 재현할 수 있도록 설계한다.

가능하면 서버에서:

```text
Shuffle
↓
Deal
↓
각 Player에 결과 전달
```

한다.

클라이언트가 독립적으로 셔플하여 서로 다른 카드 상태가 발생하지 않도록 한다.

---

# 45. 개발 시 우선순위

1. Network Room Player Count 확인
2. GameMode 결정
3. 2인 → 맞고
4. 3인 → 일반 고스톱
5. 4인 → 참가 여부 결정
6. Dead / LightSeller 처리
7. Active Player 3명 확정
8. 패 분배
9. 공통 고스톱 진행
10. 점수 계산
11. Go/Stop
12. 정산
13. 나가리
14. 다음 판
15. UI
16. 애니메이션
17. 비상 시스템

---

# 46. 테스트 시나리오

## 2인

```text
Room = 2
→ Matgo
→ A 10장
→ B 10장
→ Field 8장
→ 정상 맞고 진행
```

## 3인

```text
Room = 3
→ GoStop
→ 각 7장
→ Field 6장
→ 정상 고스톱 진행
```

## 4인 / 2번 불참

```text
A = 선
B = 불참
C = 참가
D = 참가

→ A/C/D 게임
→ B Dead
→ 광팔이 없음
```

## 4인 / 3번 불참

```text
A = 선
B = 참가
C = 불참
D = 참가

→ A/B/D 게임
→ C Dead
→ 광팔이 없음
```

## 4인 / 모두 참가 의사

```text
A = 선
B = 참가
C = 참가
D = 강제 광팔이

→ A/B/C 게임
→ D LightSeller
→ 광팔이 금액 즉시 지급
```

## 광팔이 계산

```text
광 2
비광 1
쌍피 2
국열끗 1
보너스패 2

총 8장

기준 단가 = 8 × 1점 가격
→ 2번·3번 플레이어가 각자 이 기준 단가 전액을 지급(49.1)
```

정확한 금액이 지급되는지 확인한다.

## 나가리

```text
A = 기존 선
게임 나가리
↓
A가 그대로 선
↓
참가 여부 생략
↓
광팔이 생략
↓
게임비 ×2
↓
즉시 게임 시작
```

## 정상 승리

```text
A 승리
↓
A가 다음 판 선
↓
4인 참가
↓
다시 참가 여부 결정
```

---

# 47. 작업 전 반드시 확인할 것

현재 프로젝트의 기존 고스톱/맞고 코드와 데이터 구조를 먼저 분석한다.

특히 다음을 확인한다.

* 현재 GameManager
* Network Room 구조
* Player 상태
* Card 데이터
* Deck
* Card Dealer
* Turn Manager
* Score Manager
* Settlement
* Cap
* UI
* Popup
* Animation
* Prefab
* 현재 점수 계산
* 현재 싹쓸이 구현
* 현재 국열끗 구현
* 현재 뻑 판정
* 현재 나가리 구현

기존 기능이 있다면 새로 만들기보다 최대한 확장한다.

**작업 원칙 (값 확인 및 보고)**: 이 문서 전반에서 다루는 모든 수치/설정값(특수 규칙 배율, 정산 관련 값, 타임아웃 시간 등)에 대해 기존 코드에서 발견한 값은 반드시 표로 정리하여 먼저 보고한다. 기존 코드와 본 기획서 내용이 다르거나, 코드에서 확인이 불가능하거나, 여러 값이 혼재되어 있어 애매한 경우 임의로 결정하여 구현을 진행하지 않고 **즉시 구현을 중단한 뒤 확인을 요청한다.**

---

# 48. 리스크 관리

다음과 같은 상황에서는 무리해서 전체 구조를 변경하지 않는다.

* 기존 네트워크 구조를 대규모로 변경해야 하는 경우
* 카드 랜덤/셔플 구조를 변경해야 하는 경우
* 기존 점수 계산을 전면 재작성해야 하는 경우
* 다른 게임 UI에 영향을 주는 경우
* 저장 데이터와 호환되지 않을 가능성이 있는 경우

이런 경우:

1. 문제
2. 원인
3. 수정하지 않은 이유
4. 권장 해결 방법

을 최종 보고서에 작성한다.

---

# 49. 정산 / 머니 세부 규칙

## 49.1 광팔이 금액 지급 (분담이 아니라 각자 전액 — 2026-08-23 확정)

* 선 플레이어는 광팔이 금액을 지급하지 않는다.
* 참가가 확정된 나머지 두 플레이어(2번, 3번)는 **분담하지 않고 각자 독립적으로** "판매 대상 장수 × 1점 가격" 전액을 광팔이 플레이어(4번)에게 지급한다. 결과적으로 4번은 장당 1점 가격의 **2배**를 수령한다.

예:

```text
판매 대상 장수 = 6장, 1점 가격 = 100원
기준 단가 = 6 × 100원 = 600원

→ 2번 플레이어 600원 지급
→ 3번 플레이어 600원 지급
→ 4번(광팔이) 플레이어 총 1,200원 수령
```

* 각자 정수 금액(장수 × 1점 가격)을 그대로 지급하므로 분담에 따른 반올림/나머지 처리 이슈 자체가 발생하지 않는다.
* 기존 프로젝트에 이미 구현·검증돼 있던 규칙을 그대로 채택한 것이다(47번 원칙) — 이 문서의 최초 초안이었던 "총액 균등분담(장당 100원 실수령)" 방식은 폐기한다.

## 49.2 1점 가격(단가) 설정

* 방 생성 시 **호스트가 1점당 금액을 설정**한다.
* 설정 범위: 최소 **10원** ~ 최대 **1,000,000원**.
* 호스트가 보유한 머니가 설정하려는 1점 금액 미만이면 해당 금액으로 설정할 수 없다. 즉, 설정 가능한 1점 가격의 최댓값은 호스트의 현재 보유 머니 이하로 제한된다.
* 게임 진행 중에는 1점 가격을 변경하지 않는다. (나가리로 인한 게임비 2배 배율은 16번 규칙에 따라 별도로 적용)

## 49.3 정수 처리 원칙

* 정산, 광팔이 지급, 배율 적용 등 **모든 금액 계산은 소수점 없이 정수 단위로만 처리한다.**
* 49.1(광팔이)은 "장수 × 1점 가격"이라는 정수×정수 곱셈만 있고 나눗셈(분담)이 없어 소수 발생 여지가 없다. 다른 계산에서 나눗셈으로 소수가 발생할 수 있는 경우(예: 배율 적용 후 정산액 배분 등)에는 반드시 정수화(반올림/올림 등) 규칙을 적용한다.
* 기존 코드에서 새롭게 발견되는 정수화 필요 계산이 있다면 47번 원칙에 따라 표로 정리해 보고한다.

## 49.4 머니 소진 처리 및 자동 모드 전환

* 정산 결과 특정 플레이어의 머니가 0 이하가 되면, 해당 플레이어는 **보유한 금액 한도 내에서만 지급**하고 게임에서 퇴장 처리한다.
* 퇴장 후 남은 Active Player 수에 따라 게임 모드를 **자동으로 전환**한다.

```text
4인 게임 중 1명 머니 소진
→ 퇴장
→ 남은 3명으로 3인 고스톱 자동 전환

3인 게임 중 1명 머니 소진
→ 퇴장
→ 남은 2명으로 맞고 자동 전환

2인 게임 중 1명 머니 소진
→ 퇴장
→ 방 폭파(해체)
→ 전원 타이틀 화면으로 이동
```

* 자동 전환된 이후에는 1~17번(게임 모드 결정 / 선 결정 규칙)을 그대로 따른다.

> **2026-08-23 확정**: 기존 프로젝트에는 "머니가 0원 이하가 되면 5만원을 리필해서 그대로 계속 진행한다"는, 과거 세션에서 사용자가 직접 확정했던 규칙이 있었다(파산으로 세션이 끝나버리는 문제를 리필 방식으로 해결했던 것). 이번 통합 작업에서는 그 리필 규칙을 **폐기**하고 위 49.4의 퇴장+게임모드 자동 다운그레이드 방식으로 **교체**하기로 확정했다.

## 49.5 미확정 사항 (구현 전 확인 필요)

* 지급해야 할 금액보다 실제 보유 머니가 적어 **부족분(차액)** 이 발생하는 경우, 그 차액을 하우스가 흡수하는지, 다른 플레이어가 나눠 부담하는지, 아니면 단순히 미지급 처리하는지는 정책이 정해지지 않았다. 구현 전 확인이 필요하다.
* ~~49.1의 "나머지 1원은 2번 플레이어가 부담" 규칙~~ — 2026-08-23, 광팔이 지급 방식이 "분담"에서 "각자 전액 지급"으로 확정되면서 반올림/나머지 처리 이슈 자체가 사라져 이 항목은 해소됐다.

---

# 50. 네트워크 엣지 케이스

아래는 상식적인 수준에서 정리한 기본 처리 방향이다. **기존 프로젝트에 이미 구현된 타임아웃/재접속/방장 이탈 로직이 있다면 그것을 우선한다.** 없는 경우에 한해 아래 기준을 신규로 적용한다.

## 50.1 입력 대기 타임아웃

참가 여부, Go/Stop 선택, 국열끗 끗/쌍피 선택 등 플레이어 입력이 필요한 모든 시점에 제한 시간을 두고, 시간 내 응답이 없으면 아래 기본값으로 자동 처리한다.

```text
참가 여부 무응답      → 불참(죽기) 처리
Go/Stop 무응답        → Stop 처리
국열끗 선택 무응답     → 쌍피 처리 (기존 프로젝트 기본값이 있다면 그것을 우선)
카드 선택(턴) 무응답   → 가능한 카드 중 첫 번째를 자동 선택
```

정확한 제한 시간(초 단위)은 기존 프로젝트의 턴/타이머 로직을 조사해 확정하고, 없다면 별도로 확인을 요청한다.

## 50.2 게임 중 접속 종료 / 재접속

* 게임 중 접속이 끊긴 플레이어는 즉시 퇴장 처리하지 않고, 일정 유예 시간 동안 재접속을 허용한다.
* 유예 시간 동안 해당 플레이어의 턴/선택은 50.1의 타임아웃 기본값으로 자동 처리되어 다른 플레이어의 게임 진행이 멈추지 않도록 한다.
* 유예 시간 내 재접속하면 손패 / 필드 / 점수 / Go 횟수 등 게임 상태를 그대로 복원하여 이어서 진행한다.
* 유예 시간을 초과하면 해당 플레이어를 퇴장 처리하고, 49.4의 자동 모드 전환 규칙을 적용한다. (단, 이 경우는 머니 소진이 아닌 이탈이므로, 이탈 시점까지의 정산 처리 방식은 기존 프로젝트에 재접속/이탈 처리 로직이 있다면 그것을 우선하고, 없다면 확인을 요청한다.)

## 50.3 방장(호스트) 이탈

* 게임 시작 전 방장이 이탈하면 방을 해체하거나, 다음 순번 플레이어에게 방장 권한을 위임한다. (기존 로비 로직 우선)
* 게임 진행 중에는 방장 권한과 게임 판정 권한을 분리한다. 43번 규칙대로 게임 결과/판정은 서버 권한이므로, 방장이 이탈해도 진행 중인 게임 로직 자체는 영향을 받지 않아야 한다. 방장 권한만 다른 플레이어에게 위임하거나 서버가 유지한다.

## 50.4 전원 이탈

활성 플레이어가 모두 접속을 종료하면 해당 방은 즉시 정리(해체)한다.

---

# 51. 완료 기준 (Definition of Done)

다음을 모두 만족할 때 이번 작업을 완료로 간주한다.

1. 46번 테스트 시나리오 전체 통과
2. 52번 추가 테스트 시나리오(특수 규칙별) 전체 통과
3. 특수 규칙(뻑 / 첫뻑 / 연뻑 / 따닥 / 쪽 / 폭탄 / 흔들기 / 총통 / 국열끗 / 싹쓸이 등) 각각에 대한 유닛 테스트 작성 및 통과
4. 47번 작업 원칙에 따라 조사한 기존 코드 값(표)을 최종 보고서에 포함
5. 48번 리스크 관리 기준에 해당하는 항목이 발생했다면, 문제 / 원인 / 수정하지 않은 이유 / 권장 해결 방법을 보고서에 기재

---

# 52. 추가 테스트 시나리오 (특수 규칙 / 정산 / 네트워크)

## 뻑

```text
Player A가 손패를 냈을 때 필드에 같은 월 카드가 2장 존재
→ 뻑 처리
→ 해당 카드는 필드에 그대로 유지
→ 21번 순서(손패 제출 → 필드 처리 → 뒷패 공개 → 최종 판정)대로 진행되는지 확인
→ 뒷패 공개 전에는 뻑 여부가 노출되지 않는지 확인
```

## 첫뻑

```text
게임 시작 후 처음 발생하는 뻑
→ 첫뻑 페널티/효과가 기존 프로젝트 규칙대로 적용되는지 확인
```

## 연뻑

```text
동일 플레이어에게 뻑이 연속 2회 이상 발생
→ 연뻑 효과가 기존 프로젝트 규칙대로 누적/적용되는지 확인
```

## 따닥

```text
손패 카드와 뒷패 카드가 필드의 동일 월 카드 2장과 각각 매칭
→ 따닥 판정
→ 상대방으로부터 피를 가져오는 효과가 기존 프로젝트 규칙대로 적용되는지 확인
```

## 쪽

```text
손패로 낸 카드가 필드의 카드 1장과 바로 매칭되고, 뒷패도 동일 월과 매칭
→ 쪽 판정 및 효과가 정상 적용되는지 확인
```

## 폭탄

```text
손패에 동일 월 카드 3장 보유
→ 폭탄 선언
→ 필드의 동일 월 카드까지 한 번에 획득되는지 확인
→ 배율이 정상 적용되는지 확인
```

## 흔들기

```text
손패에 동일 월 카드 3장 보유 상태에서 흔들기 선언
→ 이후 해당 월 카드를 낼 때 배율이 정상 적용되는지 확인
```

## 총통

```text
초기 패 분배 시 손패에 동일 월 카드 4장 모두 보유
→ 총통 판정
→ 기존 프로젝트 규칙에 따른 효과(즉시 승리 또는 추가 점수 등)가 적용되는지 확인
```

## 국열끗 선택

```text
국열끗 카드가 최종적으로 Cap에 포함됨
→ 24번 순서대로 모든 카드 위치가 확정된 이후에 선택 팝업이 노출되는지 확인
→ 끗 / 쌍피 선택에 따라 점수가 즉시 재계산되는지 확인
```

## 싹쓸이

```text
필드의 모든 카드를 획득하는 싹쓸이 발생
→ 점수가 가산되지 않는지 확인
→ 상대 플레이어 각각으로부터 피 1장씩 획득되는지 확인 (23번)
→ ScoreDetail에 싹쓸이가 점수로 잘못 반영되지 않는지 회귀 확인
```

## 광팔이 금액 지급 (각자 전액, 분담 아님)

```text
광 2 / 쌍피 2 / 국열끗 1 / 보너스패 1 = 총 6장
1점 가격 = 100원 → 기준 단가 600원

→ 2번 플레이어 600원 지급
→ 3번 플레이어 600원 지급
→ 4번(광팔이) 플레이어 잔고에 1,200원이 정확히 반영되는지 확인
```

## 1점 가격 설정 제한

```text
호스트 보유 머니 = 50,000원
호스트가 1점 가격을 100,000원으로 설정 시도
→ 설정 실패(보유 머니 미만) 확인

호스트가 1점 가격을 5원으로 설정 시도
→ 최소값(10원) 미만이므로 설정 실패 확인

호스트가 1점 가격을 2,000,000원으로 설정 시도
→ 최댓값(1,000,000원) 초과이므로 설정 실패 확인
```

## 머니 소진 자동 전환

```text
4인 게임 중 한 명 머니 0 이하
→ 보유 금액 한도 내 지급 후 퇴장
→ 남은 3명으로 3인 고스톱 자동 전환 확인

3인 게임 중 한 명 머니 0 이하
→ 퇴장 → 남은 2명으로 맞고 자동 전환 확인

2인 게임 중 한 명 머니 0 이하
→ 퇴장 → 방 폭파 후 전원 타이틀 화면 이동 확인
```

## 타임아웃 처리

```text
참가 여부 응답 제한시간 초과 → 자동 불참(죽기) 처리 확인
Go/Stop 응답 제한시간 초과   → 자동 Stop 처리 확인
국열끗 선택 제한시간 초과    → 자동 쌍피 처리 확인
```

## 접속 종료 / 재접속

```text
게임 중 플레이어 접속 종료
→ 유예 시간 동안 자동 턴 처리로 게임 진행이 멈추지 않는지 확인

유예 시간 내 재접속
→ 손패 / 필드 / 점수 상태가 정상 복원되는지 확인

유예 시간 초과
→ 퇴장 처리 및 49.4 자동 모드 전환이 정상 동작하는지 확인
```

---

# 53. 최종 목표

이 시스템은 다음 세 가지 게임 경험을 하나의 네트워크 게임 안에서 자연스럽게 제공해야 한다.

```text
2명
↓
맞고

3명
↓
일반 고스톱

4명
↓
광팔이/죽기
↓
3명 확정
↓
일반 고스톱
```

그리고 개발 구조는:

```text
Network Room
      ↓
Player Count
      ↓
GameMode
      ↓
┌──────────────┬──────────────┬──────────────────┐
│              │              │                  │
2인            3인            4인                │
│              │              │                  │
맞고           고스톱          참가 판정          │
│              │              ↓                  │
10장           7장            3명 Active         │
8장 Field      6장 Field      ↓                  │
│              │              3인 고스톱          │
└──────────────┴──────────────┴──────────────────┘
```

형태로 설계한다.

**2인 맞고와 3인 고스톱의 핵심 게임 로직은 최대한 공유하고, 4인 고스톱은 게임 시작 전 참가자 결정 단계만 추가하는 방향을 우선한다.**

UI는 GameObject/Prefab 기반으로 구성하여 향후 리소스 교체와 디자인 수정이 쉽게 만들어야 한다.

게임 규칙과 UI 표현을 분리하고, 네트워크 환경에서는 서버가 게임 상태와 결과를 결정하며 클라이언트는 이를 시각적으로 표현하도록 한다.

작업 완료 후 실제 구현한 내용, 변경 파일, GameMode 흐름, 네트워크 처리, 테스트 결과, 미완료 항목 및 리스크를 상세하게 보고한다.

---

# 54. 구현 진행 상황 (2026-08-23 기준 — 상태 보존용 스냅샷)

이 섹션은 매 작업 세션이 끝날 때마다 갱신한다. "지금 뭐가 되어 있고 뭐가
안 되어 있는지"를 이 파일 하나만 보고 파악할 수 있게 하는 게 목적이다 —
상세 근거·코드 위치·버그 수정 이력은 `CLAUDE.md`의 "고스톱 — design.md
통합 작업 v1" 이하 섹션에 있다.

## 완료

| 항목 | 섹션 | 비고 |
|---|---|---|
| §47 원칙에 따른 기존 코드 특수규칙 수치 감사 | §22/§47 | CLAUDE.md에 표로 정리, 충돌 2건(광팔이 금액·싹쓸이 점수) 확인 후 사용자 확정 |
| 광팔이 금액 — 기존 구현(장당 200원 실수령) 확정 | §8/§9/§33/§49.1/§49.5 | design.md 쪽을 정정(초안은 폐기) |
| 싹쓸이 점수 미가산으로 변경 | §23 | `GoStopRules.CalcScore` |
| 4인 참가결정 순서 정정(2번 불참 시 3번 자동참가) | §5.2/§5.3 | `GoStop3PGame.cs` |
| 2인 맞고 최초 선 결정(카드 뽑기) 신규 구현 | §14 | `GoStopGame.cs`/`.UI.cs` — 4인판은 이미 있었음 |
| 승자가 다음 판 선, 나가리 시 선 유지 | §15/§16 | 2인 신규, 4인 기존 |
| 머니 0원 이하 — 리필 폐기, 퇴장+세션종료로 교체 | §49.4 | 2인/4인 둘 다. **자동 다운그레이드(즉시 이어서 진행)는 아직** — 아래 "다음 단계" 참고 |
| 3광 비상 추가 | §26/§27 | `EffectLight.prefab` 신규, 전용 판정 함수(`CheckGwangEmergency`) |
| 첫뻑/연뻑/첫따닥 전용 이펙트 | (기획서에 명시적 항목 없음, 사용자 추가 요청) | 조사 결과 첫뻑/연뻑은 평범한 "뻑"과 색이 안 갈려 있었고 **첫따닥은 이펙트가 아예 안 뜨는 버그**였다 — 셋 다 초록(금전 신호)으로 분리, `MoneyEventColor` |
| 뻑 판정 타이밍, 국열끗 팝업 타이밍, 카드 애니메이션 순서 | §19~21/§24 | 이미 구현·검증 완료(이전 세션) |

## 다음 단계 (사용자가 지시한 순서)

1. **1점 가격 호스트 설정 UI** (§49.2) — 지금은 `WON_PER_POINT` 고정 상수(100원). 착수 예정.
2. **`GoStopScene`(2인)과 `GoStop3PScene`(3·4인)을 한 씬으로 통합** — 게임모드
   자동결정·자동 다운그레이드(§36/§41/§42/§45/§49.4)를 하려면 씬 전환 없이
   한 씬 안에서 맞고↔고스톱 3인↔4인이 전환돼야 한다는 게 사용자 판단 —
   **이 작업을 게임모드 자동결정/다운그레이드보다 먼저 한다.**
3. **게임모드 자동결정** (§36/§41/§42/§45) — 방 인원수로 자동 전환. 2번(씬
   통합) 완료 후 착수.
4. **게임모드 자동 다운그레이드** (§49.4) — 4인→3인→2인, 위 §48 리스크
   관리 항목에서 "좌석 재번호매김 필요, 3인→2인은 다른 씬" 이라 미뤘던
   것. 씬 통합이 이 문제를 구조적으로 해결해줄 것으로 예상 — 2번 완료 후
   착수.

## 아직 미착수 (§48 리스크 관리 — 우선순위 밖으로 밀림)

- §50 네트워크 엣지케이스(입력 타임아웃, 재접속 유예, 방장 이탈 처리)
- §35 UI 전면 프리팹화(팝업·이펙트는 이미 프리팹, 카드/필드/손패 등 매판
  달라지는 요소는 의도적으로 코드 생성 유지 — 별도 세션에서 확정한 경계)
