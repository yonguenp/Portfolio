# 삼국지 로그라이크 디펜스

## Game Design Document / Claude Code 개발 지침서

---

# 1. 프로젝트 개요

## 1.1 게임 장르

* 삼국지 IP 기반 로그라이크 디펜스
* PvE 자동 전투
* 캐릭터 수집 및 성장
* 아이템 파밍 및 조합
* 캐릭터 조합 시너지
* 웨이브/라운드 생존형 게임

## 1.2 핵심 게임 플레이

플레이어는 삼국지의 실제 등장인물을 수집하고 성장시킨다.

게임 시작 전 최대 3명의 캐릭터를 선택하여 전투에 참여시킨다.

전투에서는 플레이어가 직접 공격하거나 이동시키지 않는다.

각 캐릭터는 자신의 AI Controller에 따라 자동으로 이동하고 적을 공격한다.

라운드가 시작되기 전 플레이어는 캐릭터의 위치를 전략적으로 재배치할 수 있다.

전투가 진행되면서 몬스터를 처치하고:

1. 골드를 획득
2. 아이템을 획득
3. 아이템을 장착
4. 동일 아이템 3개를 조합하여 상위 등급 아이템 제작
5. 캐릭터와 아이템의 조합을 최적화
6. 최대한 많은 라운드를 생존

하는 것이 핵심 목표다.

---

# 2. 핵심 게임 루프

전체적인 게임 루프는 다음과 같다.

```text
[로비]
   ↓
캐릭터 수집 / 성장
   ↓
최대 3명 캐릭터 선택
   ↓
[전투 시작]
   ↓
맵에 캐릭터 및 몬스터 랜덤 배치
   ↓
플레이어가 캐릭터 위치 재배치
   ↓
[라운드 시작]
   ↓
자동 전투
   ↓
몬스터 20마리 처치
   ↓
골드 / 아이템 획득
   ↓
라운드 종료
   ↓
아이템 정리 / 장착 / 조합
   ↓
다음 라운드
   ↓
반복
   ↓
모든 캐릭터 사망
   ↓
게임 종료
   ↓
획득 보상 정산
   ↓
[로비]
```

---

# 3. 게임의 핵심 목표

플레이어의 최종 목표는 단순히 몬스터를 빠르게 처치하는 것이 아니다.

**보유 캐릭터를 최대한 죽지 않게 유지하면서 아이템을 획득하고 성장하여 최대한 높은 라운드까지 도달하는 것**이다.

따라서 다음 요소가 중요하다.

* 캐릭터 조합
* 캐릭터 배치
* 시너지 조합
* 아이템 장착
* 아이템 조합
* 공격력
* 방어력
* HP
* 공격속도
* 이동속도
* 공격범위
* 무기 특성
* 적의 능력치
* 전투 중 타겟 선정

---

# 4. 게임 시작 / 신규 유저

게임 로비에 최초 진입한 유저에게 기본 골드 100을 지급한다.

```text
초기 골드 = 100
```

별도의 튜토리얼 시스템은 1차 프로토타입에서는 구현하지 않는다.

---

# 5. 로비 시스템

## 5.1 로비 주요 기능

로비에서는 다음 기능을 제공한다.

* 골드 확인
* 캐릭터 뽑기
* 보유 캐릭터 확인
* 캐릭터 능력치 강화
* 전투 시작
* 캐릭터 선택

---

# 6. 캐릭터 시스템

## 6.1 캐릭터 등급

캐릭터는 총 6개 등급으로 구분한다.

```text
1성
2성
3성
4성
5성
6성
```

삼국지에 실제 등장하는 인물을 기반으로 캐릭터를 구성한다.

예:

* 유비
* 관우
* 장비
* 조조
* 하후돈
* 조운
* 제갈량
* 여포
* 손권
* 손책
* 주유
* 황충
* 마초
* etc.

1차 프로토타입에서는 캐릭터 수를 제한하여 구현하고 이후 확장할 수 있도록 데이터 기반으로 제작한다.

---

# 7. 캐릭터 기본 능력치

모든 캐릭터는 기본적으로 다음 능력치를 가진다.

```text
HP
공격력
방어력
공격속도
이동속도
공격범위
```

추후 확장을 고려하여 Stat 시스템은 데이터 기반으로 구현한다.

---

# 8. 캐릭터 기본 공격

모든 캐릭터는 기본 공격을 가지고 있다.

기본 공격은 근접 공격으로 시작한다.

캐릭터가 적에게 접근한 후 공격범위 안에 적이 존재하면 공격속도에 따라 기본 공격을 실행한다.

기본 공격 피해량:

```text
Damage = Max(Attacker.Attack - Target.Defense, 1)
```

단, 게임 디자인상 피해량은 최소 1보다 크게 설정한다.

프로토타입에서는 최소 피해량을 별도 상수로 관리한다.

예:

```text
MIN_DAMAGE = 1
```

향후 밸런싱을 위해 변경 가능하도록 한다.

---

# 9. 공격속도 시스템

공격속도는 매우 중요한 스탯이다.

범위:

```text
0.01 ~ 100.00
```

기본값:

```text
1.00
```

공격속도는 **초당 공격 횟수(Attack Per Second)** 로 정의한다.

## 공격속도 예시

```text
공격속도 1.00
→ 1초마다 1회 공격
→ 60프레임마다 1회 공격

공격속도 2.00
→ 0.5초마다 1회 공격
→ 30프레임마다 1회 공격

공격속도 100.00
→ 0.01초마다 공격
→ 60 FPS 기준 1프레임 단위로 공격

공격속도 0.01
→ 100초마다 1회 공격
→ 6000프레임마다 1회 공격
```

실제 프레임 계산은 다음 방식으로 처리한다.

```text
attackInterval = 1 / attackSpeed
```

예:

```text
attackSpeed = 1
attackInterval = 1초

attackSpeed = 2
attackInterval = 0.5초

attackSpeed = 100
attackInterval = 0.01초

attackSpeed = 0.01
attackInterval = 100초
```

Unity에서는 프레임 수를 직접 하드코딩하기보다는 시간 기반으로 계산하되, 게임 시뮬레이션은 60 FPS 기준으로 동작하도록 한다.

---

# 10. 이동속도

추가 스탯으로 이동속도를 사용한다.

```text
MoveSpeed
```

캐릭터와 몬스터 모두 이동속도를 가진다.

이동속도가 높을수록 목표에게 빠르게 접근한다.

프로토타입에서는 캐릭터/몬스터마다 데이터로 설정할 수 있도록 한다.

---

# 11. 공격범위

추가 스탯으로 공격범위를 사용한다.

```text
AttackRange
```

기본 공격뿐 아니라 무기에도 별도의 공격범위를 설정할 수 있도록 한다.

캐릭터 기본 공격범위:

```text
Character.AttackRange
```

무기 공격범위:

```text
Weapon.AttackRange
```

무기 장착 시 무기의 공격범위를 사용하는 구조를 기본으로 한다.

---

# 12. 캐릭터 아이템 슬롯

캐릭터의 등급에 따라 아이템 슬롯 개수가 결정된다.

```text
1성 = 1슬롯
2성 = 2슬롯
3성 = 3슬롯
4성 = 4슬롯
5성 = 5슬롯
6성 = 6슬롯
```

즉,

```text
ItemSlotCount = CharacterStar
```

---

# 13. 캐릭터 강화

로비에서 보유 캐릭터의 기본 능력치를 강화할 수 있다.

강화 가능한 능력치:

* HP
* 공격력
* 방어력
* 공격속도
* 이동속도
* 공격범위

각 능력치는 강화할 때마다 +1 증가하는 방식으로 시작한다.

예:

```text
공격력 100
→ 강화
→ 101
→ 강화
→ 102
```

강화 비용은 지수 형태로 증가한다.

예시 공식:

```text
Cost = BaseCost × GrowthRate ^ UpgradeLevel
```

실제 BaseCost와 GrowthRate는 밸런스 데이터에서 조정 가능하도록 한다.

예:

```text
BaseCost = 10
GrowthRate = 1.2
```

실제 게임 밸런스에 따라 추후 조정한다.

---

# 14. 캐릭터 뽑기

로비에서 골드 30을 사용하여 캐릭터 뽑기를 할 수 있다.

```text
1회 뽑기 = 30 Gold
```

뽑기 결과:

```text
1성 ~ 6성
```

각 등급별 확률은 데이터 테이블에서 설정한다.

예시:

```text
1성 : XX%
2성 : XX%
3성 : XX%
4성 : XX%
5성 : XX%
6성 : XX%
```

실제 확률은 초기 프로토타입에서 임시값을 사용하고 추후 밸런싱한다.

---

# 15. 시너지 시스템

전투에 참여한 캐릭터들의 조합에 따라 시너지 효과가 발생한다.

대표적인 예:

## 도원결의

조건:

```text
유비
관우
장비
```

3명 모두 전투 참여 시:

```text
도원결의 활성화
→ 최대 HP 증가
```

시너지는 캐릭터 조합 데이터를 기반으로 관리한다.

예:

```text
Synergy
- ID
- Name
- RequiredCharacters
- RequiredCount
- Effects
```

---

# 16. 시너지의 적용 범위

기본적으로 시너지는 전투에 참여한 캐릭터에게 적용한다.

예:

```text
유비 + 관우 + 장비

도원결의
→ 전투 참여 3명의 HP +20%
```

시너지 효과는 추후 다음 형태로 확장 가능하도록 한다.

* HP 증가
* 공격력 증가
* 방어력 증가
* 공격속도 증가
* 이동속도 증가
* 공격범위 증가
* 특정 무기 강화
* 특정 아이템 강화
* 특수 효과

---

# 17. 아이템 시스템

아이템은 몬스터가 드랍한다.

아이템은 캐릭터에게 장착할 수 있다.

아이템 종류는 크게 3가지다.

```text
무기
방어구
장식
```

---

# 18. 아이템 주인 캐릭터

모든 아이템에는 특정한 주인 캐릭터가 지정될 수 있다.

예:

```text
청룡언월도
OwnerCharacter = 관우
```

해당 아이템을 주인 캐릭터가 장착하면 추가 보너스를 받는다.

```text
Owner Bonus = 아이템 능력의 20%
```

예:

```text
무기 공격력 = +100

관우가 장착
→ 추가 +20

최종 = +120
```

다른 캐릭터가 장착하면 기본 능력만 적용한다.

```text
관우 이외의 캐릭터
→ +100
```

주인 캐릭터 시스템은 모든 아이템에 선택적으로 적용할 수 있도록 설계한다.

---

# 19. 무기

무기는 캐릭터에게 추가 공격 능력을 제공한다.

무기는 기본 공격과 별도의 공격 쿨타임을 가진다.

```text
Weapon Cooldown
```

쿨타임이 완료되고 조건을 만족하면 자동으로 발동한다.

---

# 20. 무기 종류

무기는 크게 두 가지 공격 타입을 가진다.

```text
근접
원거리
```

---

# 21. 근접 무기

근접 무기는 가장 가까운 적을 대상으로 한다.

```text
Target = 가장 가까운 적
```

예:

```text
캐릭터
   ↓
적 A
적 B
적 C
```

거리 기준으로 가장 가까운 적을 선택한다.

---

# 22. 원거리 무기

원거리 무기는 지정된 공격범위 내의 적을 탐색한다.

기본 타겟 규칙:

```text
공격범위 안에 존재하는 가장 먼 적
```

즉, 사정거리 안에서 가장 먼 적을 타겟으로 한다.

추후 무기별 Targeting Rule을 확장할 수 있도록 한다.

예:

```text
Nearest
Farthest
LowestHP
HighestHP
Random
```

---

# 23. 무기 공격 방식

무기는 다양한 공격 방식을 가질 수 있다.

1차 구현:

* 단일 타격
* 발사체
* 스플래시

추후 확장:

* 관통
* 연쇄
* 범위 공격
* 지속 피해
* 넉백
* 다중 발사
* 특정 대상 우선 공격

---

# 24. 무기 스탯

무기는 다음 데이터를 가진다.

```text
Item ID
Item Name
Item Type
Owner Character
Damage
Cooldown
Attack Range
Target Type
Projectile Type
Splash Radius
Stat Scaling
```

---

# 25. 무기 능력치 비례 성장

일부 무기는 장착 캐릭터의 능력치에 비례하여 능력이 증가한다.

예:

```text
Weapon Damage
= BaseDamage + Character.Attack × Scaling
```

예:

```text
BaseDamage = 50
AttackScaling = 0.5

캐릭터 공격력 = 100

최종 무기 피해량
= 50 + (100 × 0.5)
= 100
```

Scaling 값은 아이템 데이터에서 설정한다.

---

# 26. 방어구

방어구는 캐릭터의 생존력을 증가시킨다.

가능한 효과:

```text
+HP
+방어력
```

예:

```text
철갑옷
HP +100
방어력 +20
```

추후:

* HP %
* 방어력 %
* 피해 감소
* 특정 공격 저항

등으로 확장할 수 있다.

---

# 27. 장식

장식은 공격 능력을 강화한다.

가능한 능력:

```text
+공격력
+공격속도
```

예:

```text
옥 장식
공격력 +20
공격속도 +0.2
```

---

# 28. 아이템 등급 및 조합

아이템은 동일한 아이템 3개를 모으면 상위 등급으로 조합할 수 있다.

```text
동일 아이템 ×3
        ↓
상위 등급 아이템 ×1
```

예:

```text
철검 ×3
↓
강철검 ×1
```

조합 규칙은 데이터 기반으로 만든다.

---

# 29. 아이템 조합 예시

```text
[철검]
[철검]
[철검]

        ↓ 조합

[강철검]
```

조합 시 기존 재료 3개는 제거된다.

상위 아이템의 능력치는 조합 규칙에 따라 결정한다.

---

# 30. 몬스터 시스템

매 라운드마다 몬스터 20마리가 등장한다.

```text
1 Round = 20 Monsters
```

몬스터는 라운드가 증가할수록 강해진다.

증가 대상:

* HP
* 공격력
* 방어력
* 이동속도
* 공격속도
* 공격범위

---

# 31. 몬스터 스케일링

몬스터 능력치는 라운드에 따라 증가한다.

기본 구조:

```text
Round 1
→ 기본 능력치

Round 2
→ 기본 능력치 × 성장률

Round 3
→ 기본 능력치 × 성장률²
```

예:

```text
HP = BaseHP × HPGrowth^(Round-1)

Attack = BaseAttack × AttackGrowth^(Round-1)

Defense = BaseDefense × DefenseGrowth^(Round-1)
```

실제 성장률은 밸런스 데이터에서 조정한다.

---

# 32. 몬스터 아이템

몬스터는 등장할 때 아이템을 장착하고 등장할 수 있다.

즉, 몬스터 역시 아이템 데이터를 가진다.

예:

```text
몬스터 A
HP 500
Attack 50
Defense 20

장착 아이템
철갑옷
녹슨 검
```

아이템 능력치가 몬스터에게 적용된다.

---

# 33. 몬스터 사망 보상

몬스터가 사망하면 다음 보상을 지급한다.

### 골드

몬스터마다 일정량의 골드를 확정적으로 지급한다.

```text
Monster Death
→ Gold + N
```

### 아이템

몬스터가 장착하고 있던 아이템 중 일부가 확률적으로 드랍된다.

```text
Monster Death
→ Item Drop Chance
```

아이템 드랍 여부는 각 아이템별 DropRate를 사용한다.

---

# 34. 라운드 시스템

게임은 라운드 단위로 진행된다.

```text
Round 1
Round 2
Round 3
...
```

각 라운드마다 몬스터 20마리가 존재한다.

모든 몬스터를 처치하면 라운드가 종료된다.

---

# 35. 라운드 시작 상태

라운드가 시작되면:

1. 맵 생성
2. 몬스터 20마리 생성
3. 플레이어 캐릭터 생성
4. 캐릭터와 몬스터 랜덤 배치
5. 플레이어에게 배치 수정 기회 제공
6. 플레이어가 캐릭터 위치를 변경
7. 전투 시작

---

# 36. 맵

프로토타입 맵은 20 × 20 크기의 Grid를 사용한다.

```text
20 × 20
= 400 Grid
```

각 Grid에는 캐릭터 또는 몬스터가 위치할 수 있다.

기본적으로 한 Grid에는 하나의 Controller만 위치할 수 있도록 한다.

추후 확장:

* 맵 크기 변경
* 라운드별 맵 크기 변경
* 장애물
* 지형
* 이동 불가 타일
* 특수 타일

등을 추가할 수 있도록 Map 시스템을 독립적으로 설계한다.

---

# 37. 전투 시작 전 배치

몬스터와 플레이어 캐릭터는 처음에는 랜덤한 위치에 배치된다.

플레이어는 전투 시작 전 자신의 캐릭터를 빈 공간으로 이동시킬 수 있다.

```text
랜덤 배치
    ↓
플레이어 배치 단계
    ↓
캐릭터 위치 수정
    ↓
전투 시작
```

전투 시작 후에는 캐릭터를 직접 조작할 수 없다.

---

# 38. 플레이어 캐릭터 선택

게임 시작 전 보유 캐릭터 중:

```text
최소 1명
최대 3명
```

을 선택할 수 있다.

선택하지 않은 캐릭터는 해당 게임에 참여하지 않는다.

---

# 39. 전투 Controller 구조

모든 전투 유닛은 Controller를 가진다.

```text
BaseController
├── PlayerController
└── CPUController
```

실제 전투 행동은 Controller가 결정한다.

진영:

```text
Player
CPU
```

---

# 40. Controller 기본 프레임 루프

게임은 60 FPS 기준으로 동작한다.

각 Controller는 매 프레임 다음 순서로 처리한다.

```text
1. 상태 확인
2. 타겟 탐색
3. 이동 판단
4. 이동 처리
5. 공격 가능 여부 확인
6. 기본 공격 처리
7. 무기 공격 처리
8. 사망 여부 확인
```

단, 실제 구현에서는 불필요한 타겟 검색을 매 프레임 수행하지 않도록 최적화할 수 있다.

---

# 41. 이동 AI

기본 이동 로직:

```text
내 Controller와 겹친 Controller 검색
        ↓
겹친 Controller 존재?
   ├── NO → '가장 가까운 상대 Controller 검색'으로
   │
   └── YES → 겹친 Controller 반대방향으로 이동(단, 바라보는 방향은 변경시키지않고 포지션만) 

가장 가까운 상대 Controller 검색
        ↓
상대 존재?
   ├── NO → 대기
   │
   └── YES
         ↓
상대가 내가 바라보는 방향에 존재?
         ↓
     ┌── YES ──────────┐
     │                  │
     ↓                  ↓
상대가 공격범위 안?     상대가 공격범위 밖
     │                  │
    YES                 NO
     │                  │
     ↓                  ↓
    대기             해당 방향 이동
     
     NO
     ↓
상대 방향으로 회전
```

---

# 42. 이동 로직 상세

## 42.1 가장 가까운 상대 검색

현재 Controller를 기준으로 가장 가까운 적 Controller를 검색한다.

```text
Target = FindNearestEnemy()
```

적이 없으면 아무 행동도 하지 않는다.

---

# 43. 방향 확인

현재 캐릭터의 Forward 방향과 Target 방향을 비교한다.

```text
Target이 바라보는 방향에 있는가?
```

아니라면:

```text
Target 방향으로 회전
```

한다.

프로토타입에서는 즉시 방향 전환 또는 설정된 Rotation Speed를 사용할 수 있다.

---

# 44. 거리 확인

타겟과의 거리를 계산한다.

```text
Distance = Distance(Self, Target)
```

그리고:

```text
Distance <= AttackRange
```

라면 이동하지 않는다.

공격범위 밖이면 타겟 방향으로 이동한다.

---

# 45. 공격 시스템

공격은 크게 두 가지다.

```text
기본 공격
무기 공격
```

---

# 46. 기본 공격 조건

기본 공격은 다음 조건을 모두 만족하면 발동한다.

```text
1. 살아있음
2. 공격 쿨타임 완료
3. 공격 대상 존재
4. 대상이 공격범위 안에 있음
5. 공격 방향이 대상 방향과 일치
```

조건을 만족하면 기본 공격을 실행한다.

---

# 47. 기본 공격 피해 계산

```text
Damage = Max(Attacker.Attack - Defender.Defense, MIN_DAMAGE)
```

예:

```text
공격력 = 100
방어력 = 30

피해량 = 70
```

예:

```text
공격력 = 30
방어력 = 100

피해량 = 최소 피해량
```

---

# 48. 무기 공격 조건

무기는 별도의 Cooldown을 가진다.

```text
Weapon Cooldown <= 0
```

그리고:

```text
Target이 존재
Target이 공격범위 안
```

이면 무기를 발동한다.

무기마다 발동 방식은 다를 수 있다.

---

# 49. 캐릭터 사망

캐릭터 HP가 0 이하가 되면 사망한다.

```text
CurrentHP <= 0
→ Dead
```

사망한 캐릭터는:

* 이동하지 않음
* 기본 공격하지 않음
* 무기 공격하지 않음
* 타겟 검색하지 않음

---

# 50. 게임 종료 조건

플레이어가 선택한 모든 캐릭터가 사망하면 게임이 종료된다.

```text
Player Character Alive Count == 0
→ Game Over
```

게임 종료 후 결과 화면을 보여준다.

```text
최종 라운드
획득 골드
획득 아이템
```

등을 표시한다.

---

# 51. 아이템 인벤토리 UI

아이템 창에서는 현재 보유한 아이템을 확인할 수 있다.

표시 정보:

```text
아이템 아이콘
아이템 이름
아이템 종류
아이템 등급
아이템 능력치
주인 캐릭터
장착 캐릭터
보유 수량
```

예:

```text
┌──────────────────────┐
│      아이템 정보      │
├──────────────────────┤
│ [아이콘] 철검         │
│ 타입 : 무기           │
│ 공격력 : +20          │
│ 주인 : 관우           │
│ 현재 장착 : 유비      │
│ 보유 : 2              │
└──────────────────────┘
```

---

# 52. 아이템 조합 UI

아이템 조합창에서는 동일 아이템 3개를 보유했을 경우 조합할 수 있다.

예:

```text
[철검] [철검] [철검]
          ↓
        [강철검]
```

UI:

```text
┌────────────────────────┐
│       아이템 조합       │
├────────────────────────┤
│                        │
│ [철검] [철검] [철검]   │
│                        │
│          ↓             │
│       [강철검]          │
│                        │
│       [ 조 합 ]         │
└────────────────────────┘
```

조합 가능한 아이템만 활성화한다.

---

# 53. 캐릭터 정보 UI

캐릭터 정보창에서는 다음 정보를 보여준다.

```text
캐릭터 이름
등급
현재 HP
최대 HP
공격력
방어력
공격속도
이동속도
공격범위
```

그리고:

```text
장착 아이템
```

을 보여준다.

예:

```text
┌────────────────────────────┐
│          관우               │
│           ★★★★             │
├────────────────────────────┤
│ HP       1,200 / 1,200      │
│ 공격력     150              │
│ 방어력      80              │
│ 공격속도    1.2             │
│ 이동속도    3.0             │
│ 공격범위    1.5             │
├────────────────────────────┤
│ 장착 아이템                 │
│ [무기] [방어구] [장식] [ ]  │
└────────────────────────────┘
```

---

# 54. 아이템 교체

캐릭터 정보창에서 장착 아이템을 선택하면 아이템을 교체할 수 있다.

```text
현재 아이템
      ↓
교체 버튼
      ↓
보유 아이템 목록
      ↓
새 아이템 선택
      ↓
장착
```

아이템 장착 시 기존 아이템은 인벤토리로 반환된다.

---

# 55. 아이템 슬롯 UI

캐릭터의 등급에 따라 슬롯을 표시한다.

예:

```text
1성
[■]

3성
[■][■][■]

6성
[■][■][■][■][■][■]
```

사용하지 않는 슬롯은 잠금 형태로 표시할 수 있다.

---

# 56. 전투 UI

전투 중 기본 UI:

```text
┌──────────────────────────────┐
│ Round 12              Gold 350│
├──────────────────────────────┤
│                              │
│          전투 맵             │
│                              │
│                              │
├──────────────────────────────┤
│ 유비 HP ████████              │
│ 관우 HP ██████                │
│ 장비 HP █████                 │
└──────────────────────────────┘
```

필요 정보:

* 현재 라운드
* 남은 몬스터 수
* 현재 골드
* 플레이어 캐릭터 HP
* 활성 시너지

---

# 57. 라운드 시작 UI

라운드 시작 전에는 배치 단계임을 명확하게 표시한다.

```text
ROUND 10

캐릭터를 배치하세요.

[ 전투 시작 ]
```

플레이어가 캐릭터를 원하는 빈 Grid로 이동시킬 수 있다.

전투 시작 버튼을 누르면 자동 전투가 시작된다.

---

# 58. 데이터 구조

Claude Code는 게임 데이터를 코드에 하드코딩하지 말고 데이터 기반으로 설계한다.

주요 데이터:

```text
CharacterData
ItemData
WeaponData
ArmorData
AccessoryData
MonsterData
SynergyData
RoundData
GachaData
UpgradeData
```

---

# 59. CharacterData

예시:

```text
CharacterData

ID
Name
Star
BaseHP
BaseAttack
BaseDefense
BaseAttackSpeed
BaseMoveSpeed
BaseAttackRange
ItemSlotCount
```

`ItemSlotCount`는 기본적으로 Star와 동일하게 계산한다.

---

# 60. ItemData

```text
ItemData

ID
Name
Type
Grade
OwnerCharacterID
BaseValue
Attack
HP
Defense
AttackSpeed
AttackRange
Cooldown
ScalingType
ScalingValue
TargetType
ProjectileType
SplashRadius
DropRate
```

실제로 필요하지 않은 데이터는 타입별로 분리해도 된다.

---

# 61. MonsterData

```text
MonsterData

ID
Name
BaseHP
BaseAttack
BaseDefense
BaseAttackSpeed
BaseMoveSpeed
BaseAttackRange
GoldReward
ItemDropTable
```

---

# 62. SynergyData

```text
SynergyData

ID
Name
RequiredCharacters
EffectType
EffectValue
Description
```

예:

```text
ID: SYNERGY_001
Name: 도원결의
RequiredCharacters:
- 유비
- 관우
- 장비

Effect:
HP +20%
```

---

# 63. RoundData

```text
RoundData

Round
MonsterCount
MonsterLevel
HPScale
AttackScale
DefenseScale
MoveSpeedScale
DropRateScale
```

기본 MonsterCount:

```text
20
```

---

# 64. 전투 아키텍처

Unity 프로젝트는 다음과 같은 구조를 권장한다.

```text
Game
├── GameManager
├── BattleManager
├── RoundManager
├── SpawnManager
├── MapManager
├── EconomyManager
├── InventoryManager
├── ItemManager
├── CharacterManager
├── SynergyManager
└── UIManager
```

---

# 65. Controller 구조

```text
Controller
├── BaseController
│
├── PlayerController
│
└── CPUController
```

BaseController가 공통 전투 로직을 가지고 PlayerController와 CPUController가 이를 상속한다.

다만 현재 게임은 전투 중 플레이어 직접 조작이 없으므로 PlayerController 역시 AI 기반으로 행동한다.

PlayerController와 CPUController의 차이는 **소속 진영**이다.

---

# 66. Unit 구조

각 전투 유닛은 다음 컴포넌트를 가진다.

```text
Unit
├── UnitStats
├── UnitHealth
├── UnitMovement
├── UnitTargeting
├── UnitAttack
├── WeaponController
├── EquipmentController
└── UnitController
```

컴포넌트 간 역할을 분리하여 향후 기능 추가가 쉽도록 한다.

---

# 67. 전투 상태 머신

각 Unit은 상태를 가진다.

```text
Idle
SearchingTarget
Rotating
Moving
Attacking
Dead
```

기본 상태 흐름:

```text
Idle
 ↓
SearchingTarget
 ↓
Target Found
 ↓
Rotating
 ↓
Target In Range?
 ├─ No → Moving
 │         ↓
 │       Target In Range
 │
 └─ Yes → Attacking
```

---

# 68. 타겟 탐색

타겟 탐색 규칙은 데이터/인터페이스 기반으로 확장 가능하게 한다.

기본:

```text
NearestEnemy
```

무기별:

```text
NearestEnemy
FarthestEnemy
LowestHPEnemy
HighestHPEnemy
```

---

# 69. 60 FPS 규칙

게임 전투 시뮬레이션은 60 FPS 기준으로 제작한다.

```text
Target FPS = 60
```

그러나 공격속도와 쿨타임 계산은 가능한 한 `Time.deltaTime` 기반으로 구현한다.

예:

```text
cooldown -= deltaTime

if cooldown <= 0:
    Attack()
    cooldown = 1f / attackSpeed
```

이를 통해 0.01~100 공격속도 범위를 안정적으로 지원한다.

---

# 70. 게임 속도

1차 프로토타입에서는 기본 게임 속도를 1배속으로 한다.

향후 확장:

```text
1x
2x
3x
```

등의 배속 시스템을 추가할 수 있도록 BattleManager가 게임 시간 배속을 관리할 수 있게 설계한다.

---

# 71. 몬스터 Spawn

라운드 시작:

```text
MonsterCount = 20
```

20개의 몬스터를 랜덤 Grid에 배치한다.

플레이어 캐릭터와 겹치지 않도록 한다.

```text
Spawn Position Validation

1. Grid 내부인가?
2. 이동 가능한 위치인가?
3. 다른 Unit이 존재하지 않는가?
4. 조건을 만족하면 Spawn
```

---

# 72. 랜덤 배치

캐릭터와 몬스터 모두 랜덤 배치를 기본으로 한다.

단, 완전 랜덤으로 인해 불가능한 상황이 발생하지 않도록 SpawnManager에서 유효 위치만 선택한다.

예:

```text
Player Spawn Area
CPU Spawn Area
```

를 별도로 지정하는 방식도 추후 사용할 수 있다.

---

# 73. 아이템 드랍 처리

몬스터 사망:

```text
Monster.Die()
       ↓
Gold Reward
       ↓
Item Drop Roll
       ↓
Inventory Add
       ↓
Round Reward Update
```

아이템 드랍은 몬스터가 장착한 아이템 리스트를 기준으로 각각 확률 판정한다.

---

# 74. 게임 종료

플레이어 캐릭터가 모두 사망하면:

```text
BattleManager
→ GameOver
```

게임 종료 화면:

```text
GAME OVER

최종 도달 라운드
: 27

획득 골드
: 840

획득 아이템
: 12개

[ 로비로 ]
```

---

# 75. MVP 구현 범위

Claude Code는 처음부터 모든 시스템을 완성하려 하지 않는다.

**1차 목표는 플레이 가능한 전투 프로토타입이다.**

## Phase 1 — Core Battle

먼저 다음 기능만 구현한다.

```text
1. 20 × 20 Grid Map
2. 플레이어 캐릭터 최대 3명
3. 몬스터 20마리
4. 랜덤 배치
5. 전투 전 캐릭터 재배치
6. 자동 이동
7. 타겟 검색
8. 방향 전환
9. 기본 공격
10. HP / 사망
11. 라운드 진행
12. 몬스터 스탯 스케일링
13. 게임 오버
```

이 단계에서 **실제로 게임을 실행하여 라운드가 진행되고 캐릭터와 몬스터가 서로 싸우는 것**을 최우선 목표로 한다.

---

# 76. MVP Phase 2 — Item

Phase 1이 안정적으로 동작하면:

```text
1. 아이템 데이터
2. 무기
3. 방어구
4. 장식
5. 아이템 장착
6. 무기 쿨타임
7. 무기 타겟팅
8. 발사체
9. 스플래시
10. 몬스터 아이템 장착
11. 아이템 드랍
12. 인벤토리
```

을 구현한다.

---

# 77. MVP Phase 3 — Item Crafting

```text
1. 동일 아이템 3개 판정
2. 조합
3. 상위 아이템 생성
4. 재료 아이템 제거
5. 조합 UI
```

을 구현한다.

---

# 78. MVP Phase 4 — Character

```text
1. 캐릭터 데이터
2. 1~6성
3. 캐릭터 보유
4. 캐릭터 선택
5. 캐릭터 슬롯
6. 캐릭터 정보 UI
7. 아이템 장착/교체
```

을 구현한다.

---

# 79. MVP Phase 5 — Synergy

```text
1. 시너지 데이터
2. 참여 캐릭터 검사
3. 조건 만족 여부
4. 효과 적용
5. 시너지 UI
```

을 구현한다.

---

# 80. MVP Phase 6 — Lobby

```text
1. 골드
2. 신규 유저 100골드
3. 캐릭터 뽑기
4. 뽑기 확률
5. 캐릭터 강화
6. 강화 비용 증가
7. 전투 시작
```

을 구현한다.

---

# 81. 개발 우선순위

다음 순서를 반드시 따른다.

```text
[1]
Grid / Map

↓

[2]
Unit / Controller

↓

[3]
Targeting

↓

[4]
Movement

↓

[5]
Basic Attack

↓

[6]
HP / Death

↓

[7]
Round

↓

[8]
Monster Scaling

↓

[9]
Item

↓

[10]
Equipment

↓

[11]
Weapon

↓

[12]
Item Drop

↓

[13]
Item Crafting

↓

[14]
Character Collection

↓

[15]
Synergy

↓

[16]
Lobby

↓

[17]
Gacha

↓

[18]
Character Upgrade

↓

[19]
UI Polish
```

---

# 82. Claude Code 구현 원칙

## 82.1 하드코딩 최소화

캐릭터와 아이템의 능력치를 코드에 직접 작성하지 않는다.

잘못된 예:

```csharp
if(character == "GuanYu")
{
    attack = 100;
}
```

권장:

```text
CharacterData
→ GuanYu Data
→ BaseAttack = 100
```

---

# 83. 데이터 기반 설계

가능하면 Unity ScriptableObject를 사용한다.

예:

```text
CharacterData : ScriptableObject
ItemData : ScriptableObject
MonsterData : ScriptableObject
SynergyData : ScriptableObject
RoundData : ScriptableObject
```

이를 통해 기획자가 코드 수정 없이 밸런스 데이터를 수정할 수 있도록 한다.

---

# 84. 시스템 간 의존성

각 시스템은 최대한 독립적으로 제작한다.

예:

```text
BattleManager
    ↓
RoundManager
    ↓
SpawnManager
    ↓
Controller
    ↓
Combat System
```

UI는 게임 로직을 직접 처리하지 않는다.

UI는 Manager 또는 Data를 통해 정보를 표시한다.

---

# 85. 저장 시스템

> **2026-09-15 정정** — 아래 "저장 대상"에 원래 있던 "Owned Items"는 삭제한다.
> 실제 구현·플레이 과정에서 "아이템 보유"까지 영구 저장하면 로그라이크의 핵심 전제
> (매 런은 항상 빈 인벤토리로 시작한다)가 깨진다는 게 드러났다 — 실제로 게임을 새로
> 시작해도 로비에 지난 런의 아이템이 그대로 남아있는 버그로 나타났다. 86절과 함께
> 정정한다.

로비에서 얻은 정보는 게임 종료 후에도 유지되어야 한다.

저장 대상:

```text
Gold
Owned Characters
Character Upgrade Levels
```

1차 프로토타입에서는 JSON 또는 PlayerPrefs 기반으로 구현할 수 있다.

실서비스 단계에서는 별도의 세이브/서버 시스템으로 교체할 수 있도록 인터페이스를 분리한다.

---

# 86. Persistent Data와 Run Data 분리

> **2026-09-15 정정** — "전투 데이터"라는 이름 대신 로그라이크 용어에 맞게
> "Run Data"(런 데이터)로 부른다. 예전 초안은 "아이템 보유"를 영구 데이터로,
> "전투 중 장착 상태"만 전투 데이터로 나눴는데 — 로그라이크에서는 아이템의
> **보유 자체**가 런 하나에만 의미가 있어야 한다(장착 상태만 런 종료 시
> 초기화되고 보유 수량은 남는다면, 그건 곧 "아이템이 로비에 쌓인다"는 뜻이라
> 매 런이 똑같이 시작할 수 없다). 실제로 이 잘못된 경계가 "로비에 아이템이
> 이미 있다" 버그의 직접 원인이었다.

중요한 원칙:

```text
Persistent Data (영구 데이터 — 앱을 껐다 켜도, 런이 몇 번을 실패해도 유지)
- 캐릭터 보유
- 캐릭터 강화(영구 강화 레벨 6종 — HP/공격력/방어력/공격속도/이동속도/사거리)
- 골드(로비 골드)
- (향후) 언락 데이터

Run Data (런 데이터 — 로그라이크 런 하나에만 존재, Game Over 즉시 전부 폐기)
- 현재 HP·현재 위치·현재 쿨타임·현재 라운드(전투 상태)
- 아이템 보유(인벤토리) — 몬스터가 드랍한 모든 아이템, 개수, 조합 결과
- 아이템 장착 상태(어느 캐릭터의 몇 번 슬롯에 뭐가 꽂혀 있는지)
- 이번 런의 골드(라운드 도중 모으는 골드 — 런 종료 시 Persistent Gold로 정산되고 사라짐)
```

게임 종료 시 Run Data는 전부 초기화되고 Persistent Data만 저장한다.
구현은 `SamgukBattleManager.Items.cs`의 `ResetRunData()`가 담당 — 새 런 시작
(`BeginRun()`)과 로비 진입(`ShowLobby()`, Game Over 직후 포함) 양쪽에서 호출된다.
`SamgukSaveManager`/`SamgukSaveData`(85절)는 Persistent Data만 다루고,
인벤토리/장비는 애초에 그 구조체에 필드 자체가 없다 — "저장을 안 한다"가 아니라
"저장할 방법 자체가 없다"로 만들어서 같은 버그가 재발할 수 없게 했다.

---

# 87. 로그 시스템

디버깅을 위해 주요 이벤트에 Debug Log를 제공한다.

예:

```text
[Battle] Round 1 Start
[Spawn] Monster 01 Spawned
[Target] GuanYu -> Monster01
[Attack] GuanYu dealt 80 damage
[Weapon] GuanYu activated BlueDragonBlade
[Drop] Monster01 dropped IronSword
[Round] Round 1 Clear
```

개발 완료 후 로그를 쉽게 끌 수 있도록 Debug 설정을 별도로 둔다.

---

# 88. 밸런스 데이터

모든 수치는 추후 쉽게 변경할 수 있어야 한다.

특히 다음 값은 반드시 데이터화한다.

```text
캐릭터 기본 능력치
성급별 능력치
몬스터 기본 능력치
라운드 성장률
골드 보상
아이템 드랍률
아이템 능력치
아이템 조합
무기 쿨타임
무기 공격력
무기 스케일링
공격속도
이동속도
공격범위
뽑기 확률
뽑기 비용
캐릭터 강화 비용
강화 성장률
시너지 효과
```

---

# 89. 1차 테스트용 캐릭터

프로토타입 테스트를 위해 최소 6명의 캐릭터를 제작한다.

예:

```text
유비 ★★★
관우 ★★★★
장비 ★★★
조운 ★★★★
조조 ★★★★★
여포 ★★★★★★
```

각 캐릭터의 실제 능력치는 테스트를 위한 임시 밸런스 값으로 설정한다.

---

# 90. 1차 테스트용 아이템

최소 다음 아이템을 구현한다.

### 무기

```text
철검
철궁
```

### 방어구

```text
가죽갑옷
철갑옷
```

### 장식

```text
옥 장식
청동 장식
```

무기 중 최소 하나는 근접, 하나는 원거리로 구현한다.

---

# 91. 1차 테스트용 시너지

최소 2개 이상의 시너지를 구현한다.

### 도원결의

```text
유비 + 관우 + 장비

효과:
최대 HP +20%
```

### 오호대장

예시:

```text
관우 + 장비 + 조운 + 마초 + 황충

효과:
공격력 +10%
```

시너지 조건과 효과는 Data로 관리한다.

---

# 92. 프로토타입의 성공 기준

Claude Code는 다음 상태까지 완성되면 1차 프로토타입을 완료한 것으로 판단한다.

### 게임 실행

```text
게임 실행
↓
로비
↓
캐릭터 선택
↓
전투 시작
↓
20 × 20 맵
↓
캐릭터 1~3명 랜덤 배치
↓
몬스터 20마리 랜덤 배치
↓
플레이어가 캐릭터 위치 변경
↓
전투 시작
↓
자동 이동
↓
자동 공격
↓
몬스터 사망
↓
골드 획득
↓
20마리 모두 사망
↓
Round Clear
↓
다음 Round
↓
반복
↓
캐릭터 전원 사망
↓
Game Over
```

이 전체 흐름이 중단 없이 동작해야 한다.

---

# 93. 중요 개발 원칙

Claude Code는 기능을 한꺼번에 구현하지 않는다.

각 Phase마다:

```text
구현
↓
컴파일
↓
에러 수정
↓
Unity 실행
↓
기능 테스트
↓
다음 Phase
```

순서로 진행한다.

특히 전투 시스템이 완성되기 전에 UI나 세부적인 그래픽 시스템을 과도하게 구현하지 않는다.

---

# 94. 향후 확장 예정 기능

현재 MVP에는 포함하지 않지만 시스템 구조는 다음 기능을 추가할 수 있도록 설계한다.

```text
영웅 스킬
궁극기
패시브
상태이상
치명타
회피
관통
넉백
범위 공격
연쇄 공격
보스 몬스터
엘리트 몬스터
특수 웨이브
맵 기믹
장애물
지형 효과
캐릭터 클래스
세력
추가 시너지
아이템 등급
아이템 랜덤 옵션
아이템 강화
캐릭터 승급
랭킹
시즌
```

---

# 95. Claude Code 작업 시작 지시

이 문서를 기준으로 Unity 프로젝트를 구현한다.

단순히 코드를 한 번에 생성하지 말고 다음 단계로 작업한다.

## Step 1

현재 프로젝트 구조를 확인한다.

## Step 2

기존 코드가 있다면 구조를 파악하고 기존 시스템을 최대한 재사용한다.

## Step 3

현재 기획서의 MVP 범위를 기준으로 필요한 시스템을 설계한다.

## Step 4

먼저 다음 기능을 구현한다.

```text
20 × 20 Grid
Player Unit
CPU Unit
Character Stats
Monster Stats
Spawn
Targeting
Movement
Rotation
Basic Attack
Damage
HP
Death
Round
Game Over
```

## Step 5

Unity에서 실제 플레이 테스트가 가능하도록 만든다.

## Step 6

컴파일 에러와 런타임 에러를 모두 수정한다.

## Step 7

기본 전투가 안정적으로 동작한 이후 다음 Phase로 진행한다.

---

# 96. 가장 중요한 게임 디자인 규칙 요약

```text
게임 장르
= 삼국지 로그라이크 디펜스

캐릭터
= 삼국지 실존/역사 등장인물 기반
= 1~6성

전투 참여
= 최소 1명 / 최대 3명

맵
= 20 × 20 Grid

라운드
= 라운드당 몬스터 20마리

전투
= 완전 자동 전투

전투 시작 전
= 캐릭터 위치 재배치 가능

기본 공격
= 근접 자동 공격

피해량
= Max(공격력 - 방어력, 최소 피해량)

공격속도
= 0.01 ~ 100.00
= 기본 1.00
= 초당 공격 횟수

이동속도
= 추가 스탯

공격범위
= 추가 스탯

아이템 슬롯
= 성급과 동일

아이템
= 무기 / 방어구 / 장식

아이템 주인
= 지정 캐릭터가 장착하면 아이템 능력 20% 추가

무기
= 별도 쿨타임
= 근접 / 원거리
= 발사체 / 스플래시 등 확장 가능

몬스터
= 아이템 장착 가능

몬스터 사망
= 골드 확정 드랍
= 장착 아이템 확률 드랍

아이템 조합
= 동일 아이템 3개
= 상위 등급 1개

시너지
= 전투 참여 캐릭터 조합에 따라 발동

예:
유비 + 관우 + 장비
= 도원결의
= HP 증가

캐릭터 사망
= HP 0

게임 종료
= 전투 참여 캐릭터 전원 사망

신규 유저
= 최초 로비 진입 시 100 Gold

뽑기
= 30 Gold
= 1~6성

캐릭터 강화
= 능력치 +1
= 강화 비용 지수 증가
```

---

# 97. 최종 개발 목표

이 게임의 핵심 재미는 다음 4가지다.

### ① 어떤 캐릭터를 데려갈 것인가?

```text
1명 안정형
vs
3명 조합형
```

### ② 어디에 배치할 것인가?

```text
근접 캐릭터
원거리 캐릭터
이동속도
공격범위
```

### ③ 어떤 아이템을 누구에게 줄 것인가?

```text
일반 캐릭터에게 강력한 아이템
vs
아이템 주인 캐릭터에게 20% 추가 보너스
```

### ④ 어디까지 살아남을 것인가?

```text
Round 10
Round 20
Round 50
Round 100
...
```

결국 플레이어는 매 게임마다 캐릭터와 아이템 조합을 최적화하면서 **“이번에는 어디까지 갈 수 있는가?”**를 반복해서 도전하게 된다.

따라서 전체 시스템은 향후 콘텐츠가 크게 증가하더라도 **캐릭터 / 아이템 / 몬스터 / 시너지 / 라운드 데이터를 추가하는 것만으로 콘텐츠를 확장할 수 있는 구조**를 목표로 한다.

---

# 96. 콘텐츠 대량 확장 + 캐릭터 조합/보스 웨이브 (2026-09-16)

§94에서 "향후 확장 예정"으로 미뤄뒀던 항목 중 세 개(아이템 등급·추가 시너지·보스 몬스터)를
이번 패스에서 실제로 구현했다. 웹 조사(나무위키/위키백과)로 실존 삼국지 소재를 찾아
캐릭터·아이템·몬스터·시너지를 대폭 늘렸다 — 특정 상용 게임의 일러스트/디자인은 베끼지
않고, 이름·역사적 배경만 참고해 이 프로젝트 고유의 절차적 스탯/스프라이트 배정으로 만들었다
(§10의 원칙 그대로).

## 96.1 캐릭터 로스터 24명(기존 6 + 신규 18)

1~6성 전 구간을 채웠다 — 기존 6명은 전부 3~6성이라 1~2성 풀이 비어 있었던 것을 이번에
신규 캐릭터로 메웠다.

| 성급 | 인원 | 신규 캐릭터 |
|---|---|---|
| 1성 | 3 | 화웅, 안량, 문추 |
| 2성 | 5 | 우금, 장합, 악진, 여몽, 황개 |
| 3성 | 4 | 장료, 마초, 태사자, 노숙 |
| 4성 | 3 | 서황, 황충, 육손 |
| 5성 | 2 | 주유, 제갈량 |
| 6성 | 1 | 사마의 |

## 96.2 캐릭터 조합(승급) — 가챠 중복을 재료로 (2026-09-16 사용자 확정)

- 가챠 중복 캐릭터는 더 이상 버려지지 않는다 — `ownedCharacterIds`를 HashSet(있다/없다)에서
  마릿수 카운트로 바꿨다(`SamgukBattleManager.Meta.cs`의 `ownedCharacterCounts`). 조조를
  또 뽑으면 조조가 2마리가 된다.
- 로비에서 같은 캐릭터 3마리 → 상위 등급 랜덤 1마리로 조합 가능(`TryCombineCharacter`).
  결과는 가챠와 같은 방식으로 그 등급 풀에서 무작위 선택 — "3마리를 합치면 정확히 그
  캐릭터가 레벨업"하는 게 아니라 상위 등급 캐릭터 아무나 하나가 나온다.
- 비용 = 재료 등급 × 100원(1성→2성 100, 2성→3성 200, 3성→4성 300 ...).
- 신규 유저 시작 지급 = 1성 서로 다른 2명 + 2성 1명, 총 3마리.

## 96.3 가챠 확률 — 조합 확률의 역산 (2026-09-16 사용자 확정 공식)

높은 등급일수록 "그 등급을 조합으로 만드는 데 필요한 하위 등급 3마리를 전부 뽑을 복합
확률"로 직접 뽑힐 확률을 역산했다: `P(N성) = P((N-1)성)^3`. P(2성)=25%, P(3성)=25%³=
1.5625%, P(4성)=1.5625%³≈0.00038%, 5~6성은 float 정밀도 이하로 사실상 0 — 가챠로는
못 얻고 조합으로만 만든다. P(1성)은 나머지 전부(≈73.44%).

## 96.4 아이템 로스터 23종(기존 7 + 신규 16)

- 일반 계열(무기/방어구/장식) 각각 1~3성 3단 조합 체인으로 통일: 철검→강철검→환두대도,
  철궁→강궁→각궁, 가죽갑옷→철갑옷→찰갑, 청동장식→옥장식→금장식.
- 4~5성은 삼국지 명검/명마 등 실존 소재 — 대부분 주인 캐릭터 전용(장착 시 +20%):
  청룡언월도(관우)·장팔사모(장비)·방천화극(여포, 스플래시)·의천검(조조)·청강검(조운,
  장판파 일화)·자웅일대검(유비)·적토마(관우, 이속 특화)·적로(유비)·절영(조조). 주인
  없는 최상급 아이템도 둘: 칠성보검(동탁 암살 미수 일화의 검), 전국옥새(만능 스탯).

## 96.5 몬스터 다양성 + 라운드/보스 스케줄

`SamgukMonsterData`에 `minRound`/`isBoss`/`bossStar` 필드 추가. 일반 스폰 풀은 라운드가
오를수록 누적 확장(이전 몬스터도 계속 나옴): 1라운드=동물(메뚜기/늑대/곰/호랑이),
4라운드=+도적(산적/해적), 7라운드=+정예부대(청주병/단양병/호표기, 실존 부대명 — 청주병은
조조의 황건적 출신 정예, 단양병은 도겸의 부대, 호표기는 조조의 정예 기병), 10라운드=
+황건적(기존).

10라운드부터 보스 웨이브가 추가로 뜬다(`SpawnBossWave`). 11라운드짜리 블록마다 보스
등급이 1씩 오르고, 블록 안에서는 라운드마다 마릿수가 1씩 늘어난다 — 10라운드=1성보스
1마리, 11라운드=2마리, ..., 20라운드=11마리, 21라운드=2성보스 1마리로 리셋, 이후 같은
규칙 반복. 보스 6명은 1~6성 캐릭터 로스터 중 하나를 그대로 재사용한다(화웅/장합/장료/
서황/주유/사마의) — 캐릭터로도, 보스 몬스터로도 등장해 같은 이름의 장수를 "내 편으로
얻거나 적으로 만난다"는 느낌을 노렸다.

> 사용자가 예로 든 세 수치(10라운드=1마리, 12라운드=2마리, 20라운드=10마리)가 서로 완전히
> 일치하지 않아서(하나의 등차수열로 셋을 동시에 만족 못 함), "라운드마다 1씩 증가, 11라운드
> 블록마다 등급 리셋" 쪽으로 통일해 해석했다 — 이 해석이 세 앵커 중 둘(10라운드=1,
> 20라운드≈10, 오차 1)에 가장 가깝다. 실제 원하시는 규칙과 다르면 알려주시면 다시 맞춘다.

## 96.6 시너지 5종(기존 2 + 신규 3), 팀 인원 3→5명

실존 삼국지 무장 그룹을 그대로 시너지로 옮겼다:
- 오호대장군(관우·장비·조운·마초·황충, 5명) — 유비가 한중왕에 오르며 봉한 다섯 맹장.
  공격력 +25%.
- 오자량장(장료·악진·우금·장합·서황, 5명) — 위나라의 다섯 정예 장수. 방어력 +25%.
- 사대도독(주유·노숙·여몽·육손, 4명) — 오나라 전략을 대대로 계승한 도독들. 체력 +22%.

5명 규모 시너지가 실제로 발동하려면 런당 선택 캐릭터 수가 3명으로는 부족해서(구조적으로
5명 시너지가 영원히 발동 불가능해짐), 팀 인원 상한을 3→5로 올렸다(`MaxRunTeamSize`,
`SamgukBattleManager.cs`). 사용자가 명시적으로 요청한 값은 아니고, "시너지를 완성해달라"는
요청이 실제로 작동하려면 필요해서 함께 조정한 것 — 관련해서 상한을 다르게 원하시면
말씀해달라.

## 96.7 리소스 — 아이콘은 부분 적용

기존 Kenney Tiny Dungeon CC0 팩(§RESOURCE_SOURCES.md 참고)을 재사용했다. 이 팩은 인간형
스프라이트가 10여 장뿐이라 신규 24개 대상(캐릭터 18 + 몬스터 15) 전부에 아이콘을 줄 수
없었다 — "캐릭터이자 보스로도 등장하는" 6명(화웅/장합/장료/서황/주유/사마의)과 몬스터
산적에만 실제 스프라이트를 배정하고, 나머지는 tintColor 사각형 폴백(이미 있는 안전한
대체 렌더링)으로 남겼다. 동물(메뚜기/늑대/곰/호랑이)·해적·청주병·단양병·호표기는 이 팩에
정확히 맞는 그림이 없어 억지로 안 맞는 스프라이트를 끼우지 않고 정직하게 색상 구분만
했다 — 다음에 동물 테마 CC0 팩을 구하면 채울 수 있는 지점으로 남겨둔다.

## 96.8 검증

라이브 리플렉션으로 전수 확인 — 로스터 개수(24/23/16/5)와 성급·등급 분포, 가챠 확률
20000회 시뮬레이션(1성 73.48%/2성 24.92%/3성 1.61%로 설계값과 오차범위 내 일치, 4~6성은
기대대로 0회), 캐릭터 조합 1회 실행(3마리 소모+상위 등급 1마리 획득+골드 정확히 차감),
5명 팀 선택 허용 + 6번째 거부, 오호대장군·용맹 시너지 동시 발동, 실제 `BeginRun`→
`BeginRound(1)`(몬스터 20/보스 0) 및 `BeginRound(21)`(몬스터 20/보스 1) 통합 실행까지
전부 기대값과 정확히 일치했다. 콘솔 로그 전수 확인 — 이번 작업 관련 에러/예외 0건(잡힌
에러 4건은 전부 이 환경의 기존 Pipeline exec 타임아웃/FMOD 경고로 무관).

## 96.9 아이콘 공백 — 절차적 생성 시도 후 전면 롤백(중요, 반드시 지킬 것)

**아이콘 없는 캐릭터 20명·몬스터 8종·아이템 540개를 Python(PIL)으로 직접 픽셀아트/벡터를
그려서 채웠다가 사용자에게 전면 리젝당했다** — "제발 md에 꼭기록해 너가 파이썬등으로
png, svg만들지좀마 처참해 토큰만쓰고 못쓰는 디자인만들어. 이번에 추가한거 싹지우고
웹에서 찾아서 교체해 절대 너가 그리지마". 당시 §96.7에서 기존 10장짜리 캐릭터 아이콘을
"손으로 그린 진짜 픽셀아트"로 오판했던 것도 원인 중 하나였다 — 실제로는
§RESOURCE_SOURCES.md에 이미 기록돼 있던 대로 **Kenney Tiny Dungeon(CC0)에서 다운로드한
진짜 타일**이었다. "기존 스타일이 hand-crafted처럼 보인다"고 그 출처를 다시 확인 안
하고 지레짐작한 게 "그럼 나도 그 스타일로 직접 그리면 되겠다"는 잘못된 결론으로 이어졌다.

**규칙(전 세션 공통, 이 프로젝트 전체에 적용) — 게임에 들어가는 그림 에셋(초상·아이콘·
일러스트 등)은 어떤 경우에도 Python/PIL 등으로 절차적으로 새로 그리지 않는다.** 항상
웹에서 CC0/CC-BY 같은 라이선스가 명확한 기존 에셋 팩을 찾아 다운로드해서 쓴다 — 이
프로젝트에 이미 확립된 방식(§RESOURCE_SOURCES.md의 Kenney Tiny Dungeon·OpenGameArt
Generic Fantasy RPG Items, GoStop의 Wikimedia Hwatu SVG·Twemoji·OpenMoji 등)을 그대로
따른다. **다운로드한 실제 이미지를 Python으로 크롭/리사이즈/색조 틴트하는 것은 허용** —
`leather_armor.png`(iron_armor 원본의 곱연산 틴트 파생본)처럼 "실제 존재하는 그림에서
시작한 변형"은 절차적 생성이 아니다. 구분 기준은 "빈 캔버스에 도형을 그렸는가"(금지)
vs "실제 파일을 열어서 잘랐거나 색만 바꿨는가"(허용).

**롤백 — 절차적으로 생성했던 568개(캐릭터 20+몬스터 8+아이템 540) 파일을 전부 삭제하고
해당 ScriptableObject의 `icon` 필드도 전부 null로 되돌렸다.** §96.7/§RESOURCE_SOURCES.md가
이미 명시한 "실제 에셋이 없으면 tintColor 사각형 폴백으로 정직하게 남겨둔다"는 원래
관례로 복귀한 것 — 이 폴백 자체는 이 프로젝트가 처음부터 써 온 정상적인 상태이지 퇴보가
아니다. 이후 실제 웹 리소스로 다시 채운 내역은 §RESOURCE_SOURCES.md에 이어서 기록한다.

## 96.10 함정 — 새 PNG를 추가하면 기존 스프라이트와 다른 기본 임포트 설정을 받는다

**§96.9에서 실제 CC0 에셋으로 캐릭터/몬스터/아이템 아이콘을 채운 직후 두 가지 시각
버그가 나왔다** — "Billboard 안에 Body는 5배는 해야 캐릭터가 정상으로 보이는듯"
(유닛이 훨씬 작게 렌더링됨), "캐릭터들이 도트 느낌인데 확대를 하니까 뿌옇게 나와"
(픽셀아트인데 확대 시 흐려짐). 둘 다 코드(`SamgukUnit.cs`의 body 스케일 등)를 고칠
문제가 아니라 **새로 추가한 PNG 595장(캐릭터 32+몬스터 16+아이템 547)이 Unity의
기본 텍스처 임포트 설정으로 들어간 것**이 원인이었다:

- `spritePixelsToUnits`(PPU) — 기존 스프라이트는 전부 **16**로 맞춰져 있다(16×16
  타일이 정확히 1×1 월드 유닛이 되도록, `SamgukUnit.cs`의 흰색 폴백 스프라이트도
  `pixelsPerUnit=1f`로 만들어 같은 1유닛 크기를 맞춘 것과 일관). Unity 기본값은
  **100**이라, 16×16 이미지가 0.16×0.16 유닛으로 렌더링돼 약 6.25배 작아 보인다
  ("5배는 해야" 라는 체감과 정확히 일치).
- `filterMode` — 기존 스프라이트는 전부 **Point**(0, 무보간)로 맞춰져 있다. Unity
  기본값은 **Bilinear**(1)라, 픽셀아트를 확대할 때 경계가 부드럽게 뭉개져 흐려 보인다.

**교훈 — 이 프로젝트에 새 PNG 스프라이트를 추가할 때는 항상 같은 폴더의 기존 파일
.meta와 `spritePixelsToUnits`/`filterMode`를 대조해서 맞출 것.** 이번엔
`TextureImporter`를 리플렉션 없이 정식 API로 열어(`AssetImporter.GetAtPath(path)
as TextureImporter`) `spritePixelsPerUnit=16f`, `filterMode=FilterMode.Point`를
설정하고 `SaveAndReimport()`로 일괄 수정했다 — Characters/Monsters/Items 세 폴더
595장 전부 확인(`ok=595 bad=0`). Play 모드에서 실제 배치된 유닛(곰/호랑이/늑대/벌/
장수)을 캡처해 격자 크기에 맞고 픽셀 경계가 선명한 것까지 육안으로 재확인했다.

## 96.11 캐릭터 초상 전면 교체 — 사용자 제공 Dynasty Kingdoms Pack 1/2 (2026-09-16)

**사용자가 직접 `Assets/Art/` 밑에 `Dynasty_Kingdoms_Pack_1`(QianGuo, 16종)·
`Dynasty_Kingdoms_Pack_2_Lianzhou`(LianZhou, 15종) — 총 31종의 64×64 픽셀아트
워리어 스프라이트를 넣어주고 캐릭터 이미지로 쓰라고 지시했다.** 라이선스 파일은
동봉돼 있지 않았다 — 사용자가 직접 보유/제공한 에셋이라 별도 출처 검증 없이 그대로
사용했다(§RESOURCE_SOURCES.md에도 같은 취지로 기록). `Large_*` 폴더는 같은
그림을 4배 단순 업스케일한 것뿐임을 픽셀 대조로 확인하고(diff 0), 원본 64×64만
채택했다.

**배정 — 30명 전원에 1:1로 실제 그림 배정, 재사용 없음.** 31종>30명이라 처음으로
캐릭터마다 완전히 고유한 그림을 줄 수 있었다(이전엔 13장으로 24슬롯을 채워야 해서
일부는 틴트 재사용이 필요했던 것과 대비된다). 성급 내림차순으로 정렬한 캐릭터
목록에, 각 팩의 "화려해 보이는(금장식·복잡한 구성) 뒤쪽 인덱스"부터 두 팩을
번갈아가며 배정해서 — 높은 성급일수록 더 화려한 그림을, 같은 성급 구간 안에서도
QianGuo/LianZhou 두 색감이 고르게 섞이게 했다. 남은 QianGuo 1종은 안 썼다.

**사용자 지시대로 원본 팩 폴더는 완전히 삭제했다** — "활용할것만 폴더링
Resources밑으로 해주고 다른건 지워줘"라는 명시적 요청에 따라, 이번엔 이 프로젝트의
기존 관례(Assets/Art에 원본 보관)를 따르지 않고 선택된 30장을 `Assets/Resources/
Sprites/Samguk/Characters/char_{id}.png`로 복사한 뒤 `Assets/Art/Dynasty_Kingdoms_Pack_1`/
`..._Pack_2_Lianzhou` 두 폴더(Large 변형·미리보기·미사용 1종 포함)를 통째로
지웠다 — Art 밑에 원본이 남아있지 않다.

### 함정 — 파일 내용을 통째로 갈아치워도 TextureImporter의 "Multiple 스프라이트"
슬라이스 설정은 안 없어진다

배정 직후 로비에서 "간옹"/"한현" 카드 두 개가 비어 보이는 버그가 났다 — 배정 전체
30명 중 23명이 같은 증상이었다(리플렉션으로 전수 확인). 원인: 이 파일들은 이전
세션(§96.9~96.10, 지금은 삭제된 Kenney/Tiny Creatures 크롭 단계)에서 **한 번이라도
`spriteImportMode`가 `Multiple`로 설정된 적이 있으면**, 그 `.png`의 실제 픽셀
내용을 완전히 다른 그림으로 덮어써도(`shutil.copy`) **`.meta`에 저장된 옛
슬라이스 좌표(예전 16×16 격자 기준)가 그대로 남아**, Unity가 새 64×64 이미지를
그 옛 좌표로 다시 잘라 "char_xxx_0" 같은 16×16짜리 부분 스프라이트만 만들어냈다.
`AssetDatabase.LoadAssetAtPath<Sprite>(path)`는 이 경우 첫 조각(_0)을 돌려주므로
`icon != null` 체크만으로는 못 잡는다 — **`sprite.rect`가 원본 이미지 전체
크기와 일치하는지까지 확인해야** 진짜로 걸러진다.

고침: `importer.spriteImportMode = SpriteImportMode.Single`을 명시적으로
강제하고 `importer.spritesheet = new SpriteMetaData[0]`으로 옛 슬라이스 데이터를
비운 뒤 재임포트했다 — 30명 전원 `sprite.rect == (64,64)`로 재확인.

**교훈 — 같은 파일 경로에 완전히 다른 그림을 덮어씌울 때는 파일 내용만 바뀌는 게
아니라 `.meta`의 임포트 설정(PPU·filterMode뿐 아니라 스프라이트 슬라이스 모드까지)
이 그대로 남아있을 수 있다는 걸 염두에 둘 것.** `icon != null` 같은 존재 여부
체크만으로는 부족하고, 실제 렌더 결과(스프라이트 크기·비율)까지 검증해야 한다.

## 96.12 장비 슬롯 = 성급으로 재환원 + 일괄 장착 버튼 + 골드 경제 전면 개편 (2026-09-16)

**장비 슬롯 개수 — §96(고정 3슬롯 결정)을 사용자가 다시 뒤집었다.** "장비슬롯이
4성이면 4개여야되는데 3개밖에안나옴 저번에도 리포트한거같은데"라는 재확인을 받고,
`SamgukCharacterData.ItemSlotCount`를 `FixedItemSlotCount(=3)`에서
`Mathf.Clamp(star, 1, 6)`으로 되돌렸다(1성=1칸 ~ 6성=6칸). 예전에 고정 3슬롯으로
바꾼 근거(ssam_info.md의 3부위×등급 표)와 정면으로 충돌하지만, 사용자가 명시적으로
재확인한 최신 지시를 그대로 따른다 — 장비 슬롯은 원래부터 아이템 타입을 안 가리는
범용 칸이라(`SamgukUnit.SetEquipment` 참고, 무기든 방어구든 아무 슬롯에나 낄 수
있다), 3칸을 넘는 여분 슬롯도 "그 캐릭터 전용 4번째 부위" 없이 인벤토리의 아무
아이템이나 담는 범용 칸으로 자연스럽게 동작한다 — 540개 아이템(캐릭터 30×슬롯3×
등급6) 데이터 자체는 안 건드렸다. `data.ItemSlotCount`/`slots.Length`를 쓰는 다른
모든 코드가 이미 값 자체를 동적으로 읽고 있어서(하드코딩된 "3" 없음) 이 필드 하나만
고치면 됐다.

**일괄 장착 버튼 신설.** "장비 일괄 장착 버튼도 추가해줘, 가장 높은 등급이고
전용장비 우선으로 착용되게"라는 요청 — `EquipAllBest()`(`SamgukBattleManager.Items.cs`)
가 배치된 전 캐릭터의 모든 슬롯을 순회하며, 인벤토리에서 구할 수 있는 최선의
아이템으로 채운다. 우선순위는 명시된 그대로 **1순위 등급 내림차순, 2순위(동일
등급 내에서) 전용장비 우선**(`IsBetterEquip`). 이미 장착 중인 게 더 좋으면 그대로
둔다(불필요한 인벤토리 요동 방지). 인벤토리 패널의 "일괄조합" 버튼 왼쪽에 같은
스타일로 배치했다.

**골드 경제 전면 개편 — "신규 유저 100골드 지급" 폐지, 라운드 클리어 + 장비 정산
방식으로 교체.**
- `SamgukSaveManager.Load()`의 신규 유저 기본 `persistentGold`: 100 → **0**.
- `BeginRun()`의 런 시작 지급 골드(`balance.startingGold`): 100 → **0**(더 이상 안
  읽음, 필드 자체는 남겨둠).
- **라운드 클리어 보상** — `RoundClearSeq()`(몬스터를 실제로 전멸시켜 이 코루틴이
  도는 시점)에서 `gold += round * 100` — 1라운드 클리어=100원, 2라운드=200원 …
  "클리어"가 트리거라 죽어서 못 끝낸 라운드는 자연히 제외된다(사후에 `round` 값만
  보고 계산했다면 "죽은 그 라운드"까지 잘못 포함될 뻔했다).
- **장비 정산** — 게임오버 직전(`HandlePlayerDied`)에 `ComputeItemSettlementValue()`
  (`SamgukBattleManager.Items.cs`)를 한 번 더해서 `gold`에 합산한다. 아이템 1개당
  가치는 `ItemSettlementValue`: 1등급=1원, 등급이 하나 오를 때마다 뒤에 0이 하나씩
  더 붙음(2~6등급=10/100/1,000/10,000/100,000원) — `Mathf.Pow` 대신 정수 곱셈
  루프로 계산해 고등급에서 부동소수점 오차가 안 생기게 했다.
  > **사용자가 준 두 예시("일반등급 1원, 희귀등급 10원")가 이 "등급마다 0이 하나씩"
  > 규칙과 정확히는 안 맞는다** — 등급명 기준으로는 일반=1등급, 희귀=3등급이라
  > 이 둘 사이에 100배 차이가 나야 하는데 예시는 10배 차이였다. "등급이 올라갈수록
  > 뒤에 0이 하나씩 더 붙는다"는 명시적 수식 쪽이 더 정밀한 규칙이라고 판단해
  > 그걸 그대로 구현했다(1/10/100/1,000/10,000/100,000) — 의도와 다르면 알려주면
  > 바로 맞춘다.
  > **"이미 죽은 캐릭터의 장비도 정산금에 포함되게 해달라"는 요청은 사실 이미
  > 코드 구조상 자연스럽게 성립해 있었다** — `equipment` 딕셔너리는 캐릭터 id
  > 기준으로 관리되고 유닛이 죽어도 안 지워진다(`BeginRound`의 기존 주석에도
  > 명시돼 있음). 다만 `ComputeItemSettlementValue`가 `playerUnits`(살아있는
  > 유닛만 남는 리스트)가 아니라 `equipment.Values`(전 캐릭터, 죽은 사람 포함)를
  > 직접 훑도록 짜야만 실제로 반영된다 — 이 부분만 명시적으로 챙겼다.
- `ShowGameOverUI`가 보여주는 "획득 골드"는 이제 (라운드 보상 합계 + 장비 정산액)의
  합이다 — 기존 `gold`(런 내내 누적되는 하나의 값)에 두 보너스를 그대로 더해
  넣는 방식이라, `ShowGameOver(round, gold)` 호출부·`persistentGold += finalGold`
  로직 자체는 손 안 대고 그대로 재사용했다.
- **몬스터 처치 골드(`HandleMonsterDied`의 `gold += u.GoldReward * ...`)와
  `GoldPercent` 시너지는 그대로 뒀다** — 사용자가 명시적으로 제거를 요청하지
  않아서, 기존 시스템을 건드리지 않고 새 보너스 두 가지를 그 위에 얹는 식으로
  최소 변경 범위를 지켰다.

## 96.13 아이템 드랍 테이블 전면 재설계 — 등급별 확률 차등 + 캐릭터별 전용장비
보스 종속 문제 해결 (2026-09-16)

**발단 — "관우 전용장비는 필드에서 드랍이 안 되더라" 버그 제보.** 조사 결과
드랍 로직(`RollItemDrops`) 자체엔 소유자/보유 여부 체크가 전혀 없었다 —
`ownerCharacterId`는 스탯 보너스·장착 우선순위 계산에만 쓰이지 드랍 판정에는
관여하지 않는다. **진짜 원인은 콘텐츠 배치 공백**이었다: 540개 아이템(30명×
3부위×6등급) 중 **1등급(90개)만** 16개 몬스터(필드10+보스6)의 `possibleDrops`에
들어가 있었고, 2~6등급(450개, 전체의 83%)은 어느 몬스터의 드랍 목록에도 없어
조합(3개→1개 승급)으로만 도달 가능했다. 그마저도 1등급 아이템조차 30명이
"필드10종에서 드랍(10명)" / "보스3종에서만 드랍(10명, 관우 포함 — 43~65라운드
이후에야 등장)" / "보스3종+30라운드 필드몹 1종(10명)" 세 그룹으로 쪼개져 있어서,
관우 같은 그룹은 사실상 라운드 40대 중반까지 그 캐릭터의 장비를 구경도 못 했다.

**재설계 — 전 몬스터가 전체 540개 풀을 공유, 등급으로만 확률 차등.** 사용자
요청("1성 아이템들은 확률 높게 6성아이템들은 확률낮게")에 맞춰 몬스터별 특화
드랍 목록이라는 구조 자체를 없앴다 — 16개 몬스터(필드+보스 전부) 전부
`possibleDrops = 전체 540개 아이템`으로 통일하고, `dropRate`(아이템 자산 자체에
붙은 전역 필드, 몬스터-아이템 조합이 아니라 아이템 하나당 확률 하나)만 등급별로
차등 부여했다:

```csharp
float RateForGrade(int grade) {
    float rate = 0.03f;
    for (int i = 1; i < grade; i++) rate /= 3f;
    return rate;
}
// 1등급 3% → 2등급 1% → 3등급 0.333% → 4등급 0.111% → 5등급 0.037% → 6등급 0.0123%
```

등급마다 1/3씩 줄어드는 완만한 기하감소를 골랐다 — 캐릭터 뽑기(`gachaRatesByStar`)
가 쓰는 세제곱 감소는 "가챠로는 5~6성을 사실상 못 뽑고 조합으로만 만든다"는
의도된 극단값인데, 아이템은 조합(3개→1개 승급)이 이미 상위 등급으로 가는 주된
경로라 직접 드랍까지 그렇게 희박하게 만들면 드랍 확률표 자체가 무의미해진다 —
그래서 "가끔 직접 나오면 럭키" 수준으로만 낮췄다.

**검증(Play 모드 라이브, 실제 `RollItemDrops` 2000회 몬테카를로).** `gom`
(1라운드부터 나오는 일반 필드 몬스터)을 소스로 2000번 처치를 시뮬레이션한 결과:

| 등급 | 회당 평균 드랍 개수 | 설계 기대값 |
|---|---|---|
| 1(일반) | 2.746 | 2.7 |
| 2(고급) | 0.932 | 0.9 |
| 3(희귀) | 0.3005 | 0.27 |
| 4(영웅) | 0.1025 | 0.09 |
| 5(전설) | 0.0355 | 0.027 |
| 6(신화) | 0.0145 | 0.009 |

회당 총 드랍 개수 평균 4.13개(재설계 전 30개 항목×0.15 균일 확률 기준 평균
4.5개와 비슷한 체감 유지). `guanYuG1DroppedFromFieldMonster=True`로 관우 전용
1등급 무기가 `gom`에서 실제로 드랍되는 것까지 확인 — 신고된 버그가 해결됐다.
디스크 검증도 병행(`guan_yu_weapon_g1.asset`의 GUID가 `gom.asset`과
`boss_hwaxiong.asset` 양쪽 `possibleDrops`에 정확히 1회씩 존재).

> **함정 — 테스트 스크립트가 처음엔 `RollItemDrops`를 2000번 호출해도 드랍이
> 0개였다.** `SamgukUnit.SourceMonsterData`가 `NonPublic` 바인딩 플래그로만
> 리플렉션 필드를 찾다가(실제로는 `public` 필드) 못 찾아서, 조용히 아무 값도
> 안 채워진 유닛으로 `RollItemDrops`를 호출한 것이었다(`src == null`이면
> 함수가 즉시 반환) — 필드 자체를 직접 `unit.SourceMonsterData = gom;`으로
> 대입하니 바로 정상 작동했다. **리플렉션으로 필드를 못 찾을 때는 `public`/
> `NonPublic` 바인딩 플래그 조합부터 의심할 것.**

> **함정(재확인) — 테스트로 오염된 `inventory`(런 중 아이템 보유 딕셔너리)는
> 테스트 직후 반드시 비울 것.** 이번엔 저장 데이터가 아니라 순수 런타임
> 전용 상태(`SamgukSaveData`에 절대 안 저장됨, 96.9 이전 세션에서 이미 확정된
> 설계)라 세션이 끝나면 자동으로 무해해지지만, 같은 Play 세션이 계속 이어진다면
> 테스트가 쌓아둔 8262개의 가짜 아이템이 그대로 인벤토리에 남으므로 검증 직후
> `inventory.Clear()`로 정리했다.

**변경 안 한 것.** 몬스터 종류별(필드 vs 보스) 드랍 확률 차등이나 보상 배율은
이번에 추가하지 않았다 — 사용자가 등급 기준 차등만 요청했고, 몬스터 타입 차등은
별도로 요청받지 않은 판단을 얹으면 또 다른 "동의 안 된 설계"가 될 수 있어
의도적으로 배제했다. 필요하면 보스 처치 시 드랍률 배수 등을 추가로 요청할 것.

## 96.14 캐릭터 강화(레벨업) — 절대값 +1 → 자기 기본 스탯 대비 % 로 전면 전환
(2026-09-16)

**발단 — "DPS 관점으로 강화할 때 골드는 동일하게 드는데 올라가는 수치 편차가
있는 것 같다."** 조사해보니 정확한 지적이었다 — 강화 비용(`SamgukBalanceConfig.
UpgradeCost`, `10 × 1.2^레벨`)은 30명 전원 완전히 동일한데, 레벨당 올라가는
실제 스탯은 `ComputeEffectiveStats`에서 **모든 캐릭터·모든 스탯이 무조건
"레벨당 절대값 +1"**(`atk += lv[1]` 등, 이름 붙은 상수조차 아니고 하드코딩된
리터럴 `1`)이었다. 30명의 기본 공격력이 13~53(4배 차이), 기본 공격속도가
0.8~1.3(체급상 +1이 거의 절반)이나 벌어지는데 절대값을 그대로 더하니:
- 공격력 강화 레벨당 %DPS 상승폭이 미축(기본공격13) 7.69% vs 조조(기본공격53)
  1.89%로 **최대 4배** 차이.
- 공격속도는 더 심각 — 기본값 자체가 0.8~1.3으로 워낙 작아서 **레벨 1번**에
  캐릭터 전원 77~125%씩 DPS가 치솟는, 사실상 밸런스가 성립하지 않는 수준이었다.

**고침 — `SamgukBalanceConfig.upgradePercentPerLevel`(기본 2%) 신설, 레벨당
증가량을 "자기 기본 스탯 × 이 비율"로 전환.**
```csharp
float pct = balance != null ? balance.upgradePercentPerLevel : 0.02f;
hp += baseData.baseHP * pct * lv[0];
atk += baseData.baseAttack * pct * lv[1];
def += baseData.baseDefense * pct * lv[2];
aspd += baseData.baseAttackSpeed * pct * lv[3];
mspd += baseData.baseMoveSpeed * pct * lv[4];
range += baseData.baseAttackRange * pct * lv[5];
```
사용자가 요청한 범위는 "DPS 관점"(공격력·공격속도)이었지만, 나머지 4개
스탯(HP·방어력·이동속도·사거리)도 정확히 같은 "절대값+1 vs 캐릭터마다 몇 배
차이나는 기본값" 결함을 그대로 안고 있어서, 별도 판단을 더 얹는 게 아니라
이미 있던 결함을 동일한 수정 하나로 6개 스탯 전부에 일관되게 적용했다. HP·
DPS 둘 다 아니라 6개 스탯 전부를 하나의 `upgradePercentPerLevel` 비율로
묶은 건 "강화 시스템 자체가 %스케일이어야 한다"는 근본 원칙이 스탯 종류를
안 가리기 때문 — 대신 스탯별로 다른 비율을 주고 싶으면 이 필드를 스탯별
배열로 쪼개면 된다(지금은 굳이 나눌 근거가 없어 단일 값으로 뒀다).
2%는 예전 설계 문서(§13, "공격력 100→101 예시" = 1% 근사)와 비슷한 체감을
유지하면서 조금 더 실감 나게 넉넉히 잡은 값 — `SamgukBalanceConfig`의 다른
밸런스 값들처럼 인스펙터에서 바로 조정 가능하다.

**검증(Play 모드 라이브, `ComputeEffectiveStats` 직접 호출).** 기본 스탯이
극단적으로 다른 4명(미축 공13/속0.80, 조조 공53/속1.00, 제갈량 공38/속0.85,
유타 공15/속1.30)을 리플렉션으로 강화 레벨 0/1/10에 대해 직접 호출해 비교 —
**넷 다 예외 없이 레벨당 정확히 2.000%**(공격력·공격속도 각각), 10레벨씩
누적 시 **DPS 44.00%로 완전히 동일**하게 나왔다(수정 전이었다면 미축이
조조보다 몇 배는 더 크게 올랐을 상황). 골드 비용이 원래도 캐릭터 무관
동일했으므로, 이제 "골드 대비 DPS 효율"이 30명 전원 완전히 같아졌다.

> **함정 — 테스트가 `characterUpgrades`(강화 레벨, `SamgukSaveData`에 실제
> 영구 저장되는 메타 진행도)를 직접 덮어쓰고 끝에 `Clear()`까지 했다.**
> 인벤토리(런 전용, 절대 저장 안 됨)와 달리 강화 레벨은 `SaveMeta()`를 거쳐
> 디스크에 저장되는 진짜 영구 데이터라, 테스트 중 `SaveMeta()`를 한 번도
> 안 불러서 디스크 원본 자체는 안전했지만 메모리 상태는 테스트로 오염된
> 채 남아있었다 — 그대로 뒀다가 이후 세션에서 다른 이유로 `SaveMeta()`가
> 한 번이라도 불리면 실제 저장 파일까지 덮어써 진행도가 날아갈 뻔했다.
> `LoadMeta()`를 다시 불러 디스크 저장값으로 메모리를 원상 복구해 정리했다
> — **영구 저장 필드를 리플렉션으로 건드리는 테스트 뒤에는 반드시 그
> 필드를 채워주는 `Load*()` 함수를 다시 불러 복원할 것**(인벤토리 같은
> 런 전용 데이터는 그냥 Clear()면 충분하지만, 영구 데이터는 그걸로 안 된다).

## 96.15 캐릭터 방향에 따른 스프라이트 좌우 반전 (2026-09-20)

`SamgukUnit.SetFacing(Vector2 dir)`(`FacingDirection`이 갱신되는 유일한
지점 — 초기화 시 1회 + `Tick()`에서 타겟 방향이 허용각 밖일 때만) 끝에
`bodyImage.flipX = dir.x < 0f;`를 추가했다. `localScale.x` 부호를 뒤집는
대신 `SpriteRenderer.flipX`를 쓴 이유 — `bodyGo.transform.localScale`은
이미 아이콘 원본 비율(aspect)로 X/Y를 각각 계산해 둔 값이라(`Init` 참고),
부호만 뒤집으면 그 계산을 다시 할 필요 없이 안전하게 좌우만 반전된다.
`Mathf.Abs(dir.x) > 0.05f`일 때만 갱신해서, 거의 수직으로 바라볼 때(dir.x가
0에 가까울 때) 미세한 좌우 깜빡임 없이 직전 방향을 유지한다.

**기본 방향 가정 — 확인 안 됨, 반대로 보이면 알려줄 것.** `dir.x < 0`(왼쪽을
바라볼 때)만 `flipX=true`로 뒤집고, 기본(`flipX=false`)은 "원본 아이콘이
오른쪽을 보고 있다"는 가정이다 — Dynasty Kingdoms Pack 원본 아트가 실제로
어느 쪽을 기본으로 그렸는지 픽셀 단위로 확인하지 않고 통상적인 관례로
가정한 것. 실제로 캐릭터가 등을 돌리고 공격하는 것처럼 보이면 이
한 줄(`dir.x < 0f` → `dir.x > 0f`)만 뒤집으면 된다.

**검증(Play 모드 라이브, 리플렉션).** 관우 유닛을 실제로 `Init()`해서
`SetFacing`을 오른쪽/왼쪽/수직/대각선/데드존-경계 5가지로 순서대로 호출 —
오른쪽→`flipX=False`, 왼쪽→`True`, 수직(x=0, 직전이 왼쪽)→`True`(유지
확인), 오른쪽대각선→`False`, 데드존 내 미세한 x(직전이 오른쪽대각선)→
`False`(유지 확인) 전부 설계대로 정확히 나왔다.

## 96.16 코드 생성 GameObject/Component → 씬 GameObject/프리팹 전환 — 표준
작업 방향 확정 (2026-09-20, 항상 이 방향으로 작업할 것)

**사용자 지시(고정 원칙, 향후 모든 SamgukDefense 작업에 적용):** "게임오브젝트,
컴퍼넌트 등 코드에서 만들어내고있는거 전부찾아서 씬에 게임오브젝트 혹은
프리팹으로 만들어. 이건 기록해놓고 항상 이런방향으로 작업 해줘." — 새
기능을 만들 때도, 기존 코드를 고칠 때도, "이게 매번 코드로 새로 만들어지고
있는 게 맞나?"를 먼저 확인하고 씬 오브젝트(재사용 가능한 정적 구조)나
`.prefab` 에셋(반복 생성되는 구조)으로 옮길 수 있는지부터 판단한다. GoStop
프로젝트가 수십 세션에 걸쳐 이미 정립한 경계(CLAUDE.md "코드-UI의 광범위한
Prefab/GameObject 전환" 섹션)를 그대로 가져온다 — **매판 무작위로 바뀌는
콘텐츠(카드 배치처럼)는 코드가 계속 그리는 게 맞고, 구조가 고정인 것(팝업
틀, 정적 패널, 반복 행/카드의 "틀")만 프리팹으로 옮긴다.**

**전면 감사 결과(Explore 서브에이전트, 읽기 전용) — SamgukDefense 전체
`new GameObject`(18곳+CreatePrimitive 1곳)/`.AddComponent`(24곳) 호출을
9개 그룹으로 분류.** 우선순위(전환 적합도):

| 그룹 | 빈도 | 적합도 | 상태 |
|---|---|---|---|
| #6 Canvas/EventSystem/Camera/Light 부트스트랩 | 세션당 1회 | 최상 | **전환 완료(아래)** |
| #3 게임오버 화면 | 세션당 1회 | 최상(완전 정적) | 미착수 |
| #7 보드 바닥(Floor/BoardRoot) | 세션당 1회 | 최상(고정 그리드 크기) | 미착수 |
| #9 발사체(Projectile) | 매 발사(최고빈도) | 최상 | 미착수 |
| #8 유닛 스폰(플레이어/몬스터) | 매 스폰(고빈도) | 최상 | 미착수 |
| #1 전투 HUD 틀 | 세션당 1회(시너지 줄 제외) | 상 | 미착수 |
| #2 캐릭터 선택 패널 | 틀=1회/카드=로스터 변경마다 | 분리(틀만 프리팹) | 미착수 |
| #4 인벤토리/장비 패널 | 틀=1회/행=매 갱신마다 | 분리(틀+행틀만 프리팹) | 미착수 |
| #5 로비 UI | 틀=1회/카드+상세=매 액션마다 | 분리, 가장 동적 | 미착수 |

**구조적으로 프리팹 전환이 안 맞는 부분(그대로 코드 유지)**: `RefreshSynergyPanel`
(#1)·`RebuildEquipList`/`RebuildItemList`(#4)·`RebuildLobbyOwnedList`/
`RebuildLobbyDetail`/`RebuildCombinePanel`(#5)·`ShowCharacterSelectUI`의
카드 루프(#2) — 전부 매 갱신마다 실시간 데이터 기준으로 자식을 통째로
지우고 다시 그린다. 이런 곳은 "반복되는 행/카드 하나의 틀"만 프리팹으로
옮기고(지금은 그 틀조차 `MakeRect`/`AddLabel`로 코드가 매번 새로 그림),
실제 반복 생성·파괴 로직 자체는 그대로 코드에 남긴다.

**사전 확인한 것 — 기존 설계와 충돌 안 함.** `SamgukBattleManager.UI.cs`
헤더 주석이 "완전 독립 프로젝트 요청에 따라 공용 GameUI.prefab/GameUIManager를
안 쓴다"고 명시하는데, 이번 지시는 "공용 프리팹을 쓰라"가 아니라
"SamgukDefense **자체** 프리팹을 만들라"는 것이라 충돌하지 않는다 — 그대로
독립 씬 유지하면서, 그 씬 자신의 부트스트랩 오브젝트(Canvas 등)를 코드
생성 대신 씬에 실제로 배치해 두는 것으로 이해하고 진행한다.

**#6 전환 완료 — Canvas/EventSystem/Camera/Light를 씬에 실제로 배치.**
`SamgukBattleManager.UI.cs`의 `BuildStaticUI`/`EnsureEventSystem`/
`EnsureCamera`/`EnsureLight`가 `GameObject.Find`로 이름 검색 후 없으면
새로 만드는 방어적 패턴을 이미 쓰고 있었다(중복 생성 버그를 막으려던
용도) — 이 "이미 있으면 재사용" 절반을 그대로 살리고, **찾는 방법만
`[SerializeField]` 씬 참조로 바꿨다.** 필드가 비어 있으면(참조 안 잡힌
씬) 예전처럼 코드로 새로 만드는 폴백을 남겨서 회귀 위험 없이 전환했다 —
GoStop이 여러 세션에 걸쳐 확립한 "씬에 있으면 재사용, 없으면 생성" 패턴
그대로.

**뜻밖의 큰 발견 — `MakeRect`/`AddImage`/`AddLabel`/`GetOrAddButton` 헬퍼가
이미 전부 "부모 밑에서 같은 이름 찾기 → 있으면 그대로 재사용"을 하고
있었다.** 즉 최상위 오브젝트(#6)만 이름 검색의 한계(부모가 없어 `Find`
범위를 못 좁힘)로 새 필드가 필요했을 뿐, **그 아래 전체 UI 트리(#1~#5의
틀+템플릿, #7 보드 바닥)는 코드 수정이 전혀 필요 없었다** — `BuildStaticUI()`
를 Edit 모드에서 딱 한 번 직접 호출해 주기만 하면, 내부의 모든 `MakeRect`
호출이 "씬에 이미 있으면 재사용"을 자동으로 타므로 실행하는 순간 그
결과가 그대로 씬에 구워진다. 실제로 `BuildStaticUI()`를 리플렉션으로
Edit 모드에서 1회 호출 → `SamgukCanvas` 밑에 총 128개 오브젝트(HUD/
캐릭터선택/게임오버/인벤토리/로비 패널의 틀+반복 카드·행 템플릿 전부
포함) + `BoardRoot`/`Floor`가 함께 생성 → 씬 저장. **이로써 처음 계획했던
#6 하나가 아니라 #1·#2(틀+템플릿)·#3·#4(틀+템플릿)·#5(틀+템플릿)·#6·#7
전부가 한 번에 전환됐다** — 코드 한 줄도 더 안 건드리고, 오직 "언제
그 생성 코드를 처음 실행시켜서 씬에 저장해 두는가"의 문제였을 뿐이다.

**검증(Play 모드 라이브).** 재진입 시 `SamgukCanvas`/`SafeArea`/`TopBar`/
`GameOverPanel`/`BoardRoot`/`Floor`/`LobbyPanel`/`InventoryPanel`/
`CharacterSelectPanel`/`EventSystem`/`Main Camera`/`BattleSun` 전부 정확히
1개씩(중복 생성 없음) 확인. `ScreenCapture.CaptureScreenshot`(Overlay UI를
실제로 찍어주는, 이전 세션에 확립된 방법)로 로비 화면을 실제 촬영 —
캐릭터 카드·골드·강화 버튼 전부 정상 렌더링 확인. 콘솔은 이 세션 동안
`error`/`exception` 0건(기존에 있던 obsolete API 경고, "automated mode
아님" 알림뿐 — 전부 이번 작업과 무관).

**의도적으로 그대로 코드에 남긴 것** — `RefreshSynergyPanel`/
`RebuildEquipList`/`RebuildItemList`/`RebuildLobbyOwnedList`/
`RebuildLobbyDetail`/`RebuildCombinePanel`/`ShowCharacterSelectUI`의 카드
루프는 매 갱신마다 실시간 데이터 기준으로 자식을 통째로 지우고 다시
그린다 — 이 부분은 원래 감사 결론대로 그대로 코드에 남겨야 맞다(반복되는
카드/행 하나의 **틀**은 이미 이번에 씬에 구워졌으니, "몇 개를 만들지"만
런타임에 결정된다).

**아직 안 건드린 것 — #8(유닛 스폰)·#9(발사체).** 이 둘은 `BuildStaticUI()`
호출 흐름 밖(실제 전투 중 매 스폰/매 발사마다 실행)이라 이번 Edit 모드
1회 호출로는 안 걸렸다. 진짜 `.prefab` 에셋(`Instantiate(prefab)` + 파라미터
주입 방식)이 필요한, 이번 감사에서 가장 빈도가 높다고 표시된 두 그룹 —
다음 세션에서 이어서 처리할 것.

**주의 — 이번 검증 스크린샷에서 무관한 기존 버그 하나를 우연히 발견했다.**
로비 화면 하단에 "전투 시작" 버튼 텍스트가 한 번 더 겹쳐 보이고 그 위로
잘린 텍스트("캐릭...다")가 살짝 비친다 — `LobbyPanel`의 dim이 화면 전체를
덮게 앵커돼 있는데도(`Vector2.zero~Vector2.one`) 바닥 쪽 몇 px가 안 덮이는
것으로 보인다. **이번 세션에서 내가 건드린 어떤 코드와도 무관하다**
(오브젝트를 어디서 만드는지만 바꿨을 뿐 `BuildLobbyPanel`/`BuildBottomBar`의
좌표·앵커 값은 한 글자도 안 고쳤다) — SafeArea 컴포넌트가 이 환경(에디터
Game 뷰)의 `Screen.safeArea`를 실제 기기와 다르게 계산해서 생기는 기존
버그로 추정되지만 확정 조사는 안 했다. 이번 작업 범위 밖이라 손 안 댔고,
사용자에게 그대로 보고했다 — 다음에 고쳐야 하면 별도로 요청할 것.
